# Game2 AI v3

- 目标：少用 `call_llm`，先把调查深度和阶段推进打满。
- 变化：
  - 去掉调查阶段的 LLM 规划。
  - 先对所有 NPC 做固定开场问法。
  - 再围绕 `marks`、新证据名和 `hint` 做穷举式追问。
  - 只在最终提交答案前做 1 次 LLM 汇总。
- 设计判断：
  - `chat` 本身不直接走 LLM 计费，适合用来换调查进度和成就。
  - 当前榜单目标优先级高于 prompt 花样，因此先提高稳定探索覆盖率。
