# Game2 AI v5

- 目标：把总提问数和同步频率都压到接近高分轨迹的量级。
- 关键策略：
  - 每个剧本最多约 `34` 次 `chat`
  - 开场两轮固定问法
  - 中盘最多四轮，只围绕 `marks`、`hint` 和少量新证据追问
  - 只在关键节点刷新全量状态
  - 最终只做一次 LLM 结论汇总
- 适用判断：
  - 如果 `v3/v4` 的主要问题是运行成本过高，`v5` 应更容易稳定跑完。
