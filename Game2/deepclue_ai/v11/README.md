# Game2 v11

目标：基于 `v2` 做一次最小、可解释的稳定性修正。

改动：

- 将中途提问规划与最终答案归纳的 `temperature` 降到 `0.0`
- 对最终凶手增加候选约束
- 对第一案已知英文 ID / 中文名做别名归一化，优先输出判题更可能接受的写法
- 当模型输出不在候选列表中的凶手时，追加一次“只选候选人”的修复步骤

不做的事：

- 不增加更深的调查逻辑
- 不增加更多提问轮数
- 不引入复杂手写规则搜索
