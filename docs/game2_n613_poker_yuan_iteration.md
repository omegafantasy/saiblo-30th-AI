# Game2 n613 Poker/Yuan Iteration

Updated: 2026-05-10 UTC

## Current Thesis

This iteration stops treating the plateau as wording variance. The useful split is:

- Poker late ceiling is gated by stabilizing `606` inside a high-score path, then pursuing `606 -> 605/607/608`.
- Yuan late ceiling has two sibling branches: `704 -> 706` through vote custody/admin logs and `703/704 -> 707` through the night-run contact line. `708` likely needs a concrete follow-up to the biology-building runner, not generic "exchange secret" language.

## Evidence Before n613

- Best confirmed score remains `n607a` match `8140359`: `2837`, with Poker `401,402,404,501,503,504,601-604` and Yuan `703,704`.
- Poker `606` is confirmed in `n601f` match `8140311`, but it is not stable in the best-score path.
- Yuan `706` is confirmed in `n606a` match `8140353` and `n611a` match `8140639`; it can appear without `705`, so it should be treated as a `704` vote/admin branch rather than a `705` sequel.
- Yuan `707` is confirmed in `n612b` match `8140888`: the evidence is a contact record usable with a named student who had asked the "sports girl" for contact information.
- Still unseen before n613: Poker `605/607/608`, Yuan `708`.

## n613 Candidates

- `n613a`: based on `n607a`. Keeps the high-score structure, replaces late Poker official-DNA wording with `606` three-photo / left-arm `POKER` tattoo / organization roster / `605/607/608` wording, and adds a direct `707 -> 708` runner follow-up.
- `n613b`: based on `n612b`. Keeps the route that already reached `707`, but reframes post-`707` as a concrete task: find the biology-building runner and ask what he saw or handled, avoiding "exchange secret" jargon.
- `n613c`: based on `n601f`. Focuses on Poker after `606`: photo custody, Joker cloud/phone, Red-Q liaison logs, Yu Shuhua roster, who lured Joker to the cloakroom, and missing `605/607/608`.

## Saiblo Upload

Uploaded on the current token user `thebeginning`, not activated and not laddered. Public names and remarks are neutral.

- `n613a`: entity `21705`, code `150ccfc4fd4540fb90a49151adc9c1c2`, remark `r`, compile success.
- `n613b`: entity `21704`, code `db07f61156d64298a2173b02e1291c32`, remark `r`, compile success.
- `n613c`: entity `21706`, code `c0adad2d28cb4727966d929e19feb433`, remark `r`, compile success.

## n613 Results

Direct single-player room evaluations, two samples each:

| label | matches | scores | relevant evidence |
| --- | --- | --- | --- |
| `n613a` | `8141194`, `8141201` | `2797`, `2757` | first sample only reached `401,402,703`; second reached Poker `404,501,502` plus Yuan `703,704`; no `606+` and no `707+`. |
| `n613b` | `8141193`, `8141283` | `2717`, `2657` | both reached Yuan `707`, but not `708`; Poker was weak. |
| `n613c` | `8141195`, `8141200` | `2757`, `2757` | second sample reached Poker `501,404,502,503,504,505,601-604,606`; no actual `605/607/608`. |

Important new breakpoint from `n613c` match `8141200`:

- `606` content: Joker, the current Yu Shuhua identity, and the current Red-Q/liaison identity appear together in a photo, all with left-arm `POKER` tattoos.
- A later reply explicitly says the photo original is held by the speaker, Joker phone/cloud data has been extracted, `605` is in the speaker's hands, and `607/608` are police final dossiers, but no `605/607/608` evidence was actually added.
- This is a better breakpoint than the older generic "next evidence" failures: the next probe should immediately chase the same holder after that promise, not fan out broadly.

Important Yuan breakpoint from `n613b`:

- `707` is now reproducible in 2/2 n613b samples.
- Post-`707` replies repeatedly redirect to concrete records: biology-building access logs, monitoring, security office/guard records, student-union office, phone/QQ/WeChat records, and the named runner's usual location.
- The current post-`707` route still fails when it asks people to perform an abstract "exchange"; the next probe should instead ask the witness/security/runner for the biology-building access/monitoring record as a possible `708`.

## n614 Candidates

- `n614a`: based on `n613c`. Adds an immediate same-holder follow-up whenever a reply says `605` is held, the photo original is held, or `607/608` are final dossiers. The follow-up asks for evidence registration and the exact public condition, rather than another broad organization summary.
- `n614b`: based on `n613b`. Adds a post-`707` access-record branch: once a reply points to biology-building door access, monitoring, security office, student-union office, or phone/QQ/WeChat records, it asks those sources to produce the concrete record as `708` or final evidence.

Uploaded on `thebeginning`, not activated and not laddered:

- `n614a`: entity `21708`, code `e7115b88515c41fa9e1f1f52520ac073`, remark `r`, compile success.
- `n614b`: entity `21707`, code `ff1615022f6341e1bf92f9148698b04d`, remark `r`, compile success.

Direct room evaluations:

| label | matches | scores | relevant evidence |
| --- | --- | --- | --- |
| `n614a` | `8141374`, `8141418` | `2757`, `2797` | both reached Poker `606`; no `605/607/608`. |
| `n614b` | `8141373`, `8141493` | `2657`, `2657` | one sample reached Yuan `707`; one reached `705`; no `708`. |

Interpretation:

- The post-`606` same-holder follow-up did not unlock `605/607/608`. The useful reply shape is still a promise or deferral, not evidence registration.
- The post-`707` access-record path did not unlock `708`. It tended to redirect to student-union/contact logistics or a separate `705` autopsy branch.

## n615-n617 Summary

The later n-series probes kept using direct room evaluations, no ladder activation, and neutral public names/remarks.

Important results:

| label | matches | scores | relevant evidence |
| --- | --- | --- | --- |
| `n615a` | `8141721`, `8141805` | `2757`, `2757` | Poker `501-504`, sometimes `505`; no `601+` continuation in these samples. |
| `n615d` | `8141724`, `8141808` | `2717`, `2757` | one sample reached Poker `606` plus Yuan `703,704`; no new late evidence. |
| `n616a` | `8141822`, `8141906` | `2657`, `2797` | one sample reached Poker `606` and Yuan `703`; no new late evidence. |
| `n616b` | `8141823`, `8141908`, `8141988`, `8142065` | `2757`, `2717`, `2797`, `2717` | one sample reached Poker `404,502-504` plus Yuan `703,704`; no `606+` or `707+`. |

## n617 Candidates And Results

Uploaded on `thebeginning`, not activated and not laddered:

- `n617a`: entity `21719`, code `682e262308b3489da9998f6abe15e7f9`, remark `r`, compile success.
- `n617b`: entity `21720`, code `35011708b4b34921a11e6dbdd1c8f166`, remark `r`, compile success.
- `n617c`: entity `21721`, code `9fac76ccb705458c86be0729d7ab52bd`, remark `r`, compile success.
- `n617e`: entity `21722`, code `5d7d1317e1174f5f955242332c989282`, remark `r`, compile success.
- `n617d`: entity `21723`, code `920ebf8c7e9a4d5199a98868b7fab4c6`, remark `r`, compile success.

Direct room evaluations:

| label | matches | scores | relevant evidence |
| --- | --- | --- | --- |
| `n617a` | `8142368`, `8142383` | `2717`, `2797` | second sample reached Poker `501-505,601-604,606` and Yuan `703`; no `605/607/608`. |
| `n617b` | `8142367`, `8142382` | `2657`, `2657` | no tracked late Poker/Yuan evidence. |
| `n617c` | `8142370`, `8142424` | `2757`, `2757` | Yuan `703,704`; did not reproduce `706`. |
| `n617d` | `8142371`, `8142446` | `2757`, `2757` | second sample reached Yuan `707`; no `708`. |
| `n617e` | `8142369`, `8142425` | `2757`, `2757` | Yuan `703,704`; did not reproduce `706`. |

## Current Story Reconstruction

Poker:

- Stable visible plot: a masked Poker Mansion death. The corpse is found as "Club 5", but the useful chain treats the 7:30/12:00 living Club 5, the 8:50 victim, Joker, and Lin Yuzhi as an identity-swap puzzle.
- Stable early evidence: reception/Joker contact, arrival mapping, computer search about frozen knife handles, freezer plastic box, missing knives, restaurant/gate monitor `401/402`, and sometimes vehicle `404`.
- Stable middle evidence: `501` anonymous 500000 transfer after Yu Shuhua's consultation, `502` Club 5 and Joker chat, `503` special Club 5 invitation, `504` LYZ pendant, `505` old arrest warrant, `601-604` missing-girl and Liu Liwen/doctor background.
- Real late evidence: `606` is the Joker/Yu Shuhua/Red-Q three-person photo with left-arm `POKER` tattoos. It proves the organization layer is real.
- Unopened in this workspace scan: `405`, `605`, `607`, `608`. Post-`606` replies repeatedly say the photo original, Joker phone/cloud, `605`, or `607/608` final police dossiers exist, but the game does not register them as evidence.

Yuan:

- Stable visible plot: the player is an amnesiac detective around T University/Century Woods. Yuan Yingtong's phone is found by a rival who resembles her and competes for the teacher's quota. The phone is mostly wiped but keeps a 1am dark photo of a female corpse in lolita dress and chestnut wig.
- Stable early evidence: `703` Yuan's phone and `704` abnormal vote sheet. The vote has a one-vote win and a suspicious extra handwriting mark.
- `705` is real but sparse: Li Haitian autopsy, with back stabbing, massive blood loss, severed limbs, and a blue-backpack dolphin pendant. It connects Yuan's case to an older body-disassembly case but has not itself advanced stage.
- `706` is real but very rare: Li Haitian's U-disk from lost-and-found. It contains an electronics-department recommendation list and videos/photos of Li Haitian abusing Yuan and other girls.
- `707` is reproducible: a "sports girl" contact record, explicitly usable to exchange information with a named student who promised a killer secret.
- `708` has now been observed in read-only scan of the other thread's `sk548e0910qar/qas` rooms, not in this n617 line: it is "slightly recovered memory" after the exchange/tanbai, showing an employer email requiring a post-kill photo, a bloody white bathtub, and a gloved hand holding a knife.

## Stage And Ceiling Analysis

Full local room-result scan at this point covered `3890` `analysis.json` files. Relevant counts:

- Poker `405`: `0`; `605`: `0`; `607`: `0`; `608`: `0`.
- Poker `606`: `13` occurrences, max score `2797`.
- Yuan `705`: `10` occurrences, max score `2797`.
- Yuan `706`: `2` occurrences, max score `2757`.
- Yuan `707`: `44` occurrences, max score `2797`.
- Yuan `708`: `2` occurrences, max score `2757`, both from read-only `sk548e0910qar/qas` results.

Stage state is the key bottleneck:

- Poker currently reaches stage `3` in the third script. Even when `606` appears, the third script remains stage `3`; no observed run opens a Poker stage `4`.
- Yuan currently remains stage `1` in the fourth script. Even when `705/706/707/708` appear, the fourth script remains stage `1`; no observed run opens Yuan stage `2`.
- The best confirmed score is still `n607a` match `8140359`, score `2837`, with Poker `501,601-604,503,504` and Yuan `703,704`. It does not include `606`, `706`, `707`, or `708`.

Current conclusion:

- Poker is not merely missing wording for `606 -> 605/607/608`. The gate likely needs a different state condition: correct holder, police/final-dossier authorization, final accusation, or a hidden source that is not the current speaker.
- Yuan has at least two separate late routes: `704 -> 706` through lost-and-found/U-disk/admin records, and `707 -> 708` through contact exchange. These are not yet reliably coalesced, and neither has opened Yuan stage `2`.
- The next useful work should stop broad all-NPC sweeps. For Poker, isolate the post-`606` authorization/final-accusation condition. For Yuan, import the validated `707 -> 708` exchange mechanism into the current line, then immediately pursue post-`708` employer email, email/IP/payment, bathtub location, gloves/knife, and possible `709` or stage-2 trigger.

## n618 Candidates And Results

Uploaded on `thebeginning`, not activated and not laddered:

- `n618a`: entity `21727`, code `ba23bc375f094b30b92c7757a9036740`, remark `r`, compile success.
- `n618b`: entity `21729`, code `1e412bb581824e849b9f4c1efb764dbb`, remark `r`, compile success.
- `n618c`: entity `21728`, code `f19d45893115447bb7b07c6b429b5688`, remark `r`, compile success.
- `n618d`: entity `21730`, code `baef997e544f4c35ab949af3c4ff6b35`, remark `r`, compile success.

Direct room evaluations, three samples each:

| label | matches | scores | relevant evidence |
| --- | --- | --- | --- |
| `n618a` | `8142948`, `8142958`, `8143002` | `2517`, `2797`, `2797` | Poker final-accusation branch. `606` reproduced in 2/3 samples together with `501-505,601-604`; still no `605/607/608`. |
| `n618b` | `8142947`, `8142959`, `8142976` | `2757`, `2757`, `2797` | Poker authorization branch. It reached Poker stage3 but did not reproduce `606`; no `605/607/608`. |
| `n618c` | `8142950`, `8142965`, `8143162` | `2657`, `2717`, `2797` | Yuan contact-exchange import. `707` reproduced in 3/3 samples; no `708/709`, no Yuan stage2. |
| `n618d` | `8142949`, `8142961`, `8143068` | `2617`, `2757`, `2717` | Yuan U-disk-priority plus contact exchange. `707` reproduced in 2/3 samples; no `706/708/709`, no Yuan stage2. |

Score-factor interpretation:

- `n618a`: Poker `606` is real and reproducible, but the stronger final accusation wording still does not open `605/607/608`. This weakens the hypothesis that the missing piece is just "say the final accusation after 606".
- `n618b`: explicitly asking for public authorization/签字链 made the run less useful and often lost `606`, so this wording is not a productive branch.
- `n618c/d`: importing the visible `707 -> 708` exchange wording was insufficient. `707` itself is stable, but the exchange target either refuses/does not register the trade, or the missing gate is not the contact text alone.
- All 12 n618 samples remain in the known lattice. The highest `2797` rows have Rose stage6 at index `28`, Poker stage `3`, two Z/F `KeyError(8)` records, and Yuan stage `1`; no n618 row reaches the known best `2837`.
- Several n618 late rows still show server-side instability around high Z/F stage (`KeyError: '8'`, NPC API parse retries, and embedding timeouts). Treat isolated low samples as noisy, but the absence of `605/607/608/708/709` across all samples is meaningful.

Updated direction after n618:

- Poker should stop trying broad post-`606` paraphrases. The next useful isolation is structural: identify whether `605/607/608` are unreachable because the current script never advances beyond Poker stage3, because a hidden holder is not being asked, or because the answer/accusation fields rather than chat evidence trigger the branch.
- Yuan should stop assuming `707` automatically trades into `708`. The next useful isolation is to compare the exact read-only `sk548e0910qar/qas` `708` path against n618c/d and preserve any ordering/source differences, especially whether the exchange needs the contact holder first, the exchange target first, specific identity proof, or a different "memory recovery" phrasing.
