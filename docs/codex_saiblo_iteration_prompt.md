# Codex Saiblo Iteration Prompt

专注 `Game1` 的在线验证链路。

执行前提：

- 先看 `docs/codex_automation_runbook.md`
- 再看 `docs/codex_saiblo_objective_fixed.md`
- 再看 `docs/game1_code_truth.md`
- 只基于当前代码真值做在线验证

本轮默认动作：

1. 先做只读
2. 再确认当前候选
3. 再上传并等待编译
4. 默认先做非 ladder 验证
