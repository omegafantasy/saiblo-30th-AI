# Game2 迭代记录 2026-03-11

## 1. 新确认的事实

- Game2 并非只有榜单分数可看。
- 已确认可用链路：
  - `POST /api/batches/`
  - `GET /api/batches/<batch_id>/`
  - `GET /api/matches/<match_id>/`
  - `GET /api/matches/<match_id>/download/`
- `match download` 中含有完整调查轨迹，可用于回放分析。

## 2. 高分对手轨迹观察

样本：

- batch `75631`
- match `7421777`
- 对手：`admin / 跑通测试`

观察：

- 该 match 是对手自己的独立跑分
- 对手得分：`2357`
- 对手轨迹：
  - 剧本 0：`42` 步
  - 剧本 1：`41` 步
  - 两个剧本的 `murderer / motivation / method` 都正确
- 问法风格：
  - 以直接调查为主
  - 大量短问句
  - 阶段推进明显依赖稳定追问，不依赖复杂中途规划

## 3. 版本实验矩阵

### v3

- 路线：低 `call_llm`，但调查深、刷新频繁
- 上传版本：`4`
- code_id: `f6e649f86fd04e5c9fa47d5c1934ad4b`
- batch: `75632`
- 已知结果：
  - 对手主跑 `7421802`：`2357`
  - 我方主跑 `7421801`：`评测失败`

### v4

- 路线：低 `call_llm` + 降低刷新频率
- 上传版本：`5`
- code_id: `2e0b74743d96493781f7a937932229c0`
- batch: `75633`
- 已知结果：
  - 对手主跑 `7421806`：`2357`
  - 我方主跑 `7421805`：`评测失败`

### v5

- 路线：低 `call_llm` + 降刷新 + 压提问量
- 上传版本：`6`
- code_id: `abeec6c8706f4e5cbf6780b9490e44b9`
- batch: `75634`
- 已知结果：
  - 对手主跑 `7421813`：`2357`
  - 我方主跑 `7421812`：`评测失败`

### v6

- 路线：完全移除 `call_llm`
- 上传版本：`7`
- code_id: `bcecd5940e9545f1af2aa30cae128755`
- batch: `75635`
- 已知结果：
  - 对手主跑 `7421815`：`2357`
  - 我方主跑 `7421814`：`371`
  - 两个剧本都答错，但推进到了 stage `4` / `3`

### fast probe

- 路线：几乎立即提交答案的诊断版本
- entity: `g2fastprobe0311`
- code_id: `d0246e9ca1d845a794d18237abc70539`
- batch: `75636`
- 已知结果：
  - 我方主跑 `7421824`：`107`
  - 两个剧本各只走 `1` 步，全部答案错误

## 4. 当前判断

截至目前：

- 之前把 batch 语义理解错了，导致对 `exit_code=9` 和 `0 分` 的解释都偏了
- `exit_code=9` 不能单独视为失败
- `v3-v5` 的真正问题是“我方主跑评测失败”
- `v2` 目前仍是已验证的最强版本，真实分数 `607`
- `v6` 说明无 LLM 纯规则也能稳定出分，但当前只有 `371`

## 5. 当前最重要的后续动作

1. 以 `v2` 为真实强基线，继续做小步增强
2. 控制每剧本步数在 `20-24` 左右，避免重蹈 `v3-v5` 的失败路径
3. 重点优化：
   - 最终答案准确率
   - 针对性提问密度
   - 隐藏剧本的动机/手法抽取

## 6. 当天新增结论

- 原始 `v2` 文件与重传参考版本字节完全一致
- 但重传后的 batch `75646` 仍然出现我方主跑 `评测失败`
- 这说明“重传后变差”不是因为本地文件内容不同
- 更可能是平台侧不稳定性、评测环境差异，或代码本身存在偶发性失败点

因此新版本策略调整为：

- 不再大改调查框架
- 回到 `v2` 主线做极小改动
- 新版 `v9` 只补“手法追问”这一处

当前版本状态：

- 本地版本：`v9`
- Saiblo 线上版本：`10`
- code_id: `3d5d8a5830c34c6fae7792b2edc5770f`
- batch: `75660`
- 状态：评测中

## 7. 新发现的后端问题

通过对 `7422408` 的回放与 `err` 日志解码，已经确认：

- 即使是 `admin/跑通测试` 这种高分成功对局
- 在第二个剧本推进到 `stage 8` 后继续 `chat`
- 也会触发后端异常：
  - `Stage.py`
  - `update_npc_question_marks`
  - `KeyError: '8'`

这说明：

- 高阶段继续追问本身就是风险点
- 一部分“我方主跑失败”未必是 AI 逻辑差，也可能是踩中了后端 bug

基于此，新开了一版稳定性验证：

- 本地版本：`v10`
- Saiblo 线上版本：`11`
- code_id: `f63fc8b0f3b44153b2e57bdd2cb1fb9b`
- 关键改动：`stage >= 8` 后不再继续 `chat`
- batch: `75662`
- 状态：评测中

## 8. 当天收束结论

继续核查后，当前已能明确确认：

- `v7`（线上 `version 8`）我方主跑 `7422058` 已完成，分数是 `407`
- 因此它没有超过 `v2`
- `g2v2ref0311` 的重传 `v2` 已经出现：
  - `7422137 = 407`
  - `7422139 = 607`

这说明当前最大问题不是“缺少更多新版本”，而是：

- 同一份 `v2` 代码本身就有明显分数波动
- 波动来源更像是：
  - 中途问题规划随机
  - 最终答案归纳随机

进一步的回放对比也说明：

- `v7` 额外增加的证据追问，让第一案嫌疑人从 `范敏敏` 偏到了 `叶文潇`
- 这类“更深、更细”的追问目前没有带来稳定增益
- 高分对手 `admin` 的强点是：
  - 问得更多
  - 但问题明显更短、更直接
  - 不是把多个线索塞进一条长问题

因此后续主线调整为：

1. 不再盲目新开版本
2. 先把 `v2` 的问题生成和最终答案生成尽量做得更稳定、更可复现
3. 新版本上线前，优先做重复 batch 验证，确认不是单次幸运高分

当天已按这个原则启动一组新的受控复测：

- 实体：`g2v2ref0311`
- batch：`75665`
- 对手：`admin/跑通测试`
- 目标：只测 `v2` 基线在相同强对手下的重复波动，不引入新代码

该复测现已完成：

- 我方主跑：`7422459 = 407`
- 对手主跑：`7422460 = 2357`

因此 `v2` 对 `admin` 的已知结果已经变成：

- `607`
- `407`
- `407`

这进一步确认：

- 当前主要问题确实是稳定性，而不是“版本数量不够”
- 至少在 `7421776 / 7422137 / 7422459` 这些样本里，两个 case 的初始 `hint / visible_npcs / evidences` 一致，不像是换题造成的分数差
- case0 还暴露出一个具体实现问题：
  - `FanMinMin` 这种英文 ID 写法未必被判题接受
  - 同一案中使用中文名 `范敏敏` 时成绩更好

所以下一步的正确方向不是继续加问题，而是：

1. 降低中途和最终 `call_llm` 的随机性
2. 对凶手名做候选约束和别名归一化

基于这组分析，当天又开了一个只做最小修正的新版本：

- 本地版本：`v11`
- Saiblo 线上版本：`12`
- code_id: `d76903b397804398965ebc51a3d0ac98`
- remark: `game2 v11 deterministic-alias`

改动严格限制为三项：

1. `llm_batch_plan` 的 `temperature` 调成 `0.0`
2. `final_answer` 的 `temperature` 调成 `0.0`
3. 给凶手输出加候选约束、别名归一化和非法候选修复

对应 batch：

- `75728`
- 对手：`admin/跑通测试`
- 状态：评测中

在等待 `75728` 的同时，对 `v2` 三场样本又做了一轮更细比较，新增了一个更重要的结论：

- `case0`
  - 现在更像是姓名别名和手法表述问题
  - 主调查骨架本身没有明显漂移
- `case1`
  - 才是当前总分波动的核心来源

高分 `607` 的 `case1` 特征：

- 主要追问 `NPC_E / NPC_D / NPC_B`
- 关键词更集中在：
  - `F 失联`
  - `QQ/账号`
  - `旧造谣`
  - `闺蜜`
  - `消失男生`

低分 `407` 的 `case1` 特征：

- 更容易漂到：
  - `Z`
  - `U盘`
  - `校医院`
  - `凌晨归还`
- 最差时会直接把凶手答成 `Z`

所以如果 `v12` 仍然无效，下一步最该做的不是继续加追问，而是：

1. 对 `case1` 显式锚定 `NPC_E` 线
2. 固化少量高价值模板问题
3. 降低 `Z/U盘/医院` 线在最终判断中的权重


## 2026-03-12 case0 定向分析

- `v12(online version 12)` 的 `807` 已确认主要来自 `case1` 稳定回到 `NPC_E + 动机正确`。
- 同批 `admin` 回放 `7428173` 已确认 `case0` 正解为：`崔安彦 / 动机正确 / 手法正确`。
- 这意味着此前把第一案主嫌疑人锚到 `范敏敏` 或 `叶文潇` 都是方向性错误，不是单纯措辞问题。
- `case0` 中最有价值的问题序列来自 `admin` 的短问句：
  - 先问所有人 `Rose是怎样的人` 与 `你今晚在做什么`
  - 再问 `杯子/花盆/吵架/关系/提前来/面纱/代演/18:40/威胁`
- 基于这个结论，新建 `v12(local) / online version 13`：
  - 第一案改为固定短问句脚本
  - 第一案最终答案改为 `崔安彦` 专门模板
  - 第二案与 `v11` 保持一致

- `version 13 / batch 75734` 已发起，但我方主跑 `7428541` 长时间停留在 `评测中`。
- 该现象目前更像平台层面的长挂风险，而不是立即可判的版本回退；原因是更早的 `75659/75660/75662` 也有同类长期未收口主跑。


## 2026-03-13 version 13 收口

- `version 13` 首次把第一案打到稳定满分：`7428541 / case0 = 3/3`。
- 这一结果证明：
  - 第一案过去的问题不是“大模型不够强”，而是嫌疑人锚点和问句设计错了。
  - 固定短问句 + 正确嫌疑人模板，在当前题库上明显优于泛化式调查。
- 但第二案在同题初始状态下仍从 `NPC_E` 漂到 `NPC_B`，说明第二案依旧是当前瓶颈。
