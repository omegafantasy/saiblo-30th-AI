# Game2 Saiblo 当前状态

更新时间：`2026-05-10 02:33 UTC+8 / 2026-05-09 18:33 UTC`

## 0. 2026-05-07 线上更新后的最新口径

Game53 已更新剧本与 NPC 命名机制。当前迭代记录见：

- `docs/game2_20260507_room_iteration.md`

## 0m. 2026-05-09 `n558/n559` Poker/Yuan 并发拓展

- 本轮继续只用单人房间评测，不使用 batch，不激活/不上天梯；新上传实体改用 `n558*`/`n559*` 中性名，备注统一 `r`。当前 token 为 `thebeginning / 2646`。
- Poker 追加两条路线均无上穿：`n558e` 三段式为 `2757 x2, 2717 x2`；`n558f` 记录补全为 `2757 x1, 2717 x2, 2617 x1`。结合只读复核未发现 Poker stage > 3，短期暂停 Poker 同族问句。
- Yuan 定向改法目前不优于原结构：`n558a` 角色定向为 `2757 x3, 2717 x1`；`n558b` 704/老师优先为 `2757 x3, 2717 x1`；`n558c` 证据携带已见 `2717 x3`，仍在跑。
- 中性复刻 `n559a` code_id `77c7137475e34163a4985b8f77bd8aa6` 已复现 `2797`：首轮 4 局为 `2797 x1, 2757 x2, 2717 x1`，追加扩样已见 `2697, 2717, 2757`。
- `2697` 很关键：它符合 `2657 + Yuan隐藏40`，说明 Yuan `+40` 在 Poker 未到顶格时也能外显；当前突破仍来自 `n556y1/n559a` 原四轮全员结构，而不是最终答案或新角色定向问法。
- 最新工作判断：当前应把 `n559a` 视为高上限基线继续扩样；`n559b` 老师优先首局 `2517`，负收益；`n559c/d` 只作为接近原结构的小对照继续小样本。

## 0l. 2026-05-09 `n556` Poker/Yuan 上限拓展

- 本轮不再围绕 `2757` 低尾做细节稳定性，改为 Poker/Yuan 激进拓展；仍只用单人房间，不用 batch，不激活/不上天梯，实体名 `n556*` 和备注 `r` 保持中性。
- 当前 token 实测为 `thebeginning / 2646`，不是此前口头提到的 `theend`；本轮上传均在该账号下完成。
- Poker 线没有新上限：`n556p1` 全员宽问后为 `2757 x4`；`n556p2` 改接待者嫌疑人与更细 Poker 答案后为 `2557 x2, 2517 x2`，负收益。
- Yuan 线取得实质突破：`n556y1`，code_id `42e01cb9dca44e448f9466976c720747`，完整骨架上增加四轮 Yuan broad/实际目击/证据推理/直接凶手证据链追问，但 Yuan 最终答案仍置零。
- `n556y1` 合计 18 个有效样本为 `2797 x2, 2757 x9, 2717 x4, 2657 x1, 2617 x1, 2517 x1`，另有服务端 UE x2。最高实测已从 `2757` 推到 `2797`。
- 两个 `2797` 均是 Rose fast、Z/F `err8=2`、Poker stage3 全证据、Yuan stage1 且 `001/703/704`。这证明此前隔离出的 Yuan `+40` 聊天/隐藏调查组件可以叠加到完整高分骨架；它不是 Yuan 最终答案分。
- 按旧 lattice 估算，`n556y1` 的 Yuan delta 为 `+40 x4, 0 x13, -200 x1`。外显 `2797` 只在 `+40` 与 Rose fast/Poker stage3/无隐藏 `-200` 同时满足时出现。
- Yuan 触发拆分：`n556y2` 提交推断 Yuan 答案为 `2757 x3`，无上穿；`n556y3` 删除末轮追问为 `2757 x3`，无上穿；`n556y4` 强化林晚舟/生物馆 witness 为 `2757 x2`，无上穿；`n556y5` 顺序反转为 `2757/2557/2657`，负收益。`n556y6/y7/y8` 仍在串行房间评测。
- 当前结论更新：理论上限没有到头，已实测 `2797`。但 `n556y1` 仍不是稳定版本；后续主方向应是拆 `n556y1` 的末轮 Yuan 追问触发条件，提高 `+40` 命中率，而不是继续 Poker residual 微调。

## 0a. 2026-05-08 最新稳定口径

- 当前 token 复核为 `thebeginning / user_id=2646`；本轮上传与评测均使用中性实体名和备注 `r`，未使用 batch，未上天梯。
- 单人房间评测链路已恢复；当前所有版本判断只看我方 raw score。
- 当前稳定基线仍是 `n518b`，code_id `ccab056263c540c4b61c1fec84828ac1`，25 个有效单人房间样本为 `2707 x16, 2667 x7, 2657 x2`，min `2657`、avg `2691.8`、max `2707`。
- 高上限但不稳定：`n525b` 25 样本 `2757 x20, 2717 x4, 2517 x1`；`n527e` 30 样本 `2757 x22, 2717 x6, 2657 x1, 2517 x1`；`n529b` 扩到 18 样本后一度 15 局无低尾，但第 18 局出现 `2457`；`n531b` 11 样本 `2757 x9, 2717 x1, 2557 x1`；`n534a` 20 样本 `2757 x10, 2717 x6, 2657 x2, 2557 x2`；`n539d` 36 样本 `2757 x23, 2717 x10, 2657 x2, 2557 x1`；`n542b` 首 5 局全 `2757`，扩到 9 局后为 `2757 x7, 2717 x1, 2557 x1`。这些高均值版本都不能稳定。
- 丢弃：`n520c/e` 扩样均出现 `2517`；`n520f` 出现 `2517 x2`；`n520d` 扩样出现 `2557`；`n521a` 扩样出现 `2507`；`n521b` 出现 `2457`；`n521c/d` 的 Poker stage3 答案文本改动稳定掉到 `2557`；`n522a/b` 门控候选均出现 `2617`；`n516d/e` Yuan 探针出现 `2617/2507`；`n528a` 去掉 Rose like 型问句后为 `2717 x4, 2627 x1`；`n528b` 把“喜欢你？”改成关系问句后仍出现 `2557`；`n530a/n531a/n531b` 的 stage3 兜底路线分别复现 `2517/2557/2557`；`n537a` Rose 显式冒充兜底扩样出现 `2617`；`n537b` Rose 顺序保守重排出现 `2257`；`n537c/d` Poker 自杀伪装答案文本均稳定掉到 `2517/2557` 档；`n540a` 只跳过已达 Poker stage3 后的总括问但 10 局出现 `2617/2557/2517`；`n541a/c` 的 Poker 拒证和 Rose 风险问控制扩样均出现 `2617`；`n542a/b` 信息源本人索证路线分别出现 `2557`；`n543a/b` 保守 stage2 救援分别出现 `2417/2507`；`n544a` Yuan 投票异常刷新 5 局出现 `2467`。
- 暂停中的最新隔离：`n544b` 从稳定基线只加 Yuan “谁在说谎”刷新，已完成 4 局 `2707 x2, 2667 x1, 2657 x1`，目前没有收益也没有足够样本；队列已暂停，未继续上传 `n544c/d/e`。
- 当前结论：确实进入瓶颈。Poker stage3 可把上限抬到 `2757`，但同形隐藏低尾仍不可预测；`n529b`、`n531b`、`n534a`、`n539d`、`n542b` 都出现过长前缀高分后突然低尾，说明小样本稳定假象很强。保守地只救 Poker stage1/stage2 也会破坏 `n518b` 的最低分。Yuan 已在 `n549c/d/e` 中发现 direct 骨架低频 `+40` 聊天组件，但 `n549f` 尚未证明它能叠加突破完整骨架 `2757`。Poker 自杀伪装剧情虽由 `203/204/205` 指向，但改最终答案文本会显著负收益。稳定版本仍应以 `n518b` 为准；若继续探索，应先找到新的可观测低尾过滤特征或可稳定触发的新计分组件，而不是继续扩同类 stage3、信息源索证、Rose 尾段或 Yuan 单问路线。
- 已重新生成 `docs/generated/game2_room_eval_summary.md`、`docs/generated/game2_room_score_factors.md`、`docs/generated/game2_story_unlocks.md`。

## 0b. 2026-05-08 分布审计与隔离实验口径

- 当前有效单人房间样本重新统计为 `1277` 个；最高仍是 `2757`，没有观测到 `>2757`。
- `2757` 的当前差分分解为 `2520` 基础包 + `137` Z/F hidden err8 + `50` Poker stage2 + `50` Poker stage3。这里的 `2520` 是“当前直接答案/基础调查包”，不是单个 Rose 分数；平台只暴露 Rose 的 `answer_result`，后续剧本不能精确逐案拆分。
- Rose 当前只确认三项全真；motivation false 在同档约 `-200`，stage6 由 `喜欢你？` 类触发常约 `-40`。Z/F 当前可证额外收益约 `+137`。Poker 当前可证额外收益约 `+100`。Yuan 当前没有证明稳定正增量，`703/704` 仍是小样本疑点。
- 在 Rose 全真且 Z/F 双 `KeyError('8')` 条件下，Poker 可见 stage 与分数台阶关系很清楚：stage None/1 最高 `2657`，stage2 最高 `2707`，stage3 最高 `2757`。
- 但 Poker stage3 不是稳定解。同一核心可见签名：Rose 全真、Rose stage6=`other`、Z/F 双 err8、Poker stage3、Poker evidence `001/002/101/102/103/104/201/202/203/204/205`、Yuan 只有 `001`，共有 464 个样本，分布为 `2517 x21, 2557 x41, 2627 x1, 2717 x122, 2757 x279`。可见日志无法解释这 `240` 分跨度。
- Yuan 的 `703/704` 不能证明独立正收益；三案 full 扩样中 Yuan zero/direct/probe 分别为 `2757 x12, 2717 x3`、`2757 x11, 2717 x2, 2657 x2`、`2757 x10, 2717 x4, 2517 x1`，没有任何上穿，且 probe 已出现隐藏低尾。
- “快速放弃非目标剧本”机制上可行，错误或低分前案后仍能进入后续案；但它只适合同一隔离骨架内做 A/B，不适合用绝对分数直接决定完整主线。
- 现在支持受控拆线评测，但不是“随便把其他案全零后外推”。Poker 线应固定 Rose+Z/F full + Yuan zero；Yuan 线应固定 Rose+Z/F+Poker full。每条线必须用中性实体名/备注、独立输出目录和固定 code_id 汇总。
- Saiblo 房间评测工具当前是串行；手工多进程并发技术上可行，但会增加 pending、编译延迟、限流与归因混淆。若以后并发，必须用不同中性实体、不同输出目录、固定 code_id、先小样本 sanity，再扩到 16+。

## 0c. 2026-05-08 `n547/n548` 置零拆分结论

- `n547a/n547b` 已确认 robust zero baseline：hard-zero 与 soft-zero 均为 `207 x5`，前案错答不阻塞后续四案。
- 直接答案贡献在 `n547i-o` 中严格可加：Rose direct `+600`、Z/F direct `+600`、Poker direct `+200`、Yuan direct/probe `+0`；Rose+Z/F+Poker direct 且 Yuan zero 为 `1607 x5`。
- `n547c+n547c_more` all-direct 合计为 `1607 x9, 1407 x1`，而 `n547o` 去掉 Yuan direct 后稳定 `1607 x5`；当前更倾向于把 `1407` 视为 direct 骨架中的偶发隐藏 `-200`，不是稳定 Yuan direct 负分。
- 单案 full 增量：Rose full 比 Rose direct 高 `+410/+450`，Z/F full 比 Z/F direct 高 `+600`，Poker full 比 Poker direct 高 `+100`。
- 组合隔离已补齐：`n548a` Rose full + direct 基底为 `2017 x2, 2057 x3`；`n548b` Z/F full + direct 基底为 `2207 x5`；`n548c` Poker full + direct 基底为 `1707 x4, 1607 x1`；`n548d` Rose+Z/F full + Poker direct 为 `2657 x4, 2417 x1`；`n548e` Rose+Poker full + Z/F direct 为 `2157 x3, 2117 x1, 1957 x1`；`n548f` Z/F+Poker full + Rose direct 为 `2307 x5`。
- `n548d` 的同形 `2417/2657` 是新关键点：隐藏低尾不只发生在 Poker stage3，Rose+Z/F full 组合已经能产生 `240` 分跨度。
- 新增 `rose_stage6_index` 因子后，`rose6_idx=28` 与 `rose6_idx=29..33` 可稳定解释一个 `-40` 档位；例如 `n548a` 的 `2057 -> 2017`、Poker stage3 的 `2757 -> 2717`。剩余 `-200` 低尾仍未被 Z/F stage、err8、文本关键词解释。
- 当前可操作分数格子：`2657` 是 Rose+Z/F full + Poker direct 高档，`2707/2757` 分别是 Poker stage2/stage3 各加 `+50`；`2617/2717` 是 Rose late `-40`，`2457/2507/2557` 是隐藏 `-200`，`2417/2467/2517` 是两者叠加。这个格子比“Poker stage3 不稳定”更精确。
- 三案 full + Yuan 条件扩样：`n548g` Yuan zero 为 `2757 x12, 2717 x3`；`n548h` Yuan direct 为 `2757 x11, 2717 x2, 2657 x2`；`n548i` Yuan probe 为 `2757 x10, 2717 x4, 2517 x1`；`n548j` direct + Yuan probe 为 `1607 x5`。Yuan 仍没有独立正分证据。
- 已有 `1277` 个有效因子样本中没有任何 `>2757`；Yuan `704` 或 `703/704` 样本也只到 `2757`，尚不能证明 Yuan 是独立正组件。
- 隐藏 `-200` 再挖掘：此前 `990` 个 comparable 样本和 `524` 个 Poker stage3 comparable 样本中，sample_count、记录长度、case index、reply 数、evidence 数和常见文本关键词均不能稳定过滤低尾；`n548i` 出现 `2517` 后，原先 Yuan `704/703+704` 的 8 个小样本 0 低尾条件已被反驳。
- 置零机制已确认可行，但后续实验应优先用“非目标 direct 正确答案、目标 full/probe”的 `1607` 基底做 A/B；全零 `207` 基底只适合验证后案不中断，不适合判断完整主线收益。
- 完成度审计：置零、direct 基础分、单剧本 full 增量、两两 full、三案 full 与 Yuan 条件贡献已经足够指导下一轮受控 A/B；隐藏 `-200` 低尾仍未解释。
- Saiblo 恢复 watcher 于 `2026-05-08 21:33:51 UTC` 触发回调，`n548d_more2`、`n547c_more`、`n548e-j` 已补齐，随后 `n548g/h/i_more2` 也完成扩样。补跑脚本 `Game2/runtime/recovery_eval_queue/20260508_175135/retry_n548_failed.sh` 不再是阻塞项。

## 0d. 2026-05-09 `n549` Yuan `+40` 复核

- 当前有效因子样本已刷新到 `1925` 个；最高仍是 `2757`，仍未观测到 `>2757`。
- `n549c/d/e/h` 证明 direct 骨架中存在低频 Yuan `+40` 聊天组件：`n549c` 为 `1607 x2, 1647 x1`，`n549d` 为 `1607 x8, 1647 x1, 1407 x1`，`n549e` 为 `1607 x8, 1647 x1, 1407 x1`，`n549h` 为 `1607 x9, 1647 x1`。
- `n549e` 的 Yuan 最终答案强制置零后仍出现 `1647`，所以这个 `+40` 不是 Yuan 最终答案文本分，而是聊天/隐藏调查组件。
- `n549g` 去掉 targeted 三问后为 `1607 x8, 1407 x2`，没有 `1647`；`n549h` 保留 targeted 三问、去掉后段 followup 后仍有 `1647 x1`，因此 targeted 阶段比后段 followup 更接近触发源。
- `n549i/j/k` 把 targeted 阶段拆成单问：投票异常单问 `n549i` 为 `1607 x10`，照片/死亡时间单问 `n549j` 为 `1607 x9, 1407 x1`，生物馆实际目击者单问 `n549k` 为 `1607 x8, 1647 x1, 1407 x1`。因此当前唯一能单独复现 `+40` 的是生物馆/实际目击者问句。
- `n549l/m/n` 进一步拆生物馆问句：去掉 broad 前置的 `n549l` 为 `1607 x10`，收窄为只问真实看到的人/时间/地点的 `n549m` 为 `1607 x10`，尝试只追问路线/目击者型 NPC 的 `n549n` 为 `1607 x8, 1647 x2`。但 `n549n` 轨迹复核发现过滤器常因泛化手机字样排空目标并回退全员，因此不能证明目标选择有效。
- `n549o/p` 复核目标选择：修正 witness 过滤且不回退全员的 `n549o` 为 `1607 x9, 1407 x1`，固定只问 Yuan 可见 NPC 列表最后一人的 `n549p` 为 `1607 x9, 1647 x1`。单 NPC 追问没有提高触发率。
- `+40` 仍没有可见 stage/evidence 标识：相关样本均停在 `yuan_stage=1`，证据仍是 `001/703/704`。
- `n549f` 把该聊天置零探针叠加到 `n548g` 的 Rose+Z/F+Poker full 骨架后，15 局为 `2757 x11, 2717 x2, 2617 x1, 2557 x1`，没有 `2797` 或任何 `>2757`。
- `n550a` 改用全零骨架，只在 Yuan broad + 宽生物馆问后把“实际从生物馆跑出的人”作为凶手提交，10 局为 `207 x8, 247 x2`。这证明 `+40` 在其他三案全零时也能独立出现；同时 `247=207+40` 说明生物馆实际目击者最终答案仍没有拿到 Yuan 答案分。
- `n550a` 两个 `247` 轨迹都没有可见 stage/evidence 变化；一个回复为“张朔从生物馆跑出”，另一个回复为“许清和从生物馆出来”但本地提取器没有抓到，仍然拿到 `+40`。因此 `+40` 更像宽生物馆问句命中的隐藏聊天分，而不是提交 murderer、特定人名或提取逻辑导致。
- 结论更新：Yuan 线确实有此前未拆出的 `+40` 小组件，但目前触发率低且没有证明能叠加到完整高分骨架；Yuan 最终答案仍没有任何正分证据。独立测试可以继续推进候选触发条件，但只能在固定骨架内做 A/B；当前突破口收窄为“broad 后用宽生物馆/世纪林/尸块关系问句纠正张壹传闻”。目标 NPC 选择和生物馆嫌疑人答案均未提高触发率或答案分，后续应优先研究全员宽问的顺序、角色组合和随机身份条件。

## 0e. 2026-05-09 `n550b-g` Yuan `+40` 顺序/答案复核

- 当前有效因子样本已刷新到 `2260` 个；最高仍是 `2757`，仍未观测到 `>2757`。
- 所有本轮上传继续使用中性实体名 `n550b` 到 `n550g` 和备注 `r`；未使用 batch，未上天梯，上传账号守卫复核为 `thebeginning`。
- `n550b`：全零骨架，Yuan broad 两问 + 原顺序宽生物馆问，最终答案置零，12 局 `207 x12`。
- `n550c`：只把宽生物馆问改为逆序，最终答案置零，12 局 `207 x11, 247 x1`。
- `n550d`：在 `n550b` 基础上加回每案 `marks()` 调用，12 局 `207 x12`。
- `n550e`：在 `n550c` 基础上加回每案 `marks()` 调用，12 局 `207 x12`。
- `n550f`：无 `marks()`，原顺序宽生物馆问，并提交生物馆实际目击者答案；首轮有效 `207 x10` 另 2 局平台 UE，补 4 局后合计有效 `207 x13, 247 x1`。
- `n550g`：无 `marks()`，逆序宽生物馆问，并提交生物馆实际目击者答案，12 局 `207 x11, 247 x1`。
- 子任务轨迹复核确认：`247` 样本与 `207` 控制样本在可见层面都停在 `yuan_stage=1`，没有 achievement/testimony unlock；`703` 很常见，`704` 不是充分条件；“实际从生物馆跑出的人”这个 runner 人名也不是充分条件。
- 这批结果进一步反证：`+40` 不是 `marks()` 请求、`703/704`、runner 特定人名、最终答案文本或“把 runner 作为 murderer 提交”任一单因子。当前最合理解释仍是 broad 后宽生物馆问句触发的低频隐藏聊天/身份条件，命中率约 `0%-20%`，不可控。
- 当前不应把 Yuan `+40` 移植到主线生产版本。后续如果继续 Yuan，优先研究可见身份排列和第三轮问句顺序；只有能在全零/direct 骨架中稳定高命中，才值得回到完整 `2757` 骨架复测。

## 0f. 2026-05-09 低分格子分解复核

- 新增 `Game2/tools/analyze_score_lattice.py`，只读 `docs/generated/game2_room_score_factors.json`；默认只统计当前线程 `n*` 标签，排除 `sk*` 等其他线程样本。
- 本地生成 `docs/generated/game2_score_lattice.md/json`：`2260` 条有效因子样本中，当前线程 `n*` 样本 `1505` 条；严格格子样本 `1059` 条。
- 严格格子分解：`207` 为全零基线；`1607=207+Rose direct 600+Z/F direct 600+Poker direct 200`；`2657` 为 Rose+Z/F full + Poker direct fast 档；Poker stage2/stage3 各 `+50`，得到 `2707/2757`。
- Rose stage6 late 稳定解释 `-40` 档：`2657->2617`、`2707->2667`、`2757->2717`。
- 当前主要稳定性瓶颈是不可见 `-200` residual：严格格子 residual 分布为 `0 x945, -200 x108, -40 x6`。
- 关键同签名碰撞：`rose=mTmTmT fast#28, z_err8=2, poker=3, poker evidence=001/002/101/102/103/104/201/202/203/204/205, yuan=001` 下有 `347` 条，分布 `2757 x302, 2557 x41, 2717 x4`；这说明 `-200` 不是当前可见 stage/evidence 能解释的，也不能继续简单归咎于 Poker stage3。
- 迭代优先级更新：上限突破应优先找 Yuan 或 Poker 新正组件；稳定性突破再围绕同可见签名碰撞挖 `-200` 隐变量；Rose late 是已解释均值问题，不是突破 `2757` 的主方向。

## 0g. 2026-05-09 `n551a/b` Yuan 李海天/张壹链路隔离

- 上传实体名 `n551a/n551b` 和备注 `r` 均保持中性；未使用 batch，未激活，未上天梯；当前上传账号为 `thebeginning / user_id=2646`。
- `n551a`，code_id `b7780f1cc5a645db8a3da2983ce8d496`：全零骨架，Yuan broad 两问后额外全员追问李海天/张壹/1919/生物馆/世纪林关联，最终答案置零；12 局 `207 x11, 247 x1`。
- `n551b`，code_id `838dbbdbbcb648df9b11131451615c84`：同聊天，但最终提交 `张壹` 为凶手；12 局 `207 x12`。
- 刷新后 `docs/generated/game2_room_score_factors.json` 为 `2336` 行，当前线程 `n*` 行为 `1529`；严格高分格子仍为 `1059` 行，因为 `n551a/b` 是全零 Yuan 隔离。
- `n551a` 唯一 `247` 样本 `8108045` 仍是 `yuan_stage=1`、证据 `001/703/704`，无 achievement/testimony unlock。该局同时出现投票异常 `46/47/49/笔迹`、李海天、生物馆、世纪林、保安/奇怪网站和 1919 黑车，但这些关键词在若干 `207` 样本中也出现过。
- 结论：显式强化李海天/张壹链路不能稳定打开 Yuan `+40`；`张壹` murderer 没有 answer 分。Yuan 继续探索必须保持隔离骨架，优先拆“投票异常”和“李海天/学生会会长”是否有交互，不应回主线。

## 0h. 2026-05-09 `n552a-d` Yuan 投票/李海天链路反证

- 上传实体名 `n552a` 到 `n552d` 和备注 `r` 均保持中性；未使用 batch，未激活，未上天梯；每个版本通过单人房间评测 8 局。
- `n552a`，code_id `014375346f644f108ec2196d3b6c101c`：全零骨架，只强化投票异常、46/47/49、人头数、异笔迹/多出选票链路，最终答案置零；8 局 `207 x8`。
- `n552b`，code_id `183ebd4c2b644285833d21c6c60dd23b`：全零骨架，只强化李海天/学生会会长/生物馆/保安/奇怪网站/1919 链路，最终答案置零；8 局 `207 x8`。
- `n552c`，code_id `198dbae7a6b54e7493dc4a7545c74a9b`：组合投票异常与李海天链路，并提交可见首个 NPC/竞争者答案；8 局 `207 x8`。
- `n552d`，code_id `1436eb8cd243474eb4d96115ac13dc93`：投票异常链路加可见首个 NPC/竞争者答案；8 局 `207 x8`。
- 刷新后 `docs/generated/game2_room_score_factors.json` 为 `2448` 行，当前线程 `n*` 行为 `1561`；严格高分格子仍为 `1059` 行，residual 仍是 `0 x945, -200 x108, -40 x6`。
- 新增 `Game2/tools/analyze_yuan_isolation.py`，本地生成 `docs/generated/game2_yuan_isolation.md/json`；当前 Yuan 隔离样本为 `272` 条，其中 plus40 为 `14` 条。`n552a-d` 全部为 `207`，没有新 plus40 样本。
- 结论：投票异常、李海天/生物馆/1919 显式链路、二者组合、可见首个 NPC/竞争者答案都不是 Yuan `+40` 或 answer 分的充分条件。下一步若继续 Yuan，应优先测第三轮问句顺序与 NPC 位置，而不是继续换最终答案文本。

## 0i. 2026-05-09 `n553a-c` Yuan 位置/顺序 A/B

- 上传实体名 `n553a` 到 `n553c` 和备注 `r` 均保持中性；未使用 batch，未激活，未上天梯；每个版本通过单人房间评测 12 局，上传账号守卫为 `thebeginning / user_id=2646`。
- `n553a`，code_id `2a76cc2c7ddd44ae8b34a9562550fcf6`：全零骨架，Yuan broad 两问后只问第三个 false-marked NPC 两条生物馆/张壹/李海天纠偏问，最终答案置零；12 局 `207 x12`。
- `n553b`，code_id `7d32509b3423488f8d06c7988b7a0e87`：同骨架，但第三轮只问前两个 marked true NPC，最终答案置零；12 局 `207 x12`。
- `n553c`，code_id `0b85cf2b414c4a0da0eb9fbe1a5e1d45`：同骨架，第三优先顺序但仍覆盖全员，每人两条纠偏问，最终答案置零；12 局 `207 x12`。
- `Game2/tools/analyze_yuan_isolation.py` 已增强为记录 Yuan 初始 `marks`、`mark_pattern`、`npc0/npc1/npc2` 和 `zhangyi_pos`。刷新后 `docs/generated/game2_yuan_isolation.json` 为 `308` 条 Yuan 隔离样本，plus40 仍为 `14` 条；`n553a-c` 新增 36 条全部为 `207`。
- 刷新后 `docs/generated/game2_room_score_factors.json` 为 `2604` 行，当前线程 `n*` 行为 `1597`；严格高分格子仍为 `1059` 行。
- 结论：Yuan 初始 marks 恒为 `TTF`，不是解释变量；只问第三人、只问前两人、第三优先全员双问都不能放大 `+40`。后续不应继续同类位置 A/B，应换成旧 `n549c/d/e` 高 reply-count 结构复现，或转向完整骨架的 `-200` residual 归因。

## 0j. 2026-05-09 Poker residual 角色/回复序列复核

- 新增 `Game2/tools/analyze_poker_residual_roles.py`，在严格高分格子内回查 Poker 信息源、接待者、医生提示、回复序列和首答类型；输出 `docs/generated/game2_poker_residual_roles.md/json`。
- 最新刷新后 `docs/generated/game2_room_score_factors.json` 为 `2772` 行，当前线程 `n*` 行为 `1637`；严格高分格子为 `1099` 行，residual 为 `0 x980, -200 x113, -40 x6`。
- 顶层 stage3 同形签名为 `369` 行，分布 `2757 x321, 2557 x44, 2717 x4`。
- 新分析显示：`poker_stage_sequence=1/2/2/3` 在现有 14 个顶层 stage3 样本中暂时全 `2757`，而 `1/2/2/3/3` 为 `2757 x5, 2557 x4`。这是一个候选结构信号，但仍是小样本，不能直接定论。
- `n554a`，code_id `6921d82dfd4340e7bda3fcf4bb5a5c80`：从 `n539d` 派生，把接待者宽问收窄为先说 Joker 聊天记录和到达时间表，目标是强制 `1/2/2/3`；提前停止，4 局为 `2707 x3, 2667 x1`。
- `n554a` 轨迹全为 Poker stage2，序列 `1/2/2/2/2`，证据只有 `001/002/101/102/103/104/201/202`，没有 `203/204/205`。结论：接待者第一问不能过窄，否则会关掉 stage3 上限；后续若继续 Poker，应保留“异常发现”信息量，再研究如何减少 stage3 后的额外回复。

## 0k. 2026-05-09 `n554b` 扩样结论

- 上传实体名 `n554b` 和备注 `r` 均保持中性；未使用 batch，未激活，未上天梯；code_id 为 `c06f2ecd82764eaaab3d9faf13eac6ba`。
- `n554b` 首轮 12 局为 `2757 x4, 2717 x6, 2657 x1, 2617 x1`，当时看似 residual 全 0。
- 随后同 code_id 追加 24 局 `n554b_ext`，分布为 `2757 x15, 2717 x4, 2557 x3, 2517 x1, 2457 x1`。
- 合计 36 局为 `2757 x19, 2717 x10, 2557 x3, 2657 x1, 2617 x1, 2517 x1, 2457 x1`；residual 为 `0 x31, -200 x5`。
- `n554b` 的主要 Poker 成功轨迹是 `1/2/2/2/3 x33`，但该序列已出现 `2557/2517` 低尾，因此它不是安全替代路线。
- 低分结构仍符合现有格子：`2717` 是 Rose late `-40`；`2557` 是隐藏 `-200`；`2517` 是 `-40` 与 `-200` 叠加；`2457` 是 Poker stage1/direct 档叠加隐藏 `-200`。
- 结论：`n554b` 只能作为高上限候选，不能视为稳定版本。当前稳定版本仍应保持 `n518b`；继续推进前应优先做同签名低尾 trace comparator，而不是继续扩同族 Poker 接待者问句。

历史关键结论（2026-05-07 恢复前，保留背景；最新口径以上方 0a 为准）：

- 不再使用 batch 做 Game53 版本判断；后手/反向对局对本游戏没有意义。
- 当前只用 `player_number=1` 的单人房间评测，直接看我方 raw score。
- 当前 token 于 `2026-05-07 10:26 UTC` 实测仍解析为 `thebeginning`。
- Saiblo 可见实体名和备注必须中性；当前使用 `n506e/n507*/n508*/n511*/n512*` 与备注 `r`。
- 更新后一局稳定包含 Rose、Z/F、扑克公馆、袁樱瞳等多个剧本。
- 当前最高上界候选是 `n511a`，code_id `a2b68a7ec9b84a59a8dfd836defd930c`，19 个有效房间样本为 `2707 x7, 2667 x2, 2657 x5, 2617 x3, 2507 x1, 2457 x1`，最高 `2707` 但不能视为稳定。
- `n511a` 的新增收益来自扑克公馆案：向 hint 中的“好的信息来源”NPC 问两句，约半数样本把扑克案推进到 stage2，总分约 `+50`。
- `n512a`，code_id `673e54c862394b58a2ce790b63416e55`，3 个有效样本 `2707,2507,2507`，扑克 stage2 为 3/3；样本不足，但历史上传版未关闭 stderr，后续默认用安全版 `n514e` 复测扩样。
- 本地扫描已有 396 条扑克轨迹，尚未观测到 stage3+；旧 `v50/v52` 可解锁 `102/103/104` 和部分 `203/204/205`，但旧探针分数很低，只能作为最小问句来源。
- 本地扫描 369 条袁案轨迹，最高只观测到 stage1；旧探针可解锁 `703/704`，但未证明有净分收益。
- 旧稳定基线 `n506e`，code_id `a63c0344959c4189beba83019bdc4c2b`，18 局分布 `2520 x14, 2480 x4`。
- `n507q/n508g` 仍是旧高分基线，最高 `2657`；`n508a-e`、`n508u-w`、`n509a-g`、`n510a-d` 等 Rose/袁案/扑克旧方向均未消除低尾。
- `2026-05-07 09:00 UTC` 左右 Saiblo POST 接口异常：上传新 code 和房间 join 均连续返回 500；`n513a-d/n514a-b` 暂时卡在 `未编译`，需要恢复后再评测。`join` 返回 500 后房间详情显示 code 已坐入，但 `begin_match` 仍 500，当前阻塞在房间启动链路。
- 旧 `n513e/f/g` 已降级为历史问句来源；当前优先队列已转为 `n514/n515`，详见 `docs/game2_recovery_eval_queue.md`。
- `2026-05-07 09:17-09:25 UTC` 再次低频探测：`n513a` entity `21072` 的两个 code 仍为 `未编译`；`n511a` 单人房间新探针在 `POST /api/rooms/918242/join/` 与 `POST /api/rooms/918262/join/` 继续返回 500。
- `GET /api/rooms/918262/` 能看到 `n511a` 已坐入单人房间，但 `POST /api/rooms/918262/begin_match/` 仍返回 500；`918242` 后续查询已 404。
- `2026-05-07 09:35 UTC` 使用容错后的 `run_room_eval.py` 再测，脚本成功越过 `join` 假失败并记录 `room 918351` 已坐入，但 `begin_match` 仍返回 500。
- `2026-05-07 09:40 UTC` 再测 `n511a`，`room 918360` 仍卡在 `begin_match` 500；失败 summary 已正确保留 `room_id=918360`。
- `2026-05-07 09:56 UTC` 再测 `n511a`，`room 918362` 仍表现为 `join` 500 但已坐入，`begin_match` 500。平台开赛链路仍未恢复。
- `2026-05-07 10:14 UTC` 再测 `n511a`，`room 918406` 仍表现为 `join` 500 但已坐入，`begin_match` 500；`n513a` 两个 code 仍为 `未编译`。
- `2026-05-07 10:26 UTC` 再测 `n511a`，`room 918439` 仍表现为 `join` 500 但已坐入，`begin_match` 500；`n513a` 两个 code 仍为 `未编译`。
- `2026-05-07 10:42 UTC` 非开赛检查显示 token 仍为 `thebeginning`，`n513a` 两个 code 仍为 `未编译`。
- `2026-05-07 10:50 UTC` 单人房间恢复探针仍失败：`room 918471` 表现为 `join` 500 但已坐入，`begin_match` 500。
- `2026-05-07 11:00 UTC` 非开赛检查显示 token 仍为 `thebeginning`，`n513a` 两个 code 仍为 `未编译`。
- `2026-05-07 11:03 UTC` 单人房间恢复探针仍失败：`room 918483` 表现为 `join` 500 但已坐入，`begin_match` 500。
- `2026-05-07 11:09 UTC` 非开赛检查仍未恢复：`entity 21072 / n513a` 两个 code、`21073 / n514a`、`21074 / n514b` 均为 `未编译`。
- `2026-05-07 11:10 UTC` `entities --game-id 53` 仍返回 username `thebeginning`，`active=null`。
- 本地已补充 `n514c/d/e/f/g/h/i`、`n515a/b/c/d/e`、`n516a/b/c/d/e`、`n517a/b/c/d/e/f/g/h` 与 `n518a/b/c/d` 二十九个未上传候选，语法检查通过；它们只改 Game2 后案逻辑，主要验证扑克案 hint-first 信息源解析、`n512a` 纯问法隔离、stage2 后证据追问、stage2 后接待者问句、stage2 条件短动机、`n514e` 高 stage2 触发问法与接待者/动机组合、Poker 三证据最小异常问、Poker 自杀伪装手法文本、显式 `others()` evidence-refresh，以及袁案低成本 704/短动机/最小 704 路线隔离实验。
- `n515a/b` 已根据旧 `v52` stage2 hint 修订为接待者单句合并“聊天记录、宾客到达时间表、公馆异常”；`n515c` 保留为更高风险的旧轨迹对照，接待者问“异常发现 + 证词破绽”两句。
- `n516a/b/c` 是 `n515b` 的 Poker 接待者追问强度对照：只问异常发现、异常发现加证据关键词、以及分拆旧 hint 三问。`n516d/e` 是 Yuan `704` 的省聊天对照：`n516d` 遍历初始 `marks=True` NPC，`n516e` 优先只问第二个 `marks=True` NPC。
- `n517a/b/c/d` 是 `n514e` 的组合缺口对照：用历史 3/3 触发 Poker stage2 的信息源问法，再分别叠加接待者合并 hint 单问、异常关键词问、条件短动机和分拆旧 hint 三问。
- `n517e/f/g/h` 是 Poker 子任务复核后的更细对照：三证据精确问、电脑/冰柜/刀具低泄露问、接待者问后叠加冰块固定刀具自杀伪装他杀的手法文本，以及不加接待者问的手法文本隔离。
- `n518a/b/c/d` 是低扰动 evidence-refresh 对照：`n518a/b` 不新增聊天，只在 Poker stage2 后调用 `others()` 刷新 `102/103/104`；`n518c/d` 在接待者追问后再调用 `others()`，验证 `203/204/205` 是否需要显式取证据才计分。
- 上传安全修订已合并到本地候选：`n514c-i/n515a-e/n516a-e` 默认关闭 stderr 调试日志，Poker hint 目标先精确匹配当前 NPC 中文名，Yuan 追问版聊天后刷新 `marks=False` 凶手。
- 因 `n511a/n512a` 已上传 code 是安全修订前版本，平台恢复后默认先上传安全版 `n514d/n514e` 再做有效房间扩样；旧 code_id 只保留为历史样本和必要低频恢复探针。
- 三个只读 explorer 的结果已合并到候选队列：Poker 暂无可靠 stage3+，袁案不改 `marks=False` 凶手，候选实现已修复 Poker hint 中文名边界解析问题。
- Saiblo 恢复后继续遵守：不打天梯、不用 batch 判断、不在实体名或备注暴露策略；上传名和备注继续使用中性编号与 `r`。
- `2026-05-07 11:16 UTC` 再测 `n511a`，`room 918519` 仍卡在 `begin_match` 500。
- `2026-05-07 11:22 UTC` 换旧稳定 `n506e` code_id `a63c0344959c4189beba83019bdc4c2b` 再测，`room 918537` 也卡在 `begin_match` 500；因此当前阻塞是房间启动链路级问题，不是 `n511a` 特异。
- 最新本地差分显示 Poker stage2 是约 `+50` 的叠加层：旧基底 `2457/2617/2657` 对应 stage2 后常见 `2507/2667/2707`。`n512a` 3/3 stage2 仍出现两个 `2507`，说明 stage2 触发率提高不能单独消除低尾。
- 更细的低尾归因：`2667/2617` 与 Rose stage5->6 由 `喜欢你？` 触发有较强相关，`2507` 至少一例来自 Rose motivation false，但 `n512a` 的两个 `2507` 仍更像隐藏后案波动；Yuan 在这些样本均无 703/704 或推进证据。
- evidence-refresh 复核结论：旧 `v52` 证明确实需要显式 `others()` 才能看到 Poker `102/103/104` 与 `203/204/205`，但是否能独立转化为 raw score 仍需房间恢复后用 `n518a/b/c/d` 做 A/B。
- 已新增 `Game2/tools/watch_saiblo_recovery.py` 做低频恢复监控，避免服务器异常时高频轮询；已新增 `Game2/tools/analyze_room_score_factors.py` 做本地分数因子差分，避免手工重复分析。
- `2026-05-07 11:33 UTC` 已启动一个低频 watcher 长会话：先等待 `900s`，之后每 `900s` 用 `n511a` 做一次房间恢复探针；恢复后只跑一次 `n511a_recovered_once` 确认评测，不自动上传新候选。
- `2026-05-07 11:43 UTC` 非开赛检查显示 `entity 21072 / n513a` 两个 code 仍为 `未编译`；当前 token 仍解析为 `thebeginning`，不是 `theend`。
- 只读挖掘 300 个有效单人房样本后确认：所有 `2707` 样本共同具备 Poker stage2、Rose 三项全对且 stage6 由“态度怪？”类问句触发、Z/F 高阶段；`2707` 不是理论上限，只是当前最高观测组合。
- 新增本地候选 `n519a/b`，分别从 `n518a/b` 派生，用于应对更新后的新增剧本和 NPC 名字混淆风险。它们动态处理最多 12 个剧本，Poker 信息源先按上下文正则解析，未知新增案优先 `marks=False` 兜底；均已通过语法检查，未上传、未开房。
- 新增 `Game2/tools/run_recovery_eval_queue.py` 默认恢复队列 `n514d n514e n518a n518b n519a n519b`，dry-run 已验证只做中性上传和单人房间评测，不使用 batch，不上天梯。
- `2026-05-07 11:48 UTC` 低频 watcher 的房间探针仍失败；`POST /api/rooms/` 创建房间返回 500。平台仍未恢复，继续等待 900s 间隔检查。
- `2026-05-07 11:56 UTC` 暂停前核查：低频 watcher 仍在运行，PID `677676`，启动参数为 `--initial-delay 900 --interval 900 --probe-code-id a2b68a7ec9b84a59a8dfd836defd930c`；恢复判定依赖实际单人房间创建/开赛探针成功。回调只执行一次 `n511a_recovered_once` 单人房间评测，不上传、不 batch、不 activate；回调执行后 watcher 进程会退出。
- `2026-05-07 11:59 UTC` 当前 token 复核仍为 `thebeginning`。已给恢复队列脚本新增 `--expected-username` 账号守卫；若目标是 `theend`，必须先切 token 并用 `--expected-username theend` 校验通过，才能上传恢复队列。
- `2026-05-07 12:03 UTC` watcher 第 2 次实际房间探针仍失败；`POST /api/rooms/` 创建房间返回 500。PID `677676` 继续按 900s 间隔等待下一次恢复检查。

## 1. 当前必须先统一的口径

`Game2` 不是传统双边对战。

对 `batch` 的正确理解是：

- 每个 `match` 都是“第一个 code 自己的一次独立跑分”
- 第二个 code 的 `score=0` 只是占位
- 因此分析 `Game2` 时，核心只看我方自己的 raw score
- 选择哪个“对手 code”，本质上只是为了更方便地拿到多份自跑样本

这意味着：

- 不应该再用 `win/loss` 口径理解 `Game2`
- `2357` 最初只是 `admin` 的参考上界样本，但 `2026-04-19` 的 `v43` 已在普通账号上复现同分

## 2. 2026-04-18 线上复核结果

当日重新实测并确认：

- `entities --game-id 53` 可用
- `codes --entity-id ...` 可用
- `ladders --game-id 53` 可用
- `POST /api/batches/` 可用
- `GET /api/batches/<batch_id>/` 可用
- `GET /api/matches/<match_id>/`
- `GET /api/matches/<match_id>/download/`

额外补充的操作事实：

- `GET /api/batches/?game=53&limit=5` 可以直接列出最近 batch
- 如果自动化中断，可以用 `Game2/tools/run_batch_eval.py --batch-id <id>` 事后补抓结果

本地环境还确认了一处常见坑：

- `config.local.json` 里的 token 可能是 URL 编码后的 `Bearer%20...`
- 直接喂给 `saiblo_tools.py` 会导致 `401`
- 实际调用前需要先 URL decode，并去掉前缀里的 `Bearer `

## 3. 当前榜单与主线快照

`2026-04-18` 复核到的旧榜单前列仍然成立：

- `admin / 跑通测试 / 121207`
- `theend / g2auto / 112000`
- `stepp / 头顶圆圆的 / 58172`
- `zyfkid / zyfkid / 44500`

但在 `2026-04-19` 发生过一次真正的主线切换。注意：以下是历史主线记录；`2026-05-07` 当前本地 token 实测已变为 `thebeginning / user_id=2646`，不应把本节账号信息当成本轮评测账号。

- 当前 token 已重新核验为：
  - username: `theend`
  - user_id: `2636`
- 当前真实主线实体：
  - entity: `g2auto`
  - entity_id: `20441`
  - active version: `27`
  - code_id: `99dd696247af49eba89ad358725f47cd`
  - remark: `stable pre-answer dual hidden trigger from v47`
- 对应上传记录：
  - `Game2/runtime/submissions/20260419_031401_g2auto_v27/summary.json`

需要特别注意的是：

- `2026-04-19` 刚切到 `g2auto v27` 时，ladder 行里的 `score` 暂时显示为 `1000`
- 这个 `1000` 是榜单分，不是单局 raw score
- 对 `Game2` 版本判断，仍然只看我方自跑 raw score

因此这部分口径要改成：

- 文档里早期的 `g2base0310 / v13 / 987` 早就不是主线
- `g2auto v26 = 2220` 现在也只应被视为旧主线平台
- 当前真实主线已经变成 `theend / g2auto v27`

## 4. 历史主线回填结论

此前自动化在 `v24/v25/v26` 的 batch 轮询阶段中断，`2026-04-18` 已补抓真实结果：

- `batch 75873` / `v24` / my score = `2008`
- `batch 75874` / `v25` / my score = `2220`
- `batch 75875` / `v26` / my score = `2220`

结合仓库中已有 `iteration_16` 到 `iteration_21` 日志，可以把 `g2auto` 主线概括为：

- `v17` 到 `v22` 基本都稳定在 `2220`
- `v24` 明显回退到 `2008`
- `v25` 把分数拉回 `2220`
- `v26` 没有继续突破，仍是 `2220`

因此这部分现在更准确的判断是：

- `2220` 不是单次幸运分
- 它是 `g2auto` 旧主线在 `v26` 阶段的稳定平台
- 但它已经不是当前主线的最终状态

## 5. 2026-04-18 新 batch 复核（旧主线 `v26` 的历史平台）

当天新建 batch：

- batch: `77092`
- active code: `g2auto v26`
- 创建时间：`2026-04-18 21:16:15 +0800`

这次 batch 用了三个不同 code 作为“样本载体”，但按正确口径只看我方自跑：

- `7858555` -> `2220`
- `7858557` -> `2220`
- `7858559` -> `2220`

三场回放都已重新下载并分析，结果完全一致：

- `case0`
  - final stage = `6`
  - calls = `41`
- `case1`
  - final stage = `7`
  - calls = `40`

`stderr` 中记录的阶段推进在三场里完全相同：

- `case0`
  - `0->1` `XiaoDingAng / Rose是怎样的人？`
  - `1->2` `BaiJingTing / Rose是个怎样的人？`
  - `2->3` `YeWenXiao / 你今晚在干什么？`
  - `3->4` `YeWenXiao / Rose和范敏敏吵架了？`
  - `4->5` `YeWenXiao / Rose今天戴面纱了？`
  - `5->6` `XiaoDingAng / 范敏敏态度怪？`
- `case1`
  - `0->1` `NPC_A / 你知道Z失踪了吗？`
  - `1->2` `NPC_E / 你了解平时的Z吗？`
  - `2->3` `NPC_D / 你和E以前是不是男女朋友？`
  - `3->4` `NPC_B / 你是不是喜欢E？`
  - `4->5` `NPC_C / 昨晚你在回宿舍路上有没有看到什么？`
  - `5->6` `NPC_A / 红U盘去哪了？`
  - `6->7` `NPC_D / 你为什么装病？`

这说明：

- 当时的 `v26` 平台不是“有时 2220，有时更高”
- 至少在当天这三次独立自跑里，它已经表现出几乎完全可复现的固定轨迹
- 这组结论现在应被当作“旧主线平台的历史基线”，而不是 current 版本结论

## 6. 与参考上界样本的真实差距

当前最有价值的参考仍然是 `admin / 跑通测试` 的 `2357` 样本。

### 6.1 `v37` 隔离复测：exact-admin 顺序 + `E`

当天另外上传了一个隔离探针：

- entity: `g2v37probe0418`
- entity_id: `20678`
- code_id: `98e652eea9cb4a40b61a29c011ace90c`
- source: `Game2/deepclue_ai/v37/ai_v37.py`

它只做两件事：

- 把 `case1` 的关键顺序块改回 `v27/admin` 风格
- 把最终凶手写成 `E`

对应 batch：

- `77093`

按正确口径，只看我方三场自跑：

- `7858820` -> `2220`
- `7858822` -> `2220`
- `7858824` -> `2220`

当时 `7858823 / 7858825` 这两场“对手自跑”仍在 `评测中`，但这不影响结论，因为 `Game2` 本质只看我方自己的 raw score。

这次复测最重要的地方不只是分数，而是状态也完全收敛：

- `case0`
  - `v26` 与 `v37` 都是 `42` 步、`stage 6`、答案 `3/3`
- `case1`
  - `v26` 与 `v37` 都是 `41` 步、`stage 7`、答案 `3/3`
  - 提交答案前都持有相同的 `38` 条证言
  - `npc_marks` 都是 `['NPC_C', 'NPC_D']`
  - `visible_evidences` 与 `hint` 也完全相同

两版唯一可观察到的区别，只剩 `case1` 第 `18-25` 步的顺序块：

- `v26(v36)`：先 `NPC_E`，再 `NPC_A`，最后 `NPC_C`
- `v37(v27/admin-style)`：先 `NPC_A`，再 `NPC_C`，最后 `NPC_E`

但这个顺序差异并没有带来任何阶段、证言、得分上的变化。

### 6.2 `g2lang418` 运行类型探针：同源代码改成 `language=auto`

在 `v37` 之后，又做了一次更偏平台侧的隔离实验：

- entity: `g2lang418`
- entity_id: `20679`
- language: `auto`
- code_id: `7bc15bb1272648018f0d01420a334f1b`
- source: 仍然是 `Game2/deepclue_ai/v37/ai_v37.py`

这个实验的目的不是改脚本逻辑，而是验证另一个显眼假设：

- `admin` 的实体语言是 `auto`
- 我方之前一直是 `python`
- `stage 8` 会不会和运行类型绑定

对应 batch：

- `77095`

只看我方三场自跑：

- `7859038` -> `2220`
- `7859040` -> `2220`
- `7859042` -> `2220`

同时，作为对照：

- `7859039` (`admin` 自跑) = `2357`

更重要的是，`v37_python` 与 `v37_auto` 的运行结果也是逐项一致：

- `case0`
  - 都是 `42` 步、`stage 6`、答案 `3/3`
- `case1`
  - 都是 `41` 步、`stage 7`、答案 `3/3`
  - 问句序列一致
  - 证言集合一致
  - 最终状态一致

因此又可以明确排除一类解释：

- 不是“我方之所以上不去，是因为实体语言是 `python` 而不是 `auto`”

### 6.3 `v38` 失败诊断：不是“多问两句导致普通超时”

当天继续往 `stage 7 -> 8` 挖时，先上传过一个更激进的窄探针：

- entity: `g2v38p418`
- entity_id: `20680`
- code_id: `e5d566db605b45eaa11526f9fe92b0b8`
- source: `Game2/deepclue_ai/v38/ai_v38.py`

对应 batch：

- `77096`

表面结果是三场我方自跑都变成了：

- `7859332` -> `0 / TLE`
- `7859334` -> `0 / TLE`
- `7859336` -> `0 / TLE`

但回放与日志复盘后可以确认，它并不是常规意义上的“后段多聊两句把预算耗光”：

- 三场都只停在初始 stdin 记录，`case0` 根本没有开始
- `performance.sample_count` 只有 `2`
- player `stderr` 为空
- `err_decoded` 显示的是：
  - NPC embedding 启动完成
  - 然后在 `round 2` 直接出现 `runError (Code 0)`

把 `v37` 和 `v38` 逐项对比后，最关键的实现差异其实是：

- `v38` 把 `main()` 里的启动握手 `sdk._receive()` 删掉了
- 同时还引入了额外的响应探测与入口结构改动

因此这组 `0/TLE` 更合理的解释是：

- `v38` 是探针实现本身把协议入口改坏了
- 它不能被解读成“stage7 再多两句问题本身一定不可行”

### 6.4 `v39` 最小扰动复测：只加两句 stage7 证据质询

为把变量收紧到只剩“额外两句问题”，当天又基于 `v37` 做了一个最小扰动版本：

- entity: `g2v39p418`
- entity_id: `20681`
- code_id: `a60363c47d564611841e8311236cae03`
- source: `Game2/deepclue_ai/v39/ai_v39.py`

这个版本只做一件事：

- 在 `case1` 已到 `stage 7` 后，额外追加两句带证据的质询

新增的两句是：

- `NPC_D / 你看到E藏在小树林里，是想埋伏F吗？` + `['313']`
- `NPC_C / 有人拿你的水果刀按你的小说手法杀了F，还改了你的文档嫁祸你，对吗？` + `['315']`

对应 batch：

- `77097`
- `77099`

截至 `2026-04-18` 当天已完成的我方三场自跑结果：

- `7859427` -> `2220`
- `7859431` -> `2220`
- `7859644` -> `2220`

同时，同批次参考样本仍然维持：

- `7859428` (`admin` 自跑) -> `2357`
- `7859645` (`admin` 自跑) -> `2357`

这三场 `v39` 自跑的结构也完全收敛：

- `case0`
  - `42` 步
  - `stage 6`
- `case1`
  - `43` 步
  - `stage 7`
  - 最终答案仍然是 `3/3`

与 `v37` 相比，`v39` 的变化非常干净：

- `case0` 完全不变
- `case1` 只比 `v37` 多 `2` 步
- `NPC_C` 提问数从 `10 -> 11`
- `NPC_D` 提问数从 `9 -> 10`

更关键的是，这两句新问题确实拿到了更直接的回答：

- `NPC_D` 明确承认自己修车路上看见 `E` 躲在小树林里
- `NPC_C` 明确把“水果刀丢失 + 文档被改 + 被嫁祸”这条叙事直接说了出来

但即便如此，结果仍然没有发生任何实质提升：

- 没有新的阶段推进
- `case1` 仍然停在 `stage 7`
- 分数仍然稳定停在 `2220`

对比已经确认：

- `case0`
  - 我方与 `admin` 一致，都是 `stage 6`
- `case1`
  - `admin` 是 `stage 8`
  - 我方固定停在 `stage 7`

另外还有五个容易被误判的点，现在都已排除：

1. 不是“再多问一点就行”
   - `v24` 的 `case1` 问到 `66` 步，结果只到 `stage 4`，分数反而掉到 `2008`
   - `v26` 把 `case1` 收敛到 `41` 步后，稳定回到 `2220`

2. 不是“把凶手从 NPC_E 改成 E 就行”
   - `v18` 已经试过把最终凶手写成 `E`
   - `v37` 又在隔离实体上重新复测了一次
   - 结果仍然是 `2220`

3. 不是“把 case1 顺序改回 admin 就行”
   - `v37` 已经把关键顺序块改回 `v27/admin` 风格
   - 但提交答案前的状态与当前主线完全相同
   - 所以 `stage 8` 缺口也不能简化成“顺序没对上”

4. 不是“把实体语言改成 auto 就行”
   - `g2lang418` 已经用同一份 `v37` 源码做了 `language=auto` 探针
   - 三场我方自跑仍然全是 `2220`
   - 因此当前缺口也不能简化成“python/runtime 类型问题”

5. 不是“在 stage7 末尾再补两句更直接的证据指控就行”
   - `v39` 已经只加了两句证据绑定的 `NPC_C/NPC_D` 质询
   - 三场我方自跑仍然全是 `2220`
   - 新问句拿到了更直接的口供，但没有把 `case1` 从 `stage 7` 推到 `stage 8`

当前更合理的解释是：

- `g2auto` 这条脚本主线已经把显性、低风险的推进路径基本吃满
- 剩余差距更像是：
  - `case1` 的隐藏触发条件
  - 或某个需要特定回复内容才能稳定触发的阶段门槛
  - 或某种账号/平台侧差异
  - 或未被现有脚本覆盖的少量高价值隐藏成就

### 6.5 `v40` 提交帧诊断：提交前最后两次 `chat` 没有暴露额外控制字段

在 `v39` 之后，又基于 `v37` 做了一个更窄的诊断版：

- entity: `g2v40diag418`
- entity_id: `20682`
- code_id: `c35342e76d934576aded4cf49b57237a`
- source: `Game2/deepclue_ai/v40/ai_v40.py`

对应 batch：

- `77101`

结果：

- `7859943`（我方自跑） -> `2220`
- `7859944`（`admin` 自跑） -> `2357`

这个版本没有改问法路径，只加了三类诊断日志：

- `NPC_D / 你为什么装病？` 触发 `stage 7` 时的原始返回
- `NPC_B / 你是不是准备杀A？` 的最后一次正常 `chat` 返回
- `submit()` 返回的摘要

实际保留下来的有效日志只有前两类，而且它们的结构完全普通：

- `case1 stage7_trigger`
  - keys = `['achievements', 'reply', 'stage', 'unlock_testimony']`
- `case1 last_chat`
  - keys = `['achievements', 'reply', 'stage', 'unlock_testimony']`

这说明：

- 至少在提交前最后两次关键 `chat` 里，没有出现额外可见的控制字段
- `reply/stage/unlock_testimony` 之外，没有观察到能解释 `stage 8` 差异的新 payload

与此同时：

- `v40` 的 `submit_resp` 日志没有被保留下来
- 对照 `match_download.json`，我方与 `admin` 的提交帧仍然只差一个字段：
  - 我方 `result_state.stage = 7`
  - `admin result_state.stage = 8`
  - 其余 `answer_result / stop_reason / ending / visible_*` 都一致

所以 `v40` 把问题继续压缩到：

- 差距已经不在“最后几句普通 `chat` 的可见返回”
- 如果还存在差异，它要么在平台没有暴露出来的隐藏状态里，要么在平台侧对提交结算的内部逻辑里

### 6.6 `v41` submit-only 复测：成功路径下 `answer` 之后的玩家 `stderr` 不会保留

为确认 `v40` 丢失 `submit_resp` 日志到底是“被截断”还是“提交后根本不会保留”，当天又做了一次更窄的探针：

- entity: `g2v41submit418`
- entity_id: `20683`
- code_id: `00f09bc9bd1d41be8752496528734ddd`
- source: `Game2/deepclue_ai/v41/ai_v41.py`

这个版本进一步收缩到：

- 不再打印任何阶段推进日志
- 不再打印任何提交前诊断
- 只在 `case1` 最终 `answer` 返回后打印一条 `submit_resp`

对应 batch：

- `77102`

结果：

- `7860126`（我方自跑） -> `2220`
- `7860127`（`admin` 自跑） -> `2357`

这次最关键的新事实不是分数，而是日志保留行为：

- `7860126` 是成功对局
- 但 `analysis.json` 里 `players[0].stderr == ''`
- 同目录下 `analysis.json / match_detail.json / top-level err/err_decoded` 都找不到任何 `[v41]`

这说明：

- 成功路径下，`answer` 之后输出到玩家 `stderr` 的日志不会可靠保留
- 因此继续靠客户端脚本去“打印 raw submit response”已经拿不到稳定观测

结合 `v40` 与 `v41`，现在可以更明确地说：

- 提交前可见 `chat` payload 已经没有新信息
- 提交后的玩家日志在成功路径下又不可观测
- 所以“继续从客户端 `stderr` 抠 `submit()` 隐藏字段”这条路，信息增益已经非常低
- 当前我方与 `admin` 在提交帧里仍然只有 `stage 7` vs `stage 8` 的差别，其余可见字段完全相同

### 6.7 跨账号复测：切到 `thebeginning` 后仍然稳定是 `2220`

在用户更新 token、切到另一账号后，当天继续做了一次平台侧复测。

新账号信息：

- username: `thebeginning`
- user_id: `2646`

测试仍然使用完全相同的参考源码：

- source: `Game2/deepclue_ai/v37/ai_v37.py`

新账号上传的隔离实体：

- entity: `g2xaccv37a418`
- entity_id: `20684`
- code_id: `cffcb843c4d543e9b34616823ebe9547`
- remark: `game2 v37 cross-account retest`

对应 batch：

- `77104`

截至 `2026-04-18` 已完成的三场我方自跑：

- `7860680` -> `2220`
- `7860682` -> `2220`
- `7860684` -> `2220`

这三场结构再次完全收敛：

- `case0`
  - `42` 步
  - `stage 6`
  - 答案 `3/3`
- `case1`
  - `41` 步
  - `stage 7`
  - 答案 `3/3`

提交帧对比依旧没有任何新变化：

- 新账号我方 `7860680` 的 `case1` 提交帧：
  - `result_state.stage = 7`
- 同 batch 中 `admin` 的参考样本 `7860681`：
  - `result_state.stage = 8`
- 除 `stage` 外，其余可见字段仍完全一致

这次复测的重要意义是：

- “换成另一个普通账号就会直接跳到 `2357`”这个解释，现在也站不住了
- 之前怀疑的账号差异，至少不能再表述成“只有 `theend` 这个账号有问题”
- 如果平台侧真的还有账号相关差异，它更像是：
  - `admin` 特权/内部白名单
  - 或某种更窄的隐藏服务端条件
  - 而不是普通账号之间的普遍差异

### 6.8 新账号 + `language=auto` 复测：组合假设也仍然是 `2220`

为了继续排掉最后一层组合变量，当天又在新账号 `thebeginning` 下，把同一份 `v37` 源码上传成 `auto` 实体：

- entity: `g2xaccauto418`
- entity_id: `20685`
- code_id: `f3aac74a2c314fbcb6a6b3dd275e866b`
- language: `auto`
- source: `Game2/deepclue_ai/v37/ai_v37.py`

对应 batch：

- `77105`

截至 `2026-04-18` 已完成的两场我方自跑：

- `7860966` -> `2220`
- `7860968` -> `2220`

同批参考样本：

- `7860967` (`admin` 自跑) -> `2357`

结构仍然完全没有变化：

- `7860966`
  - `case0 = 42` 步、`stage 6`
  - `case1 = 41` 步、`stage 7`
  - 答案仍然 `3/3`

更关键的是，`7860966` 与 `7860967` 的 `case1` 轨迹再一次满足：

- `steps 0..39` 完全一致
- 第一处差异仍然只出现在提交帧 `step 40`
- 我方 `stage = 7`
- `admin stage = 8`
- 除 `stage` 外其余可见字段完全一致

因此现在可以把这个组合也明确排掉：

- 不是“旧账号不行，但新账号 + auto 就能行”
- 普通账号切换与 `python/auto` 运行类型的组合变量，当前也已经没有信息增益

## 7. 当前结论

截至 `2026-04-19`，可以把 `Game2` 的现状总结为：

- 线上链路是通的，且可重复验证
- 当前主线已不是早期 `v2/v13`，也不再是旧的 `g2auto v26`
- `g2auto v26` 现在应视为历史平台：稳定 `2220`
- 当前真实主线已经是 `theend / g2auto v27`
- `v37` 复原 exact-admin 顺序并把凶手写成 `E`，仍然稳定给出 `2220`
- 同一份 `v37` 源码改成 `language=auto` 后，仍然稳定给出 `2220`
- `v39` 在 `stage 7` 末尾多加两句证据质询后，仍然稳定给出 `2220`
- `v40` 说明提交前最后两次关键 `chat` 返回里，没有额外可见控制字段
- `v41` 说明成功路径下 `answer` 之后的玩家 `stderr` 不会被平台保留
- 切到另一普通账号 `thebeginning` 后，用同一份 `v37` 源码仍然稳定是 `2220`
- 切到另一普通账号后，再把同一份源码改成 `language=auto`，仍然稳定是 `2220`
- `v42` 在 `case1 answer` 之后主动补发旧 `admin` 日志里那两句隐藏尾部 `chat`，我方自跑仍然是 `2220`
- `v43` 首次证明：普通账号也能稳定复现 `2357`
- `v43` 的关键不是公开问题顺序变化，而是把旧 `admin` 隐藏两问前置到 `answer` 之前
- `v43` 的公开回放仍只有 `41` 步，但隐藏层的 `decoded_stdin_records` 与 `err_decoded` 已经对齐到 `stage 8` 路径
- `v44` 说明只问 `NPC_C / 你是色盲？` 也会进入隐藏 `stage 8`，但稳定分数只有 `2317`
- `v45` 说明只问 `NPC_D / F的QQ里面还有什么信息？` 也会进入隐藏 `stage 8`，但稳定分数也只有 `2317`
- `v46` 说明把两句顺序改成 `NPC_D -> NPC_C` 之后，仍然稳定是 `2357`
- `v47` 进一步证明：把同一组双隐藏问句直接贴回 `g2auto v26(v36)` 风格路径，我方三场自跑也全部是 `2357`
- 随后同一份 `v47` 逻辑已被直接上传到真实可写主线 `theend / g2auto`
- 现在的真实主线版本是：
  - version: `27`
  - code_id: `99dd696247af49eba89ad358725f47cd`
  - remark: `stable pre-answer dual hidden trigger from v47`
- `g2auto v27` 的主线自跑已确认：
  - `7865373 = 2357`
  - `7865375 = 2357`
  - `7865377 = 2357`
- 同码随后又出现额外单边自跑样本：
  - `7865379 = 2357`
  - `7865380 = 2357`
  - `7865381 = 2357`
- 因此现在的 current 结论已经不是“如何把补丁迁回主线”
- 而是“补丁已经迁回主线，并形成稳定版本 `g2auto v27`”
- 当前已知最小有效条件已经收敛成：
  - 两句 pre-answer 隐藏问句都要出现
  - 但顺序不是关键变量
- 当前已经明确没有信息增益的方向：
  - “换对手看看”
  - “多问一点”
  - “NPC_E 改成 E”
  - “把顺序改回 admin”
  - “把实体语言改成 auto”
  - “在尾部再补两句固定的强指控”
  - “继续靠 submit-only stderr 诊断去抠隐藏字段”
  - “随便换个普通账号再跑一次”
  - “随便换个普通账号，再配成 auto 跑一次”
  - “在 `answer` 之后机械重放旧 `admin` 样本里那两句隐藏尾部 `chat`”
- 当前真正有效的方向已经收敛成：
  - `case1 stage 7` 末端的隐藏 pre-answer 双问补丁
  - 而这组补丁现在已经稳定落到真正可写的主线实体上

### 7.1 `v42` post-answer hidden-tail 探针：主动补发两句隐藏尾部 `chat`，普通账号仍然不复现 `stage 8`

在前面的交叉对比中，`admin` 样本的 `decoded_stdin_records` 总是比我方多两条尾部返回，而且这两条返回正好对应旧日志中已经见过的报错：

- `Round=83`: `NPC_C / 你是色盲？`
- `Round=84`: `NPC_D / F的QQ里面还有什么信息？`
- 两次都落到 `KeyError: '8'`

为了验证“普通账号是不是其实也能在 `answer` 之后进入隐藏 `stage 8`，只是之前没有主动继续发请求”，当天新增了一个极窄探针：

- entity: `g2v42tail418`
- entity_id: `20686`
- code_id: `dae7d32535fc45f0a06dd9b7e08692db`
- source: `Game2/deepclue_ai/v42/ai_v42.py`
- batch: `77106`

这个版本保持 `v37` 的两案路径不变，只在 `case1` 的 `answer` 之后补发两句固定尾部问题：

- `NPC_C / 你是色盲？`
- `NPC_D / F的QQ里面还有什么信息？`

结果：

- `7861231`（我方自跑，对 `admin` 载体） -> `2220`
- `7861233`（我方自跑，对 `stepp` 载体） -> `2220`
- `7861232`（`admin` 自跑） -> `2357`

这次探针最关键的结论不是分数，而是“补发后的可见轨迹仍然没有变化”：

- 我方 `7861231 / 7861233`
  - `case1` 仍然只有 `41` 步
  - 提交帧仍然停在 `stage 7`
  - `match_download.json` 里看不到那两句 post-answer `chat`
  - `decoded_stdin_records` 数量仍是 `83`
- `admin 7861232`
  - `decoded_stdin_records` 仍是 `85`
  - 最后两条仍是 `{\"error\": \"Internal Server Error: '8'\"}`
  - `err_decoded` 仍明确对应：
    - `Round=83 / NPC_C / 你是色盲？`
    - `Round=84 / NPC_D / F的QQ里面还有什么信息？`

因此这次实验可以再排掉一类解释：

- 不是“普通账号其实也能进隐藏 `stage 8`，只要在 `answer` 之后手动继续补那两句尾部 `chat`”
- 至少在当前普通账号路径里，主动机械重放这两句尾部请求，也没有把可见结果从 `stage 7 / 2220` 推到 `stage 8 / 2357`

### 7.2 `v43` pre-answer hidden-question 探针：普通账号首次稳定复现 `2357`

在 `v42` 排除了“`answer` 之后机械补发两句尾部请求”之后，剩下最窄、最值得验证的假设变成：

- 同样是这两句隐藏问题
- 关键不在于 `post-answer`
- 而在于它们是否必须发生在 `case1 stage 7`、提交答案之前

因此 `2026-04-19` 新增了一个只改这一点的隔离探针：

- entity: `g2v43pre419`
- entity_id: `20688`
- code_id: `1527acf5a91447b09e94c95e0b2a0f6b`
- source: `Game2/deepclue_ai/v43/ai_v43.py`
- batch: `77111`
- 本地分析目录：`Game2/runtime/batches/20260419_020300_g2v43pre419_batch_77111`

这个版本保持 `v37` 的公开两案路径不变，只在 `case1` 的 `stage 7` 末端、`answer` 之前主动插入两句旧 `admin` 隐藏问题：

- `NPC_C / 你是色盲？`
- `NPC_D / F的QQ里面还有什么信息？`

截至本次文档更新时，`77111` 里还有 `7864665`（`stepp` 自跑）处于 `评测中`，但我方三场自跑已经全部结束，因此不影响结论：

- `7864660`（我方自跑，对 `admin` 载体） -> `2357`
- `7864662`（我方自跑，对 `theend/g2v41submit418` 载体） -> `2357`
- `7864664`（我方自跑，对 `stepp` 载体） -> `2357`
- `7864661`（`admin` 自跑） -> `2357`

这次探针最关键的新发现，是“公开回放看起来几乎没变，但隐藏层已经发生了决定性变化”：

- 我方三场 `match_download.json`
  - `case1` 仍然只有 `41` 步
  - `steps 0..39` 仍然和 `v37/admin` 的公开轨迹一致
  - 最后提交帧 `step 40` 现在变成了 `stage 8`
- 我方三场 `analysis.json`
  - `decoded_stdin_records` 从之前普通账号路径的 `83` 增加到了 `85`
  - `case1 final_stage = 8`
- 我方三场 `stderr/err_decoded`
  - 明确出现了这两句隐藏 `chat`
  - 并且两句都落到了与 `admin` 同源的后端异常：
    - `NPC_C / 你是色盲？`
    - `NPC_D / F的QQ里面还有什么信息？`
    - `KeyError: '8'`

也就是说，`v43` 给出了一个和 `v42` 完全不同的结论：

- `post-answer` 机械重放无效
- 但把这两句问题前置到 `answer` 之前，普通账号就能稳定复现 `2357`

### 7.3 `v44 / v45` 单问最小化探针：单条隐藏问句只能稳定到 `2317`

在 `v43` 证明“两句一起问”有效之后，下一步先把变量继续压到最小：

- `v44`
  - entity: `g2v44c419`
  - entity_id: `20690`
  - code_id: `baf6a82f125c44a2a19350b62c7b8f4e`
  - source: `Game2/deepclue_ai/v44/ai_v44.py`
  - batch: `77114`
- `v45`
  - entity: `g2v45d419`
  - entity_id: `20689`
  - code_id: `cb3208268a024769859a16567045fb00`
  - source: `Game2/deepclue_ai/v45/ai_v45.py`
  - batch: `77115`

结果非常稳定：

- `v44`
  - `7864868` -> `2317`
  - `7864870` -> `2317`
  - `7864872` -> `2317`
- `v45`
  - `7864874` -> `2317`
  - `7864876` -> `2317`
  - `7864878` -> `2317`

这两版最关键的共同点是：

- 公开 `case1` 回放仍然只有 `41` 步
- `case1 final_stage` 已经进入 `8`
- `decoded_stdin_records` 都是 `84`

但它们的隐藏层签名是各自单边触发：

- `v44`
  - 只出现 `NPC_C / 你是色盲？`
  - 对应 `KeyError: '8'`
- `v45`
  - 只出现 `NPC_D / F的QQ里面还有什么信息？`
  - 对应 `KeyError: '8'`

因此现在可以把最小化结论说得更准确：

- 任意一条隐藏问句单独存在时，也足以把普通账号从旧路径推进到 partial hidden `stage 8`
- 但单问的稳定平台只有 `2317`
- 要拿到完整 `2357`，两句都需要出现

### 7.4 `v46` 反序探针：`NPC_D -> NPC_C` 仍然稳定是 `2357`

在 `v44/v45` 确认“两句都需要”之后，还剩最后一个窄问题：

- 完整触发到底依赖“两句都出现”
- 还是依赖 `v43` 里已经验证过的特定顺序 `NPC_C -> NPC_D`

为此新增了反序探针：

- entity: `g2v46rev419`
- entity_id: `20691`
- code_id: `73382c8f0d7c45329d7dbfab245406a6`
- source: `Game2/deepclue_ai/v46/ai_v46.py`
- batch: `77116`
- 手工分析目录：
  - `Game2/runtime/manual_v46/7864970`
  - `Game2/runtime/manual_v46/7864972`
  - `Game2/runtime/manual_v46/7864974`

截至本次更新时，`77116` 里仍有 `7864975`（`stepp` 自跑）处于 `评测中`，但我方三场自跑都已结束，因此不影响结论：

- `7864970`（我方自跑，对 `admin` 载体） -> `2357`
- `7864972`（我方自跑，对 `theend/g2v41submit418` 载体） -> `2357`
- `7864974`（我方自跑，对 `stepp` 载体） -> `2357`
- `7864971`（`admin` 自跑） -> `2357`

这三场的隐藏层签名和 `v43` 完全同级：

- `decoded_stdin_records = 85`
- `case1 final_stage = 8`
- `err_decoded` 里会先出现：
  - `NPC_D / F的QQ里面还有什么信息？`
  - 再出现 `NPC_C / 你是色盲？`
- 两次都落到 `KeyError: '8'`

因此又可以把结论收紧一步：

- 完整 `2357` 触发依赖的是“两句都出现”
- 不是依赖 `NPC_C -> NPC_D` 这个特定顺序
- 当前已知最小有效条件已经缩到“pre-answer 双问集合”，而不是“固定顺序”

### 7.5 `v47` g2auto-path 探针：双隐藏问句可直接迁回主线路径

在 `v46` 排除顺序变量之后，剩下最有工程价值的问题变成：

- 这组双隐藏问句是不是只对 `v37/v43` 这条分支有效
- 还是可以直接贴回当前 `g2auto v26(v36)` 风格路径

为此新增了一个主线贴近版探针：

- entity: `g2v47g2a419`
- entity_id: `20692`
- code_id: `f463cf4a531a48edb8278786d97ee104`
- source: `Game2/deepclue_ai/v47/ai_v47.py`
- batch: `77117`
- 手工分析目录：
  - `Game2/runtime/manual_v47/7865084`
  - `Game2/runtime/manual_v47/7865086`
  - `Game2/runtime/manual_v47/7865088`

这个版本有两个刻意保留的“旧主线特征”：

- `case1` 继续使用 `v36` 的顺序块，也就是 `g2auto v26` 那条公开路径
- 最终答案继续写成 `NPC_E`，不回退到 `E`

截至本次更新时，`77117` 里仍有 `7865089`（`zyfkid` 自跑）处于 `评测中`，但我方三场自跑都已结束，因此不影响结论：

- `7865084`（我方自跑，对 `admin` 载体） -> `2357`
- `7865086`（我方自跑，对 `theend/g2v41submit418` 载体） -> `2357`
- `7865088`（我方自跑，对 `zyfkid` 载体） -> `2357`
- `7865085`（`admin` 自跑） -> `2357`
- `7865087`（`theend/g2v41submit418` 自跑） -> `2220`

这三场的隐藏层签名继续稳定：

- `decoded_stdin_records = 85`
- `case1 final_stage = 8`
- `err_decoded` 中依次出现：
  - `NPC_C / 你是色盲？`
  - `NPC_D / F的QQ里面还有什么信息？`
- 两次都落到 `KeyError: '8'`

因此当前最重要的新结论不是“又有一个 2357”，而是：

- 这组双隐藏问句补丁并不是 `v37` 分支特有
- 它能直接把 `g2auto v26(v36)` 风格路径从 `2220` 拉到 `2357`
- `case1` 的公开顺序块和 `NPC_E` 这类旧主线特征，并不会阻止隐藏 `stage 8` 触发

在 `77117` 之外，平台随后又对同一份 `v47` 代码补出了额外的单边自跑样本：

- `7865090` -> `2357`
- `7865091` -> `2357`
- `7865092` -> `2357`
- `7865093` -> `2357`
- `7865094` -> `2357`
- `7865095` -> `2357`

这些对局的 `match_detail` 里第一位仍然都是：

- user: `thebeginning`
- code_id: `f463cf4a-531a-48ed-b827-8786d97ee104`

而且没有第二位玩家信息，表现得更像平台额外触发的同码自跑样本。它们进一步强化了同一个判断：

- `v47` 并不是“三场刚好都高分”的偶然现象
- 至少在当前可见样本里，它已经表现出明显的稳定 `2357` 特征
- 截至本次更新，最近一批公开可见 `Game2` raw score 样本中，也还没有看到超过 `2357` 的分数

### 7.6 `g2auto v27` 主线正式提升：当前稳定版本已切到真实主线

在 `v47` 已经证明“同一组双隐藏问句补丁可直接迁回 `g2auto` 路径”之后，`2026-04-19` 已把同等逻辑直接上传到真实主线实体：

- username: `theend`
- entity: `g2auto`
- entity_id: `20441`
- uploaded version: `27`
- code_id: `99dd696247af49eba89ad358725f47cd`
- remark: `stable pre-answer dual hidden trigger from v47`
- 上传记录：
  - `Game2/runtime/submissions/20260419_031401_g2auto_v27/summary.json`

对应主线验证 batch：

- batch: `77118`

截至本次文档更新时，batch 里仍有两场“对手自跑”挂在 `评测中`：

- `7865376`
- `7865378`

但这不影响 `Game2` 的主结论，因为我方主线自跑已经完成并且全部同分：

- `7865373`（我方自跑，对 `admin` 载体） -> `2357`
- `7865375`（我方自跑，对 `zyfkid` 载体） -> `2357`
- `7865377`（我方自跑，对 `the_north` 载体） -> `2357`
- `7865374`（`admin` 自跑） -> `2357`

此外，同一份主线代码随后还出现了额外单边自跑样本：

- `7865379` -> `2357`
- `7865380` -> `2357`
- `7865381` -> `2357`

主线样本 `7865373` 的本地分析目录：

- `Game2/runtime/manual_g2auto_v27/7865373`

它的隐藏层签名和 `v43/v46/v47` 保持一致：

- `decoded_stdin_records = 85`
- `case0 final_stage = 6`
- `case1 final_stage = 8`
- `err_decoded` 中仍然出现：
  - `NPC_C / 你是色盲？`
  - `NPC_D / F的QQ里面还有什么信息？`
  - 并两次落到 `KeyError: '8'`

因此截至 `2026-04-19` 当前最应该被写进结论区的话是：

- `g2auto v27` 已经是当前真实主线
- 它在真实可写实体上已经表现出稳定 `2357`
- `g2auto v26 = 2220` 现在只应保留为历史平台记录
- 如果把“当前稳定版本”定版，那么应当定为 `theend / g2auto v27`

## 8. 下一步建议

后续如果继续迭代，优先级应改成：

1. 把 batch 当成“重复自跑采样器”，不再用对手强弱来解释分数。
2. 不要再为 `NPC_E/E`、`A/C/E` 顺序块、或 `python/auto` 运行类型继续花时间。
3. `v42` 已经排掉了“`answer` 之后机械补发两句隐藏尾部请求”这条线；后续不要再回到 post-answer 机械重放。
4. 重点盯这组已验证有效的 pre-answer 双问补丁，而不是继续堆 `case0` 细节。
5. `v44/v45/v46` 已经把“单问是否足够”和“顺序是否关键”都做空了；不要再回到 `C-only / D-only / 顺序反转` 这三类验证。
6. 不要再把“如何迁回真实主线”当成待办；这一步已经通过 `g2auto v27` 完成。
7. 如果还要继续压缩，只适合做极窄的“同义问句/措辞变体”实验：
   - 针对关键触发问句准备少量高针对性替换句
   - 根据回复关键词决定是否切换变体，而不是盲目重复问，或在尾部机械追加两句
   - 目标不是重新验证“有没有潜力”，而是确认是否存在同分的等价问法或更稳的措辞
8. 不要继续把“打印 `answer` 原始返回”当成高优先级方向；`v41` 已显示成功路径下 post-answer 玩家 `stderr` 不会稳定保留。
9. 普通账号之间的随机跨账号复测，以及“普通账号 + auto”组合复测，都已经被做空了；除非有新假设，否则没必要再重复。
10. 如果没有新的强假设，现在就可以把 `theend / g2auto v27` 视为 `Game2` 当前稳定版本。
11. 任何后续实验都至少做多次自跑样本验证，避免再次被单场结果误导。

## 9. 历史材料

更早的历史记录仍保留在：

- `docs/game2_iteration_notes_20260311.md`
- `docs/game2_batch_and_result_analysis.md`
- `docs/game2_deepclue_rules_and_sdk_analysis.md`

但阅读顺序应改为：

1. 先看本页
2. 再看 `game2_batch_and_result_analysis.md`
3. 最后把 `game2_iteration_notes_20260311.md` 当历史背景

## 10. 2026-05-10 状态修正：当前不是“接近上限”，而是 Poker/Yuan 搜索方式卡住

当前新环境下的主线分数已不是本页早期记录的 `2357` 阶段。近期单人房间评测的已知最高为 `2797`，来自 `n556y1/n559a` 一类版本：完整 Rose+Z/F+Poker 高分骨架上叠加 Yuan 低频隐藏 `+40`。`n559a` 已知有效分布为 `2797 x2, 2757 x7, 2717 x5, 2697 x1, 2517 x1`，不是稳定全高分版本。

需要修正的判断：

- `2757` 不是理论上限，只是当前完整骨架的常见高分格。
- `2797` 不是 Yuan 最终答案正确，而是 Yuan 隐藏聊天/调查 `+40` 叠加到 `2757`。
- Poker 目前只确认到 stage3 聊天增量，没有确认答案字段或 stage4 入口。
- Yuan 目前只确认到低频 `+40`，没有确认 murderer/motivation/method 的稳定答案分。
- 后续卡点主要是方法论问题：过度围绕已知高分 trace 做措辞微调，缺少对 Poker/Yuan 单独评分维度的系统重建。

新的执行原则：

1. 继续只用开房间单人评测，不使用 batch，不上天梯。
2. 上传实体名/备注必须保持中性，不能暴露策略。
3. 后续 Poker/Yuan 优先用专项 harness 拆分评分维度，而不是继续在完整高分骨架上微调问句。
4. 如果专项实验不能在隔离态产生明确增量，不应贸然移植到完整骨架。

## 11. 2026-05-10 后段剧本专项状态

当前仍无超过 `2797` 的稳定突破，但后段剧情上限有了更清晰边界：

- Poker 已确认可触发 `401/402` 监控证据；隔离分仍为 `507`，说明它不是独立 stage4 分层。
- Poker 监控长答案 `n569a-e` 全部负收益，不能作为新答案分入口。
- Yuan `703/704` 可稳定出现，但不是充分加分条件；多个隔离样本拿到 `703/704` 仍为 `207`。
- Yuan `n572b` 的 false-marked witness-first 十点半链出现 `247`，这是当前唯一新的 Yuan 隔离正信号。
- 完整移植暂未突破：`n572c` 为 `2717,2757,2757`；`n573a` 正在扩样。
- `n568c` 小样本为 `2757,2797`，已启动追加房间评测验证是否可作为优于 `n559a` 的候选。
- 增量观察：`n568c_more` 暂见 `2717,2657`，`n573a` 为 `2757,2757,2517,2717`；两者都还不能视为稳定突破。
- 后续扩样：`n568c_more` 完整为 `2717,2657,2757,2797,2757,2717`，只能说明可复现 `2797`，不能说明稳定提升。
- `n573b/c` witness 移植继续出现 `2657/2557` 低尾；强行嫁接 witness 十点半链暂时不是 keeper。
- `n574` Poker 医生/尸检方向未打开新分：隔离 `507 x2`，完整首局 `2757`。
- `n575` Yuan witness 后答案方向未打开：competitor/false-witness/teacher 答案当前均为 `207` 起步。
- `n576` witness 双问强化首次因 Saiblo API timeout 未上传成功；低频长超时重试仍卡在 `/api/profile/`，未绕过 username safety check。
- 已启动 `scripts/game2_late_probe_retry.sh` 作为恢复回调；profile 校验通过后会自动重跑 `n576a-c` 与 `n574c`，日志在 `Game2/runtime/game2_late_probe_retry.log`。

当前建议版本仍不是定版。若 `n568c_more` 或 `n573*` 不能提高 `2797` 频率，继续把 `n559a/n568c` 作为高分基线，把 `n572b` 作为 Yuan 隐藏 `+40` 的专项线索，而不是把 witness 问句直接合入主线。

## 12. 2026-05-09 18:33 UTC 后段剧本上限扩展队列

本轮停止围绕 `2757/2797` 既有高分骨架做细节微调，改为把 Poker/Yuan 拆成评分维度专项。Saiblo 当前仍在 `/api/profile/` 读超时，未通过 username safety check，因此没有上传、没有开房、没有 batch。

新增 Yuan 专项候选，均使用中性实体名并准备通过单人房间评测：

- `n577a`：全零 Yuan 隔离；聚焦真实死者/身份置换、李海天尸检、海豚挂件、尸块 DNA、死亡时间和手机操作时间线。
- `n577b`：全零 Yuan 隔离；先稳定问投票异常，再接生物馆/1919/世纪林 witness，测试投票与 witness 是否存在交互触发。
- `n577c`：全零 Yuan 隔离；把李海天/学生会会长链与十点半生物馆目击拆成两轮，避免旧复合长问互相污染。
- `n577d`：全零 Yuan 隔离；继续用 `marks=False` 角色提交短答案，验证“身份置换/灭口/转移尸块”短答案是否有直接答案分。
- `n577e`：完整骨架；保留 `n559a` 的 Yuan 前三轮，第四轮改为 `705` 条件追问李海天尸检报告，否则只问下一项关键物证。
- `n580a`：全零 Yuan 隔离；凶手填第一个 `marks=True` 竞争者，测试投票获利+手机/身份置换答案字段。
- `n580b`：全零 Yuan 隔离；凶手填第二个 `marks=True` 老师，测试投票造假/掩盖名额答案字段。
- `n580c`：全零 Yuan 隔离；凶手填 `marks=False` witness，测试生物馆/1919/尸块转移答案字段。
- `n580d`：全零 Yuan 隔离；凶手仍为 `无名氏`，只强化真实死者、替身、李海天尸检、海豚挂件和死亡时间伪造 method/motivation。

新增 Poker 专项候选：

- `n578a`：Poker 隔离；只把 method 置为 `未知`，验证当前 Poker direct 分是否主要来自 murderer，而不是错误 method 文本。
- `n578b`：Poker 隔离；只把 murderer 置为 `无名氏`，验证 murderer 字段贡献。
- `n578c`：Poker 隔离；只给 motivation 增加身份/监控时间线动机，验证 motivation 是否能打开答案分或反而扣分。
- `n578d`：Poker 隔离；基于 `n568c` 监控路线，保留动态 suspect，只改 method 为监控时间线与冰冻刀具身份错位。
- `n578e`：完整骨架；Poker stage3 后只问接待者责任链/隐瞒原因，不改答案，测试隐藏 `+40` 或 residual 过滤。
- `n578f`：完整骨架；Poker stage3 后只问一个非信息源/接待者 NPC 的尸检事实，不改答案，测试医生线隐藏组件。
- `n579a`：Poker 隔离；复现 `401/402 -> 真梅花5/Joker身份 -> 衣帽间密码/破绽 -> 404/501` 的 post-monitor 链，不改答案，验证 `404/501` 是否有独立净分。
- `n579b`：完整骨架；把 post-monitor 链接回 `n568c`，测试高证据链是否能在完整路线中稳定上穿。
- `n579c`：完整骨架；`n579b` 加 Yuan `705` 条件第四问，给 Poker 长链后留更有针对性的 Yuan 预算。
- `n579d`：Poker 隔离；post-monitor 链后保留动态 suspect，只改 method/motivation 为监控身份错位与移尸破绽，验证答案字段是否能吃到 `404/501`。
- `n581a`：Poker 隔离；post-monitor 链后若拿到 `501`，追问匿名转账、看诊、Joker、林渝植和人口贩卖关系，不改答案。
- `n581b`：Poker 隔离；post-monitor 链后若拿到 `404`，追问车牌、7:20车辆、司机、移尸地点、后院窗户和衣帽间关系，不改答案。
- `n581c`：完整骨架；post-monitor 后 Yuan 首轮直接问李海天尸检、海豚挂件、501、人口贩卖、1919黑车和生物馆跨线索，Yuan 仍置零。
- `n581d`：完整骨架；若 Poker 侧拿到 `501`，Yuan 改成极短同源 meta 问，否则保持原 Yuan 结构。

恢复回调已更新并重启：

- 进程：`scripts/game2_late_probe_retry.sh`，当前 PID 以 `ps` 为准，本节最后一次重启后为当前后台 watcher。
- 账号守卫仍为 `--expected-username thebeginning`，不绕过 profile 校验。
- 恢复后会并行启动三组队列：`n576a-c/n577a-d/n580a-d`，`n578a-d/n579a/n579d/n581a-b/n574c`，`n577e/n578e/n578f/n579b/n579c/n581c-d`；每组内部串行，每个默认 `3` 局单人房间评测。
- 队列结束后会自动运行 `Game2/tools/summarize_late_probe_results.py`，生成 `docs/generated/game2_late_probe_results.md/json` 供快速看分布和 `401/402/404/501/703/704/705` 等证据。
- 回调仍不使用 batch、不激活、不上天梯，备注统一 `r`。

本地只读复盘补充：

- Poker 已见 `404` 车牌证据与 `501` 巨额转账证据，但都出现在 `401/402` 之后的身份/密码链上；`n579a-d` 专门验证这条 post-monitor 链是否有净分，而不是继续把 `401/402` 当 stage4。
- Yuan 已见 `705` 李海天尸检报告共 5 个房间样本，但仍是 stage1，且没有稳定分数上穿；当前判断为剧情上限线索，不是已证实的独立计分层。
- `705` 常在第一轮 broad 后、第二轮 witness 给出十点半生物馆跑出者时与 `703/704` 一起刷新；因此下一步优先测 `n577e` 的条件追问，而不是继续增加李海天关键词堆叠。
