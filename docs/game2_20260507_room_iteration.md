# Game2 2026-05-07 Room Iteration

更新时间：`2026-05-09 06:39 UTC`

## 当前口径

- 只迭代 `Game2 / Game53`，不能影响 `Game1`。
- 不上天梯；Saiblo 可见实体名与备注必须保持中性，例如 `n507c` / `r`。
- 不再用 batch 做版本判断。Game53 的后手/反向对局没有意义，当前只用单人房间评测自己分数。
- 当前 token 于 `2026-05-07 10:26 UTC` 通过 `entities --game-id 53` 实测仍解析为 `thebeginning`。如果预期账号是 `theend`，需要先重新核验 token。

## 单人房间评测方法

已确认 Game53 支持单人房间：

```bash
python3 Game2/tools/run_room_eval.py \
  --code-id <code_id> \
  --label <neutral_label> \
  --count 5 \
  --timeout 420 \
  --poll-interval 2
```

API 流程：

1. `POST /api/rooms/`，payload 为 `{"game_id":53,"player_number":1}`。
2. `POST /api/rooms/<room_id>/join/`，把我方 code 放在 `order=0`。
3. `POST /api/rooms/<room_id>/begin_match/`。
4. 拉取 match detail、download、analysis，并写入 `Game2/runtime/room_matches/`。

注意：

- `player_number=2` 对 Game53 会报 `游戏人数设置不正确`。
- `match_download.json` 当前只暴露 key `0`，后续案子的行为主要要从 `decoded_stdin_records` 与玩家 `stderr` 反推。
- 因为后续案子的 per-case answer_result 不可见，版本判断必须依赖多局 raw score 分布，而不是单局高分。

## 当前剧本结构

更新后的线上 Game53 一局会连续跑多个剧本，目前稳定观测到：

- `case0`: Rose 舞厅案。
- `case1`: Z 失踪 / F 死亡案。
- `case2`: 扑克公馆案。
- `case3`: 袁樱瞳碎尸案。

NPC 名字会混淆。当前可用鲁棒识别方式：

- Rose 案通过初始证据标题、证据正文、`marks` 映射角色，不依赖固定 NPC 名。
- Z 案通过初始证据 `002..006` 映射 `A..E`，不依赖固定 NPC 名。
- 扑克公馆案初始 hint 会给出“好的信息来源”。当前最高分突破来自只问该 NPC 两句，把扑克案从 stage1 偶发推进到 stage2。
- 袁樱瞳案当前最有效的直接策略是从 `marks=False` 中取嫌疑人，并使用专用动机/手法文本。

## 当前候选结果

以下结果均为单人房间或等价的先手自跑样本，只看自己 raw score：

| label | code_id | 房间样本 | 结论 |
| --- | --- | --- | --- |
| `n506o` | `4bc09eb23d4a4ca4bcc41d90ef69a143` | `2320, 2320, 2320, 2520, 2520` | 不稳定，Rose motivation 在部分局仍 false |
| `n506e` | `a63c0344959c4189beba83019bdc4c2b` | `2480, 2520, 2520, 2520, 2480` | 当前最稳基线，继续扩大样本 |
| `n507a` | `771c9b2bbca14abfb843446b1571d3f2` | `2520, 2320, 2520, 2520, 2520` | 丢弃，合并后的长 Rose 动机仍会 false |
| `n507b` | `6a4227aaba064fb89cb852215f7f0b3d` | 已见 `2280, 2320, 2120, 2080` | 丢弃，扑克案追加聊天/接待者作答明显负收益 |
| `n507c` | `708161b6c6904f9298997a0026ff4288` | `2280, 2280, 2320, 2320, 2520` | 丢弃，扑克案直接详细答案明显负收益 |
| `n507d` | `ef2b644ccb4a4d0e932e686810590624` | `2120, 2280, 2280, 2280, 2320` | 丢弃，扑克案林渝植自杀假设明显错误 |
| `n507e` | `5f6989b8499140b8bf61456f998bfc09` | `2520, 2480, 2520` | 诊断版，行为基本等同 `n506e`；确认 `answer` 返回的是下一案初始状态，不是已提交案子的 answer_result |
| `n507f` | `95399a50b88d4354aafa2adb64981a18` | `2520, 2520, 2520, 2280, 2520` | 丢弃，只改袁案长措辞仍会出低分 |
| `n507g` | `3f704336ae714e789c885d7d8985898b` | `2480, 2520, 2480, 2480, 2520` | 不优于基线；扑克案少量初始聊天能保住但不抬低尾 |
| `n507h` | `6271cc5a296144aebc4818bb680e042f` | `2480, 2520, 2320, 2480, 2520` | 丢弃，袁案追加聊天明显负收益 |
| `n507i/j/k/m` | `3c5a...` / `512d...` / `71d...` / `d3e...` | `n507i=2280/2480/2520`，`n507j=2480/2520`，`n507k=2280/2320`，`n507m=2480/2520` | 均未超过基线；扑克手法细化高风险 |
| `n507n` | `a8ac4d5ec26b4106b14e16814e8ff0d0` | `2617, 2617, 2657` | 移植 Z/F 双隐藏问句但顺序/重试形态不稳 |
| `n507p` | `12969fee7edd418da40f9842db155866` | 13 局：`2657 x8, 2617 x3, 2457 x1, 2417 x1` | 普通 `chat()` 对 stage8 错误重试，低尾明显 |
| `n507q` | `22ae3a9fadb84581a59f32255bf9d49a` | 13 局：`2657 x11, 2617 x1, 2457 x1` | 当前最高均值候选；Z/F 隐藏双问用单次 probe，最高 `2657`，但不稳定 |
| `n507r` | `4f9af4b7a36b4ccc858bdb91b0214421` | `2657, 2657, 2457` | 丢弃，扑克轻调查/接待者方向仍有低尾 |
| `n508a` | `5cfde1928dfa42d294c3466808f4c094` | 成功 19 局：`2657 x10, 2617 x4, 2457 x2, 2417 x3`；另 1 局评测失败 `0` | 丢弃，Rose stage6 兜底不能稳定，且低尾更差 |
| `n508b` | `ccf8d9322f8a45dd9b87a5dedda306bd` | `2617, 2457, 2657, 2617, 2657` | 丢弃，袁案基础问 + 改嫌疑人无净收益 |
| `n508c` | `19e123d68fae4f3584121c39305d8cd5` | `2657, 2617, 2457, 2657, 2617` | 丢弃，扑克案改取首个 `marks=False` 无收益 |
| `n508d` | `64948c193e4e49a7bcd559ca664f6ea2` | `2657, 2217, 2617, 2457, 2617` | 丢弃，袁案改取初始首角色会严重掉分 |
| `n508e` | `0fa75da095d149719855afc0402d7723` | `2657, 2617, 2617` | 丢弃，袁案保留原答案但增加调查无突破 |
| `n508g` | `300115b399414abba2842ffef141ebcb` | 约 24 局：`2657 x16, 2617 x7, 2457 x1` | `n507q` 同级候选；加入较短后案答案措辞但未突破 `2657` |
| `n508t` | `123fdc09f0ae41c3b776ec51bd4f7242` | 扩样后仍见 `2617/2457` | Rose 补问保护未消除低尾 |
| `n509a` | 未记录在本表 | `2657 x5, 2617 x1` | Rose fan 冷淡 fallback 仍会低尾 |
| `n511a` | `a2b68a7ec9b84a59a8dfd836defd930c` | 19 局：`2707 x7, 2667 x2, 2657 x5, 2617 x3, 2507 x1, 2457 x1` | 当前最高上界候选；扑克“好信息来源”两问把最高分从 `2657` 提到 `2707`，但仍有低尾 |
| `n511b` | `38741d0f96d74d42a8c315aa2cb4870c` | `2657, 2457` | 丢弃，袁案全员基础问无净收益 |
| `n512a` | `673e54c862394b58a2ce790b63416e55` | 有效 3 局：`2707 x1, 2507 x2`；另 1 局平台失败 | 扑克精确问“案发现场+手中证据”后再问身份关系，3/3 触发 stage2；历史上传版未关闭 stderr，后续默认用安全版 `n514e` 复测 |
| `n512b` | `06cbd0a140e344ed896fd4d334e3c959` | 有效 3 局：`2667, 2657, 2457`；另 1 局平台失败 | 拆分“现场/证据”只 1/3 触发 stage2，降优先级 |
| `n512c` | `38d60d2bbcd64ae7ac060ac388ebf5e6` | 有效 3 局：`2617, 2617, 2507`；另 1 局平台失败 | 面具/身份导向差，降优先级 |
| `n512d` | `4ce60e89a15d4103a0d5cd956a4ff9f0` | 有效 3 局：`2707, 2667, 2617`；另 1 局平台失败 | 原计划 stage fallback 未生效，因为未知案前 `g.stage` 未重置；结果基本等价 `n511a` |

`n506e` 当前已知样本扩大到 `18` 局，分布为 `2480 x4, 2520 x14`，均值约 `2511.1`，最低分 `2480`。

`n511a` 是当前最高上界候选：在 `n507q` 基础上只改扑克公馆案，对 hint 中的“好的信息来源”NPC 追加两句：

```python
g.chat(info_id, '你为什么是好的信息来源？你发现了哪些别人不知道的线索？')
g.chat(info_id, '你是谁？你和死者是什么关系？')
```

这会在约一半样本里把扑克公馆案推进到 stage2，raw score 对应增加约 `+50`：基础 `2657/2617/2457` 分别变成 `2707/2667/2507`。19 局均值约 `2651.7`，最高 `2707`，但不能视为稳定。

## 分案进度判断

当前只能完整看到 `case0` 的 replay；`case1..3` 主要靠玩家 `stderr` 和总 raw score 反推。

- Rose 舞厅案：可见 replay 中稳定 `stage=6`，提交答案三项全对，通常调用约 `41` 次聊天。`2617` 低尾常见于 stage6 由 `某某喜欢你？` 而不是 `某某态度怪？` 触发；`n508a` 尝试多问同角色兜底仍不能稳定修复。
- Z/F 案：`n507q` 稳定推进到 `stage=7`，公开调查约 `40` 次聊天，加上两句隐藏 probe 共 `42` 次。两句隐藏问句会在 decoded records 中产生两次 `Internal Server Error: '8'`，这是当前已知的有效高分触发。普通 `chat()` 重试版 `n507p` 会把错误刷到更多次并带来更差低尾。
- 扑克公馆案：`n511a` 首次证明后案调查可加分；当“好信息来源”两问触发 stage2 时，总分稳定多约 `50`。`n512a` 的“请说说案发现场情况，以及你手中的证据。/你是谁？你和死者是什么关系？”在 3 个有效样本中 3/3 触发 stage2，但历史上传版未关闭 stderr，因此下一轮默认用安全版 `n514e` 扩样。注意 `Game.stage` 在进入 `solve_unknown()` 前不会自动重置，任何依赖 stage 的未知案条件逻辑都必须先显式 `g.stage = 0`。本地扫描已有 396 条扑克轨迹，尚未观测到 stage3+；旧 `v50/v52` 能解锁现场证据 `102/103/104`，部分样本还能到 `203/204/205`，但原探针伴随大量聊天和错误接待者嫌疑人，分数仅约 `1457/907/584`，只能抽取最小问句复测，不能直接复刻。
- 袁樱瞳碎尸案：当前基线用 `marks=False` 取嫌疑人并直接提交专用动机/手法。`n508b` 的基础问显示初始首角色会给出“长相相似、捡到手机、等到周五、竞争名额”等强线索，但 `n508d` 直接改用首角色会掉到 `2217`，说明这些线索角色不是可提交凶手。`n511b` 隔离“只问所有 visible NPC 一句基础问，不改嫌疑人”的结果为 `2657,2457`，没有净收益。本地扫描 369 条袁案轨迹，最高只观测到 stage1；旧探针能解锁 `703/704`，但分数低且没有证明调查进度能抵消效率/答案扰动，因此袁案继续降优先级。

## 理论上限判断

按 SDK 文档，每个剧本分数由答案正确性最高 `600`、调查进度最高 `400`、成就 `40/个` 和效率系数组成，总分为各剧本求和。因此四个剧本的纯理论上限至少应远高于当前最高观测 `2707`。当前差距最大的位置不是 Rose，而是：

- 后两个新增案子仍几乎没有做完整调查；扑克案只证明到 stage2 就能多约 `50`，理论上 stage3+ 和袁案调查仍应有空间。
- Z/F 案当前已恢复旧隐藏双问并实测提升到 `2657`，但可见 stage 仍停在 `7`，stage8 通过平台 `KeyError('8')` 体现，无法继续正常深挖。
- 但平台评分含效率系数，盲目加聊天会降低效率且可能扰乱答案匹配；已完成测试显示扑克、袁案追加信息经常从 `2657` 掉到 `2617/2457/2417/2217`。

### 2026-05-07 理论上限审计

本地当前可用分析材料约为 `327` 个 room `analysis.json` 和 `192` 个 batch `analysis.json`。按现有证据，仍不能认为已接近所有剧本理论上限：

- 最高房间样本仍来自 `n511a` 系列：合并可见有效样本为 `2707 x7, 2667 x2, 2657 x5, 2617 x3, 2507 x1, 2457 x1`。单个目录中最高均值是 `n511a_more1`，`8` 局均值 `2678.25`，但最低仍有 `2617`。
- 旧高基线 `n507q/n508g/n508t` 最高停在 `2657`，说明 Z/F 隐藏双问和 Rose 当前路径只能形成旧平台，不能解释 `2707`。
- 扑克公馆案只确认到 stage2；旧 `v52`/`v50` 能把可见证据拓到 `102/103/104` 和 `203/204/205`，但旧探针提交凶手和额外问句有明显负收益。`n515a/b/c` 正是为了隔离 stage2 后最小接待者追问是否能把这些证据转化为净分。
- 袁樱瞳案只确认 `703` 较容易出现，`704` 只在旧 `v50` 长路线中被观测到，且没有房间净收益证明。`n515d/e` 只抽取最小 704 前置问句，属于低优先级验证。
- 成就分当前没有任何稳定可利用证据；历史规则分析也认为成就系统长期像占位符。因此当前理论上限扩展主要应看调查进度和答案文本，而不是成就。
- 结论：当前不是“已到理论上限”，而是“外部开房间阻塞导致不能验证下一批后案进度候选”。平台恢复后的首要任务仍是用单人房间评测安全版 `n514d/e/g`、`n515b/a` 与后续 `n516/n515c` 接待者候选。

## 已排除或降优先级方向

- 不再用 batch 做 Game53 版本判断；后手/反向样本无意义。
- `n506m` 试过删 Z 案尾部 3 问，结果掉到 `2280`；Z 尾问虽然不升 stage，但保分很重要。
- `n506n` 试过 Rose stage6 后提前提交，结果 `2400`；Rose 尾问保分很重要。
- `n507a` 证明 Rose 动机不能简单变长；即使包含家族资金、独占、情敌等词，仍会有 motivation false。
- `n507b` 证明扑克案不能盲目追加聊天；额外步骤没有带来可见收益，反而大幅掉分。
- `n507f/n507h` 证明袁案不能盲目拉长答案或追加聊天；当前直接短答案更稳。
- `n507k` 的早期样本显示扑克手法细化可能直接掉到 `2320`，后续若继续挖扑克应先做诊断而不是生产候选。
- `n508a` 证明 Rose stage6 同角色兜底问句不能稳定修复 `2617`，扩样还出现 `2417`。
- `n508u/v/w`、`n509a-g`、`n510a-d` 多组 Rose stage6 补问/重问/改问方向未能消除低尾，Rose 方向降优先级。
- `n508b/e` 证明袁案基础问和后续针对性问法没有产生可见净收益。
- `n508c/d` 证明后案嫌疑人不能简单从 `npcs[0]` 或首个 `marks=False` 以外的可疑角色改写；特别是袁案首角色假设会出现 `2217`。
- `n512b/c` 证明扑克“拆分现场/证据”和“面具/身份导向”不如 `n512a`，stage2 触发率分别只有 1/3。

## 当前疑点

- `2617/2457/2417` 的差距仍无法直接定位到某个后续案子的 answer_result，因为平台下载只给第一案完整 trace。
- 扑克公馆案的正确直接答案仍未确定。当前“接待者杀人”和“广义身份混淆杀人”都不可靠。
- `2707` 是否是当前实现可达上界还未证明；它只是扑克 stage2 叠加旧高基线后的最高观测值。
- `n512a` 的 stage2 触发率 3/3 很有价值，但样本太少，且同时出现两个 `2507` 低尾；后续应通过安全版 `n514e` 复测并扩到至少 12-16 个有效样本。
- 袁案基础问答暴露了“强线索角色”和“可提交凶手”之间的反直觉差异，后续若继续挖袁案，应优先寻找 scoring 接受的 murderer/motivation/method 组合，而不是只按剧情直觉改凶手。
- `2026-05-07 09:00 UTC` 左右，Saiblo 的 `POST /api/entities/<id>/codes/` 和 `POST /api/rooms/<id>/join/` 连续返回 500；新建的 `n513a-d/n514a-b` 代码卡在 `未编译`，扩样暂时受阻。进一步诊断显示 `join` 虽返回 500，但房间详情里 code 已坐入；随后对这些 Game53 房间调用 `begin_match` 仍返回 500，因此当前阻塞在房间启动链路，不是 code_id 或单人房间 payload 问题。
- 旧 `n513*` 候选已被后续 `n514/n515` 队列吸收或降级为历史问句来源：`n514c/h` 覆盖 `n513g` 的现场证据追问，`n514e` 覆盖 `n512a` 问法纯隔离，`n515a/b/c` 覆盖 stage2 后接待者问句。

## 当前迭代原则

- 以 `n511a` 为当前最高上界/最高样本候选；安全版 `n514d/n514e` 是下一轮优先上传评测候选。若追求更低风险，可同时保留 `n507q/n508g` 作为未做扑克 stage2 追问的旧高分基线。
- 每次只改一个后续案子的直接答案或极少量问题，避免同时扰动 Rose/Z。
- 每个候选至少跑 5 局房间；如果出现明显低分，可以提前判坏，但仍记录结果。
- 判断优先级：最低分 > 均值 > 最高分。单局 `2657` 不足以定版。
- 平台 POST 恢复后按 `docs/game2_recovery_eval_queue.md` 执行：先上传并测安全版 `n514d/e`，再测 `n514g` 与 `n515b/a`，随后再测 `n516a/b/c`、`n514c/h/n515c` 和袁案 P3 候选。旧 `n512a/n513*` 只作为历史样本或问句来源，不再作为默认扩样目标。

## 2026-05-07 09:32 UTC 继续迭代记录

Saiblo 状态：

- `codes --entity-id 21072` 仍显示 `n513a` 两个 code 卡在 `未编译`，未自动恢复。
- 对 `n511a` code_id `a2b68a7ec9b84a59a8dfd836defd930c` 再跑两次单人房间探针，`POST /api/rooms/918242/join/` 与 `POST /api/rooms/918262/join/` 均返回 500。
- 进一步手动诊断显示：`GET /api/rooms/918262/` 能看到 `n511a` 已坐入单人房间，但 `POST /api/rooms/918262/begin_match/` 仍返回 500；因此阻塞仍在平台开赛链路。`918242` 后续查询已 404。
- `2026-05-07 09:40 UTC` 再测 `n511a`，`room 918360` 仍卡在 `begin_match` 500；容错脚本已验证能在 summary 中保留失败 `room_id`。
- 当前仍不应高频重试上传/开房间；可低频探测恢复，恢复后只用单人房间扩样。

本地并行复盘的新结论：

- 扑克公馆案的初始 `marks` 形态通常是唯一 `True` NPC 等于 hint 里的“好的信息来源”。最新候选优先从 hint 解析中文名并映射 NPC；失败时才回退唯一 `marks=True`，避免 hint 与 marks 不一致时问错人或提交拼音名。
- `n511a` 未在 `solve_unknown()` 开头重置 `g.stage`，因此无法用 `g.stage >= 2` 写 stage2 后续逻辑；任何 stage 条件版都必须显式 `g.stage = 0`。
- 扑克案当前最小高潜力路线仍是：先问“好的信息来源/别人不知道的线索”，再问身份关系；如果 stage2 后出现 `102/104/103`，只加一条携带现场证据的追问。
- 袁樱瞳案目前不要改提交凶手：强线索角色和可提交凶手不一致，历史上改成首角色或按回复改凶手都出现明显掉分。
- 袁案若继续探索，应只做隔离实验：保留 `marks=False` 凶手，只问所有 NPC 一句“最关键线索”，或只做短动机文本替换；不要把 1919/生物馆等长线索一起写入答案。

新增本地候选，均未上传、未开房间：

| label | source | 目的 |
| --- | --- | --- |
| `n514c` | `Game2/deepclue_ai/n514c/ai.py` | `n513g` 的鲁棒化版：Poker 信息源优先 hint 中文名映射，保留 `n511a` 两问，stage2 后用 `102/104/103` 追加一条现场证据问。 |
| `n514d` | `Game2/deepclue_ai/n514d/ai.py` | 低风险对照版：Poker 信息源优先 hint 中文名映射，失败回退唯一 `marks=True`，不追加证据问。 |
| `n514e` | `Game2/deepclue_ai/n514e/ai.py` | `n512a` 问法纯隔离版：第一句改为“案发现场情况+手中证据”，不加证据追问、不加短动机。 |
| `n514f` | `Game2/deepclue_ai/n514f/ai.py` | 袁案低成本 704 探针：保留 `marks=False` 凶手，只遍历初始 `marks=True` NPC 问“你觉得谁在说谎？为什么？”，命中投票关键词即停止。 |
| `n514g` | `Game2/deepclue_ai/n514g/ai.py` | `n514d` 的条件短动机版：只有 Poker stage2 成功后才把 motivation 从 `未知` 改为短动机。 |
| `n514h` | `Game2/deepclue_ai/n514h/ai.py` | `n514c` 加扑克短动机文本：同时验证 stage2 后证据追问与直接动机是否有叠加收益。 |
| `n514i` | `Game2/deepclue_ai/n514i/ai.py` | `n514g` 加袁案短动机替换：只把袁案动机改成 `49人/2人不投/48票/24:23`，不新增袁案聊天。 |
| `n515a` | `Game2/deepclue_ai/n515a/ai.py` | `n515b` 加条件短动机：stage2 后从新 hint/marks 找接待者，单句合并问“聊天记录、宾客到达时间表、公馆异常”。 |
| `n515b` | `Game2/deepclue_ai/n515b/ai.py` | `n515a` 去掉扑克短动机，隔离接待者一问本身是否有收益或低尾。 |
| `n515c` | `Game2/deepclue_ai/n515c/ai.py` | `n514h` 加旧接待者轨迹两问：异常发现 + 证词破绽，用于测试更贴近 `203/204/205` 历史刷新的高风险路线。 |
| `n515d` | `Game2/deepclue_ai/n515d/ai.py` | 袁案最小 704 路线：基础问全员定位老师，再只问老师“你觉得谁在说谎？为什么？”。 |
| `n515e` | `Game2/deepclue_ai/n515e/ai.py` | 袁案 703+704 路线：`n515d` 之外，如果 `703` 已出现，再对嫌疑人携带手机证据追问照片/假发/长相相似。 |
| `n516a` | `Game2/deepclue_ai/n516a/ai.py` | Poker stage2 后只问接待者“公馆内有什么异常发现？”，隔离旧轨迹中最可能直接产生 `203/204/205` 的单问。 |
| `n516b` | `Game2/deepclue_ai/n516b/ai.py` | Poker stage2 后单问异常发现，并点名电脑、方形塑料盒、厨房少三把刀，测试更强关键词是否稳定触发 `203/204/205`。 |
| `n516c` | `Game2/deepclue_ai/n516c/ai.py` | Poker stage2 后按旧 hint 分拆三问：聊天记录、宾客到达时间表、公馆异常；不问证词破绽，不改凶手。 |
| `n516d` | `Game2/deepclue_ai/n516d/ai.py` | Yuan 更省聊天 704 探针：只问初始 `marks=True` NPC 课程展示投票是否异常，命中投票关键词即停止。 |
| `n516e` | `Game2/deepclue_ai/n516e/ai.py` | Yuan 极省聊天 704 探针：优先只问第二个 `marks=True` NPC 课程展示投票异常，用于验证老师顺序假设。 |
| `n517a` | `Game2/deepclue_ai/n517a/ai.py` | `n514e` 的信息源问法 + stage2 后接待者合并 hint 单问，补齐 `n512a` 高 stage2 触发问法与接待者路线组合缺口。 |
| `n517b` | `Game2/deepclue_ai/n517b/ai.py` | `n514e` 的信息源问法 + stage2 后接待者异常关键词问。 |
| `n517c` | `Game2/deepclue_ai/n517c/ai.py` | `n514e` 的信息源问法 + 条件短动机，不加接待者问。 |
| `n517d` | `Game2/deepclue_ai/n517d/ai.py` | `n514e` 的信息源问法 + stage2 后接待者分拆旧 hint 三问。 |
| `n517e` | `Game2/deepclue_ai/n517e/ai.py` | `n514e` 的信息源问法 + stage2 后接待者三证据精确问：电脑、塑料盒、少三把刀。 |
| `n517f` | `Game2/deepclue_ai/n517f/ai.py` | `n514e` 的信息源问法 + stage2 后接待者低泄露关键词问：电脑、冰柜、刀具异常。 |
| `n517g` | `Game2/deepclue_ai/n517g/ai.py` | `n517e` 加 Poker 手法文本候选：死者用冰块固定刀具自杀并伪装他杀。 |
| `n517h` | `Game2/deepclue_ai/n517h/ai.py` | `n514e` 的信息源问法 + 条件 Poker 手法文本，不加接待者问，用于隔离答案文本本身。 |

编译检查：

- `n514c/d/e/f/g/h/i`、`n515a/b/c/d/e` 与 `n516a/b/c/d/e` 均已用 Python `compile()` 检查语法通过；最新一轮还用 `陆亦初/林晚舟/张子韩` 样例验证了 Poker hint 正则不会把姓名解析成 `林晚舟关` 或 `张子韩知`。
- `Game2/tools/run_room_eval.py` 已加一处容错：如果 `join` 返回 500 但房间详情显示 code 已坐入，会继续尝试 `begin_match`，避免平台半恢复时被 `join` 假失败挡住。
- `Game2/tools/run_room_eval.py` 继续补强：`begin_match` 失败时会把已创建的 `room_id` 写入 summary，便于后续手动诊断平台房间状态。
- 新增 `Game2/tools/summarize_room_evals.py`：扫描 `Game2/runtime/room_matches/`，按 base label 与 exact label 汇总所有单人房间有效样本、均值、最低/最高分和分布。输出到被 git 忽略的 `docs/generated/game2_room_eval_summary.{md,json}`，用于本地快速查看当前最高上界。当前合并视图确认 `n511a` 为 `19` 个有效样本，分布 `2707 x7, 2667 x2, 2657 x5, 2617 x3, 2507 x1, 2457 x1`。

恢复后的测试顺序建议调整为：

1. 先上传并评测安全版 `n514d/n514e`，各跑 5 个有效样本；`n514d` 是 `n511a` 安全对照，`n514e` 是 `n512a` 问法安全对照。
2. 若 `n514e` 复现 `n512a` 的 stage2 触发优势，再扩到 12-16 个有效样本；旧已上传 `n512a` 只保留为历史 3 样本，不再作为默认扩样目标。
3. 再测 `n514g`，确认 stage2 条件短动机是否有净收益。
4. 并行测 `n515b/n515a` 各 5 局，确认 stage2 后最小接待者一问能否用很少代价解锁 `203/204/205` 并转化为净分。
5. 若 `n514e` 复现 `n512a` 的高 stage2 触发率，优先测 `n517a/n517c`，再测 `n517e/n517f/n517h/n517g/n517b/n517d`。
6. 再测 `n516a/b/c` 与 `n514c/n514h/n515c`，比较接待者短问、分拆问、现场证据问和高风险叠加路线。
7. 最后测 `n514f/n516d/n516e/n514i/n515d/n515e`，因为袁案已有多次负收益记录，优先级低于扑克 stage2+。

`2026-05-07 10:29 UTC` 补充：新增 `n516a/b/c` 作为 `n515b` 的接待者单问/分拆问对照，应排在 `n515b/a` 后、`n515c` 前；新增 `n516d` 作为 `n514f` 与 `n515d/e` 之间的 Yuan 省聊天对照。`2026-05-07 10:35 UTC` 补充：因已上传的 `n511a/n512a` 是安全修订前版本，恢复后默认先上传安全版 `n514d/n514e`，不再优先扩旧 code。`2026-05-07 10:39 UTC` 补充：新增 `n516e`，只问第二个 `marks=True` NPC 投票异常，以最小聊天数验证老师顺序假设。

补充日志证据：

- 旧 `v52` match `8065213` 中，扑克案两句信息源问把 stage 推到 `2`，随后 hint 变为“接待者张子韩知道很多关于这次聚会的安排”。
- 同一局中，stage2 hint 原文要求问接待者“聊天记录、宾客到达时间表以及公馆内的异常发现”；随后最终可见证据出现 `203 房间电脑浏览记录`、`204 方形塑料盒`、`205 厨房刀具丢失`。因此 `n515a/b` 使用单句合并这三个 hint 关键词，`n515c` 作为 P2 高风险对照改用旧轨迹中最接近证据刷新的“异常发现 + 证词破绽”两问。
- 旧 `v52` 的完整接待者路线分数很低，主要因为它最终把接待者作为凶手提交且额外问了多句；`n515a/b/c` 只抽取最小一问，不改变提交凶手。注意 `v52` 没有在接待者单句后立刻刷新证据，因此“单句是否足够”仍需 `n515b/a/c` 实测隔离。
- `n515a/b/c` 已在 stage2 后刷新 `g.npcs()` 再找接待者，避免后续 NPC 列表变化时只查初始 `npcs` 而漏掉新可见接待者。
- 旧 `v50` match `8064818` 中，袁案 `704` 首次出现在 decoded record `180`；前一个关键回复是老师对“你觉得谁在说谎？为什么？”回答了 `49 人 / 2 人不投 / 48 票 / 24:23 / 笔迹不同`。因此 `n515d/e` 只抽取“基础问定位老师 + 说谎问”，不复刻 `v50` 的长探针。
- 袁案子任务复核认为 `v52` 的“投票结果为什么多出一票”直接问句没有确认解锁 `704`，因此优先使用 `v50` 中已观测到 704 前置的“你觉得谁在说谎？为什么？”。
- `n515d/e` 的老师定位已收窄为教师自述特征（如“我的学生/我手里/作为老师/作为教师/副教授/负责教学”），避免竞争者回复中的“课程展示/老师/出国名额”误触发。
- 对当前候选 `n514c-i/n515a-e` 补全旧 Rose 名系映射：`BaiJingTing/CuiAnYan/DengDaLing/FanMinMin/XiaoDingAng/XiaoDingGang/YeWenXiao`。本地扫描历史 `stderr` 后，当前可见 CamelCase NPC 名已全部能映射到中文，避免回滚或混入旧名时最终答案提交拼音。
- 平台恢复后的上传/评测队列已单独整理到 `docs/game2_recovery_eval_queue.md`；该队列明确不使用 batch、不激活、不上天梯，Saiblo 可见实体名保持中性编号，备注统一为 `r`。
- 当前候选 `n514c-i/n515a-e` 的扑克案提交名已改为优先从 hint 正则提取中文名；模式覆盖 `某某是个好的信息来源`、`某某会是...好的信息来源`、`问问某某关于...`、`接待者某某知道...`。即使后续出现未收录的拼音 NPC id，也尽量不把信息源凶手提交成拼音。

## 2026-05-07 10:00 UTC 继续迭代记录

Saiblo 状态：

- `2026-05-07 09:56 UTC` 对 `n511a` code_id `a2b68a7ec9b84a59a8dfd836defd930c` 再跑单人房间恢复探针，创建 `room 918362`；`join` 仍返回 500 但房间详情显示 code 已坐入，`begin_match` 继续返回 500。平台开赛链路仍未恢复。
- `2026-05-07 09:59 UTC` 通过 `entities --game-id 53` 核验当前 token，返回 username `thebeginning`，当前本地配置不是 `theend`。
- `entity 21072 / n513a` 的两个 code 于本轮 `codes --entity-id 21072` 仍为 `未编译`，不继续高频上传或开赛重试。
- `2026-05-07 10:14 UTC` 低频恢复探针仍失败：`room 918406` 表现为 `join` 500 但已坐入，`begin_match` 500；`entity 21072 / n513a` 两个 code 仍为 `未编译`。
- `2026-05-07 10:50 UTC` 低频恢复探针仍失败：`room 918471` 表现为 `join` 500 但已坐入，`begin_match` 500。
- `2026-05-07 11:00 UTC` 非开赛检查仍显示 token 为 `thebeginning`，`entity 21072 / n513a` 两个 code 仍为 `未编译`；不上传新候选。

本地理论上限扩展：

- 对旧 `v52` match `8065213` 复查：扑克 stage2 hint 位于 `analysis.json:437`，明确要求“聊天记录、宾客到达时间表以及公馆内的异常发现”；`203/204/205` 分别出现在 `analysis.json:567/574/581`。因此已将 `n515a/b/c` 的接待者最小问句统一改为 `请说说聊天记录、宾客到达时间表，以及公馆内的异常发现。`，语法检查通过。
- 对旧日志全量扫描确认：`203/204/205` 只在少量旧探针中出现，净分很低，说明它们是“有理论空间但必须最小化抽取”的方向；不能复刻旧长路线。
- 袁案 `703` 在旧日志中大量出现，通常由全员基础问触发；`704` 只在旧 `v50` 长探针中确认出现，证据内容位于 match `8064818 analysis.json:1640`。因此袁案继续保持低优先级，只测 `n514f/n515d/e` 这种最小 704 路线，不改变 `marks=False` 嫌疑人。
- 三个只读 explorer 的结论已合并：Poker 没有可靠 stage3+，应先稳住 stage2 并隔离最小接待者问；袁案不改凶手，`703/704` 只做 P3 小实验；候选实现应避免 hint/marks 不一致、中文名边界、`g.stage` 未重置等鲁棒性问题。
- 根据 explorer 结论已调整候选：`n514e` 改为纯 `n512a` 问法隔离版，`n514g` 改为 stage2 成功后才启用短动机，`n514c-i/n515a-e` 全部改为 hint-first 名字解析并修复中文姓名边界正则。
- `n514f` 进一步改为袁案低成本 704 探针，不再复测全员基础问；历史 `n511b` 已显示全员基础问没有稳定净收益。
- 新增 `Game2/tools/extract_story_unlocks.py`，扫描本地 `559` 个 `analysis.json`，其中 `525` 个有 decoded records。当前生成 `docs/generated/game2_story_unlocks.md/json`，可重复追踪 Poker `102/103/104/203/204/205` 与 Yuan `703/704` 的第一次可见位置。
- 新工具确认：`203/204` 仅各有 `3` 个本地首次解锁样本，`205` 有 `4` 个，且最高分仍只有 `1457`；`704` 仅有 `1` 个首次解锁样本。`203/204/205` 的旧轨迹在“异常发现”后又问了“证词破绽”，因此 `n515c` 被保留为高风险复核候选；总体仍支持“后案理论空间存在，但必须最小问句隔离，不可复刻旧长探针”的判断。

当前判断：

- 最高已验证仍是 `n511a`，19 个有效样本分布 `2707 x7, 2667 x2, 2657 x5, 2617 x3, 2507 x1, 2457 x1`。
- 分案理论上限仍明显未触顶：Poker 只稳定证明到 stage2，stage2 后证据 `203/204/205` 尚未做净收益隔离；Yuan 至少还有 `703/704` 调查进度空间，但历史上追加聊天负收益较多。
- 平台恢复后的队列改为优先测安全版 `n514d/n514e/n514g/n515b/n515a`，其中 `n515b/a` 代表“接待者合并 hint 单问”；`n516a/b/c` 是接待者短问/分拆问对照，`n515c` 属于 P2 高风险“现场证据问 + 接待者异常/破绽两问”。

## 2026-05-07 10:29-10:39 UTC 继续迭代记录

Saiblo 状态：

- `entities --game-id 53` 仍解析为 username `thebeginning`，不是 `theend`。
- `entity 21072 / n513a` 的两个 code 仍为长期 `未编译`。
- 对 `n511a` code_id `a2b68a7ec9b84a59a8dfd836defd930c` 再跑一次低频单人房间恢复探针，创建 `room 918439`；`join` 仍返回 500 但房间详情显示已坐入，`begin_match` 继续返回 500。
- 结论不变：不要上传新候选，不要高频开房间；平台恢复前只能做本地日志挖掘和候选准备。

本地理论上限扩展：

- 复盘旧 `v52` 轨迹发现，Poker stage2 后接待者旧路线实际包含“聊天记录”“宾客到达时间表”“异常发现”“证词破绽”四段，其中 `203/204/205` 在“异常发现”后、下一次取证据前已可能满足解锁条件，但旧日志无法确定是否必须再问“证词破绽”。
- 因此新增 `n516a/b/c`：分别测试“只问异常发现”“异常发现并点名电脑/塑料盒/刀具”“分拆旧 hint 三问”。这些候选都不改变 Poker 凶手和答案文本，目标是最小化抽取 `203/204/205` 调查进度。
- 复盘旧 `v50` Yuan 轨迹发现，`704` 的直接内容来自老师对课程展示投票异常的回答；新增 `n516d` 只问 `marks=True` NPC 关于课程展示投票是否异常，命中投票关键词即停止，用于对照 `n515d/e` 的全员基础问定位老师路线。
- 候选鲁棒性审计发现 Poker hint 正则在“牌手张朔/房客林晚舟”这类前缀下可能误切姓名；已将 `n514c-i/n515a-e/n516a-e` 改为先用当前 `npcs` 的中文名精确匹配 hint，再正则兜底，并且只用验证过的 `info_id` 对应中文名提交。
- 出于 Saiblo 对局 API 可能暴露 stderr 的风险，`n514c-i/n515a-e/n516a-e` 已统一 `DEBUG = False`，上传候选默认不输出 hint、marks、问句和阶段日志。
- Yuan 的 `n514f/n515d/n515e/n516d` 已在追加聊天后刷新 `npcs/marks`，再用 `marks=False` 确认最终提交凶手，降低后续 NPC/marks 更新导致的错提交风险。
- `n514c-i/n515a-e/n516a-e/n517a-h` 已通过语法检查。当前它们仅是本地候选，未上传、未开房间。

## 2026-05-07 10:42-10:44 UTC 本地低尾审计

Saiblo 状态：

- `entities --game-id 53` 仍解析为 username `thebeginning`，`active=null`。
- `entity 21072 / n513a` 的两个 code 仍为 `未编译`，因此继续不上传新候选。

本地低尾审计：

- 对 `n511a` 的 19 个有效 room 样本解码 stderr 后，Rose 全部为 `stage=6,calls=41`，Z/F 全部为 `stage=7,calls=42`。
- 因此 `2457/2507/2617/2657/2667/2707` 的分数波动不能简单归因于 Rose 或 Z/F 没推进到最终可见 stage。
- Rose stage5->6 的触发问句中，`喜欢你？` 与部分 `2617/2667` 低档有相关性，但不是充分条件；`2457/2507` 也可由“态度怪？”触发，说明仅靠 Rose stage6 问句替换不能解释全部低尾。
- 这支持当前优先级：恢复后先测安全版 Poker stage2 与 stage2 后接待者/证据候选，再把 Yuan `704` 放 P3；Rose/Z 只保留为子任务复核方向。

## 2026-05-07 10:50-10:53 UTC Poker 组合缺口补齐

- Saiblo 单人房间仍未恢复，`n511a_recover_probe_1048` 创建的 `room 918471` 在 `begin_match` 返回 500，因此本轮未上传新候选。
- 本地审计发现：`n512a/n514e` 的“案发现场情况，以及你手中的证据”问法在 3 个有效历史样本中 3/3 触发 Poker stage2，但此前接待者追问候选 `n515/n516` 都从 `n511a/n514d` 问法派生。
- 新增 `n517a/b/c/d` 只补齐这个组合缺口：保留 `n514e` 信息源问法，分别叠加接待者合并 hint 单问、接待者异常关键词问、条件短动机、接待者分拆旧 hint 三问。
- 这些候选继续不改 Rose/Z/Yuan 主路径，`DEBUG = False`，实体名仍保持中性编号，恢复后才按单人房间评测。

## 2026-05-07 10:55 UTC Poker 最小异常问与手法候选

- Poker 子任务复核结论：`203/204/205` 的最小解锁点更可能是接待者回答“公馆内异常发现”，其中包含房间电脑浏览记录、方形塑料盒、厨房少三把刀；“聊天记录”“到达时间表”“证词破绽”不是最小前置，先降级。
- 新增 `n517e` 精确问三证据，去掉“异常发现”外延；新增 `n517f` 问“电脑/冰柜/刀具异常”，减少直接灌入“塑料盒/少三把刀”的答案式措辞；新增 `n517g` 在 `n517e` 基础上测试手法文本“冰块固定刀具自杀并伪装他杀”；新增 `n517h` 不问接待者，仅在 `n514e` stage2 成功后替换手法文本，用于区分答案文本收益和调查进度收益。
- `n517g` 是高风险答案文本候选：它可能提高 Poker method 分，也可能因凶手字段仍未确定而掉分；恢复后应先小样本，不优先扩样。

## 2026-05-07 11:10 UTC evidence-refresh 候选

Saiblo 状态：

- `2026-05-07 11:03 UTC` 单人房间恢复探针仍失败：`room 918483` 表现为 `join` 500 但已坐入，`begin_match` 500。
- `2026-05-07 11:09 UTC` 非开赛检查仍未恢复：`entity 21072 / n513a` 两个 code、`21073 / n514a`、`21074 / n514b` 均为 `未编译`。
- `2026-05-07 11:10 UTC` `entities --game-id 53` 仍返回 username `thebeginning`，`active=null`。
- 当前继续不上传新候选、不上天梯、不使用 batch；只做本地准备与文档同步。

本地理论上限扩展：

- Poker 子任务与后案结果子任务均确认：线上下载只可靠暴露 Rose 的正式 `answer_result`，Poker/Yuan 的最终三项 true/false 仍不可直接观测。
- 旧 `v52` decoded records 显示，Poker `102/103/104` 是信息源两问到 stage2 后通过 `others()` 才可见；`203/204/205` 是接待者回答异常发现后再次 `others()` 才可见。当前高分候选多数在聊天后直接 answer，可能没有把已满足的证据刷新成可计分调查进度。
- 新增 `n518a/b/c/d` 作为低扰动 `others()` 刷新候选：`n518a` = `n514d` + stage2 后刷新证据，`n518b` = `n514e` + stage2 后刷新证据，`n518c` = `n517a` + stage2/接待者后各刷新一次证据，`n518d` = `n517e` + stage2/三证据问后各刷新一次证据。
- 这组候选不改变 Rose/Z/Yuan 主路径，不新增 `n518a/b` 的聊天，也不改 Poker 答案文本；目标是隔离“显式取证据”是否能把 `102/103/104` 或 `203/204/205` 转化为净分。
- `n518a/b/c/d` 均 `DEBUG = False`，并通过 `python3 -m py_compile`。恢复后应在 `n514d/e` 后优先小样本测试 `n518a/b`；若不退化，再测 `n518c/d`。

## 2026-05-07 11:16-11:25 UTC 平台探针与低尾差分补充

Saiblo 状态：

- `2026-05-07 11:16 UTC` 再测 `n511a` 恢复探针，`room 918519` 仍表现为 `join` 500 但 code 已坐入，`begin_match` 500。
- `2026-05-07 11:22 UTC` 换旧稳定 `n506e` code_id `a63c0344959c4189beba83019bdc4c2b` 再测，`room 918537` 同样 `join` 500 后已坐入，`begin_match` 500。
- 因此当前阻塞不是 `n511a` 代码特异问题，而是 Game53 单人房间开赛链路级问题。继续不上传新候选、不跑 batch、不上天梯。
- 已新增低频恢复 watcher：`Game2/tools/watch_saiblo_recovery.py`。默认 15 分钟检查一次，历史 `未编译` code 只记录日志，不作为默认恢复阻塞；真正恢复信号是单人房间 `begin_match` 成功。恢复后可通过 `--callback` 自动启动下一步评测。

本地低尾差分补充：

- 合并 `n511a/n512a/n512d` 当前有效房间样本后，Poker stage2 基本表现为一个约 `+50` 的叠加层：低基底 `2457/2617/2657` 对应 stage2 后常见 `2507/2667/2707`。
- 这说明 `2707` 不是全局上限，只是“旧高基底 + Poker stage2”的当前最高组合；低尾根因仍在 stage2 之外。
- `n512a` 的 `请说说案发现场情况，以及你手中的证据。` 问法在 3 个样本中 3/3 到 Poker stage2，但仍有两个 `2507`，所以它提高 stage2 触发率，不等于解决低尾。
- `n511a` 的 19 局中 Z/F 均有两次 `Internal Server Error: '8'` 隐藏 probe，说明低尾不是隐藏 probe 完全失效。
- Rose 可见提交除 match `8079935` 外基本三项全对；`8079935` 的 Rose motivation false 可以解释一个 `2507`，但不能解释其他 `2457/2617/2507`。
- Rose stage5->6 由 `某某喜欢你？` 触发时常落到 `2617/2667`，由 `某某态度怪？` 触发时也可能落到 `2457/2507/2657/2707`。因此它是弱相关信号，不适合作为下一轮主线；此前 `n508u/v/w/n509/n510` 系列也未消除低尾。
- 更细分数档解释：`2707` 通常是 Rose TTT、Z stage7+两次 hidden probe、Poker stage2、Yuan stage1 直接答；`2667` 通常仍有 Poker stage2，但 Rose stage5->6 由 `喜欢你？` 触发，形成约 `-40`；`2617` 常见为 Poker 未 stage2 再叠加这个 `-40`；`2507` 有两类，一类是 Rose motivation false，另一类是 `n512a` 的隐藏后案波动；`2457` 不是 Yuan 703/704 缺失能解释，因为相关样本 Yuan 均 stage1 且无额外聊天。
- 已新增只读差分工具 `Game2/tools/analyze_room_score_factors.py`，输出到被忽略的 `docs/generated/game2_room_score_factors.{md,json}`，用于后续自动核对 score、Rose answer、Rose stage6 问句、Poker stage 和隐藏 probe 之间的关系。
- `n512a` 三个样本的 Poker 回复差异值得保留：两个 `2507` 虽然都到 stage2，但第一条信息源回复分别是“线索指向人口贩卖集团”和“证据？时机未到”；唯一 `2707` 第一条回复明确出现“聊天记录有问题”。这支持后续接待者/聊天记录/证据刷新方向，但当前不改 `n514e/n518b`，先保留最低扰动 A/B。
- 结论维持：恢复后优先验证 `n514d/e` 安全对照与 `n518a/b` evidence-refresh，再测接待者路线；Rose/Z 大改继续降优先级。

evidence-refresh 复核：

- 子任务复核确认：`g.evidences()` 实际调用 `others()`，旧 `v52` 在 Poker 信息源两问后显式 `others()` 才看到 `102/103/104`，接待者回答后再次 `others()` 才看到 `203/204/205`。
- 当前 `n511a/n512a/n512d` 高分房间样本到 stage2 后直接 answer，没有 stage2 后刷新证据，因此没有严格 A/B 证明这些证据已被计入 raw score。
- 置信度判断：`others()` 能刷新可见证据为高置信；刷新是否独立加分为中等置信，必须等单人房间恢复后用 `n518a/b/c/d` 验证。

## 2026-05-07 11:45 UTC 理论上限与鲁棒性补充

分数进度：

- 只读挖掘汇总 300 个有效单人房样本，当前最高仍为 `2707`，共 9 次，全部满足 Poker stage2、Rose 三项全对、Rose stage6 由“态度怪？”类问句触发、Z/F 高阶段。
- `>=2667` 的 13 个样本也全部具备 Poker stage2 与 Rose 三项全对；`2667` 相比 `2707` 的主要可见差异是 Rose stage6 由“喜欢你？”类问句触发。
- 低尾 `<=2457` 多数没有 Poker stage2，且更常见 Rose motivation false 或 Z/F 未进入高阶段。`n511a` 自身仍有 `2457/2507` 尾部，因此不能只用单局 `2707` 定版。
- Yuan stage1 在现有房间样本里不是冲顶必要条件，所有 `2707` 都没有依赖 Yuan 703/704；袁案仍有理论空间，但当前历史证据显示它更适合 P3 极省聊天探针。

理论上限判断：

- SDK 名义分数包含每案答案、调查进度、成就与效率，四个以上剧本的纯理论空间显然高于当前 `2707`；`2707` 只是“旧高基底 + Poker stage2”的最高观测组合，不是全局理论上限。
- 当前最可信的可挖空间仍是 Poker stage2 后的 `102/103/104` evidence-refresh、接待者最小问句对应的 `203/204/205`，以及少量 Yuan `704` 净收益验证。
- Rose 与 Z/F 有理论空间，但历史上 Rose 大量微调未消除低尾；Z/F stage8 边界表现为平台 `KeyError('8')`，不适合作为恢复后的第一主线。

新增鲁棒候选：

- 新增 `n519a/b`，分别从 `n518a/b` 派生，只改更新后的鲁棒性，不改主路径答案文本。
- 两个候选把主循环从固定 `6` 案改为最多 `12` 案，避免新增剧本直接漏答。
- Poker 信息源解析改为先按上下文正则匹配“好的信息来源/问问/接待者知道”等结构，只有唯一 NPC 中文名命中时才回退，避免 hint 中出现多个姓名时问错人。
- 未知新增案兜底优先使用 `marks=False` NPC，而不是默认 `npcs[0]`。
- `n519a/b` 均关闭 stderr 调试并通过语法检查；恢复后和 `n518a/b` 相邻小样本评测。

平台状态：

- `2026-05-07 11:43 UTC` 非开赛检查仍显示 `entity 21072 / n513a` 两个 code 为 `未编译`；当前 token 仍解析为 `thebeginning`。
- `2026-05-07 11:48 UTC` 低频 watcher 的房间探针仍失败；`POST /api/rooms/` 创建房间返回 500，平台仍未恢复。
- `Game2/tools/run_recovery_eval_queue.py` dry-run 已验证默认队列 `n514d n514e n518a n518b n519a n519b` 只做中性上传和单人房评测，不用 batch，不带 `--activate`。

## 2026-05-08 02:46 UTC stage3 与保守兜底复测

Saiblo 状态：

- 当前 token 复核为 `thebeginning / user_id=2646`；本轮上传均使用中性实体名 `n520c-f/n521a-d` 与备注 `r`，未使用 batch，未上天梯。
- 单人房间评测链路已恢复，直接用 `Game2/tools/run_room_eval.py` 或 `run_recovery_eval_queue.py --expected-username thebeginning` 评测。

当前稳定基线：

- `n518b`，code_id `ccab056263c540c4b61c1fec84828ac1`，15 个有效房间样本为 `2707 x10, 2667 x3, 2657 x2`，min `2657`、avg `2692.333`、max `2707`。
- 暂不把任何 `n520/n521` 候选视为稳定替代；`n518b` 仍是低尾最可信版本。

stage3 高上限复测：

- `n520c`，code_id `9af44540f41f46d0ae3080deed7b2964`，25 样本汇总为 `2757 x15, 2717 x6, 2707 x1, 2657 x2, 2517 x1`，avg `2727.8`，但存在 `2517`，不能定版。
- `n520e`，code_id `54f3a9ca9c7e4102a9b3f85deb5a3ae5`，25 样本汇总为 `2757 x9, 2717 x8, 2707 x2, 2667 x2, 2657 x3, 2517 x1`，avg `2711.4`，`人口贩卖` 首答过滤不能消除 `2517`。
- `n520f`，code_id `e3b2e0f78dd84dbdb077f1142d84ce7a`，5 样本为 `2757 x1, 2717 x1, 2707 x1, 2517 x2`，丢弃。
- 子任务对 `n518c/n520c/n520e/n520f` 的 49 个 Poker stage3 样本做只读差分：`2517/2717/2757` 在 Poker stage、最终 evidence ids、Rose 三项、Z err8、Yuan stage 上均同形；doctor mention、信息源/接待者 NPC、首答文本也没有可靠硬过滤条件。
- 当前判断：Poker stage3 能把上限抬到 `2757`，但隐藏低尾不可预测，不能作为稳定主线。

stage3 答案文本对照：

- `n521c` 从 `n520c` 派生，stage3 后把 Poker 嫌疑人切到接待者，5 样本为 `2557 x4, 2707 x1`，丢弃。
- `n521d` 从 `n520c` 派生，保留信息源嫌疑人但为 stage2/3 补通用动机和手法，已完成 4 样本均为 `2557`，提前停止并丢弃。
- 结论：当前 Poker 提交里的 `motivation='未知'` 和信息源嫌疑人虽然看起来粗糙，但改成接待者或补强答案文本会明显负收益；低尾不应继续从 stage3 答案文本硬改入手。

保守 stage1 兜底：

- `n521a` 从 `n518b` 派生，只在 Poker 两问信息源后仍停 stage1 时追加“死者面具、手机聊天记录和随身物品分别说明什么？”，不追接待者。队列 8 样本为 `2707 x7, 2657 x1`，但扩样又出现 `2507`；总 12 样本为 `2707 x10, 2657 x1, 2507 x1`，不优于 `n518b`。
- `n521b` 从 `n518b` 派生，stage1 兜底改为“交出手机聊天记录、现场照片和邀请函”。8 样本为 `2707 x4, 2667 x2, 2657 x1, 2457 x1`，丢弃。
- 结论：stage1 兜底问不能稳定减少低尾，且可能引入 `2457/2507`；`n518b` 的“两问信息源 + stage2 后 others 刷新”仍是更稳的保守路线。

当前方法与疑点：

- 当前得分结构可粗分为：保守 Poker stage2 稳定上限 `2707`；Poker stage3 高上限 `2757` 但有不可预测 `2517`；改 Poker stage3 答案会稳定降到 `2557`。
- `n520d` 只有 5 样本且依赖 stage3 方向，虽然 min `2657`、avg `2729`，但在 `n520c/n520e` 扩样暴露低尾后不应仅凭 5 样本晋级。
- Yuan 仍未在本轮提供净收益证据；现有高分样本均不依赖 `703/704`。
- 下一步若继续迭代，应优先寻找 stage3 隐藏评分来源或更低风险的 Poker stage2 问法，不建议继续盲改 Poker 答案文本。

## 2026-05-08 03:09 UTC 门控候选与稳定基线扩样

本轮继续遵守 Game53 约束：只用单人房间，不使用 batch，不上天梯；上传实体名为中性编号 `n522a/b`，备注仍为 `r`。当前 token 仍为 `thebeginning / user_id=2646`。

稳定基线扩样：

- `n518b`，code_id `ccab056263c540c4b61c1fec84828ac1`，新增 10 局为 `2707 x6, 2667 x4`。
- `n518b` 累计 25 个有效单人房间样本：`2707 x16, 2667 x7, 2657 x2`，min `2657`、avg `2691.8`、max `2707`。
- 当前仍将 `n518b` 视为稳定版本；它没有突破 `2707`，但低尾显著优于所有 stage3 候选。

被排除的候选：

- `n516d`，code_id `aa22b5536a0948149e5234d0a308f3b4`，5 样本 `2707 x1, 2657 x3, 2617 x1`。Yuan 省聊天 704 探针无净收益，丢弃。
- `n516e`，code_id `c26636518d4b4b11aa3989f0fa2d56e5`，5 样本 `2707 x1, 2657 x2, 2617 x1, 2507 x1`。Yuan 第二个 marks-true 假设不稳，丢弃。
- `n520d`，code_id `b154778af028437db08e3ea63e2bc3a0`，原 5 样本看似 `2757 x3, 2717 x1, 2657 x1`；扩样后补到 7 样本为 `2757 x3, 2717 x2, 2657 x1, 2557 x1`。医生追问不能消除 stage3 低尾，丢弃。
- `n522a`，code_id `f7be33f642b54dee804303f8b5a2412b`，从 `n520c` 派生，首答含“时机未到/时机成熟/不能全说/不能给你”时不追接待者；2 样本 `2707, 2617`，提前停止。
- `n522b`，code_id `ca8366eccd9f40738061e3293f85c588`，在 `n522a` 基础上也过滤“人口贩卖”；7 样本 `2757 x2, 2717 x2, 2707 x1, 2657 x1, 2617 x1`，提前停止。

新增判断：

- “首答门控”可以避免一部分 stage3 `2517`，但不能解决 Poker stage1 对应的 `2617` 低尾；因此仍不能替代 `n518b`。
- stage3 高均值候选的可见特征仍不足以稳定区分 `2757/2717/2517/2557`。`n520d/n522b` 再次证明小样本高均值不能晋级。
- 目前最可靠结论是：稳定版用 `n518b`；高分探索保留为研究方向，但不作为稳定提交或主线版本。

## 2026-05-08 07:11 UTC Poker stage3 与 Rose 顺序复测

本轮继续只测 Game53 单人房间，不使用 batch，不上天梯；上传实体名保持中性 `n525/n527*`，备注均为 `r`。账号守卫复核为 `thebeginning / user_id=2646`。

已重新生成：

- `docs/generated/game2_room_eval_summary.md/json`
- `docs/generated/game2_room_score_factors.md/json`
- `docs/generated/game2_story_unlocks.md/json`

高均值候选结果：

- `n525a`，code_id `f36fb75c44654b7887f0f37ff6c34403`，合计 20 有效局：`2757 x9, 2717 x7, 2707 x1, 2667 x1, 2657 x1, 2557 x1`，min `2557`、avg `2721.0`。医生追问版本仍有低尾，不能稳定。
- `n525b`，code_id `15404aac42044cdfb6a3114ab5d84fb4`，合计 25 有效局：`2757 x20, 2717 x4, 2517 x1`，min `2517`、avg `2741.0`。身份关系追问均值高，但低尾复现。
- `n527a`，code_id `9a47a67beb2b47ccabe46c0a62ad6cfc`，5 局：`2757 x4, 2717 x1`。只补接待者两句最小追问，初筛很好，但样本不足。
- `n527e`，code_id `99bb4a1ce37a4a12b1f94634ee669ea3`，合计 30 有效局：`2757 x22, 2717 x6, 2657 x1, 2517 x1`，min `2517`、avg `2737.667`。20 局时曾保持 `>=2717`，但扩样复现 `2517`，不能定版。

被排除的候选：

- `n527b`，code_id `52c501b38fc748fbbe2cab0c0bf99a56`，5 局：`2757 x2, 2717 x2, 2617 x1`。只做医生兜底，不如接待者兜底。
- `n527c`，code_id `f52d152183a848c99a3912506bcc6683`，5 局：`2757 x3, 2657 x1, 2517 x1`。接待者兜底叠加医生枚举仍有低尾。
- `n527d`，code_id `79da0135a860420ba31ae60729944c4d`，5 局：`2757 x1, 2717 x1, 2667 x1, 2657 x1, 2517 x1`。只加 Rose 态度变体无效。
- `n527f`，code_id `d0797dac119646319234b3a3226114ea`，有效 3 局 `2757 x2, 2717 x1`，另有一局 `score=0/end_state=None`。新增 Rose 追问过多，存在交互预算风险，丢弃。
- `n527g`，code_id `2250749aff154226bf1b51e889019ce6`，5 局：`2757 x4, 2657 x1`。Rose 问句重排能多次触发 `2757`，但不能避免 Poker stage1 低尾。
- `n527h`，code_id `6c8c83b3b02e4a4ea75ab14438fa59e7`，5 局：`2757 x2, 2717 x2, 2517 x1`。stage1 信息源兜底不能解决 stage3 隐藏低尾。

本轮归因：

- Poker stage3 仍是冲 `2757` 的必要上限条件；`n527e` 的 30 局中 29 局 Poker stage3，但仍出现 `2517`。
- `2657` 仍可由 Poker stage1 解释；例 `n527e` match `8087794` 两问信息源后只见 `001/002`。
- `2517` 可在 Rose 三项全真、Z 双 `KeyError('8')`、Poker stage3 且全证据、Yuan stage1 直接答的同形路径中出现；例 `n527e` match `8087940`。当前没有可用可见特征过滤。
- Rose stage6 由“喜欢你？”触发时常见 `2717`，由“态度怪？”触发更常见 `2757`，但这只是 40 分项，不能解释或消除 `2517`。
- Yuan 704/703 仍没有净收益证据；`n527h` 的额外兜底再次说明“加问”会带来低尾风险。

当前稳定结论：

- 稳定版仍是 `n518b`，code_id `ccab056263c540c4b61c1fec84828ac1`，25 有效局：`2707 x16, 2667 x7, 2657 x2`，min `2657`、avg `2691.8`。
- 若只看均值，`n527e` 更高；若按既定第一指标“最低分优先”，`n527e` 因 `2517` 不能替代 `n518b`。
- 后续若继续探索，应避免继续改 Poker 最终答案文本；最有价值但仍未解决的是“stage3 同形低尾”的隐藏来源。没有新强假设时，不建议继续大量消耗房间在 stage3 同类变体上。

## 2026-05-08 07:40 UTC Rose like 替换与扩样复核

本轮继续只测 Game53 单人房间，不使用 batch，不上天梯；新增上传实体名为中性 `n528a/b`，备注为 `r`。当前 token 复核仍为 `thebeginning / user_id=2646`。

已重新生成：

- `docs/generated/game2_room_eval_summary.md/json`
- `docs/generated/game2_room_score_factors.md/json`
- `docs/generated/game2_story_unlocks.md/json`

扩样结论：

- `n527a`，code_id `9a47a67beb2b47ccabe46c0a62ad6cfc`，追加 4 个有效样本后累计 9 局为 `2757 x6, 2717 x2, 2557 x1`，min `2557`。小样本高分被证伪，已停止剩余扩样。
- `n527g`，code_id `2250749aff154226bf1b51e889019ce6`，追加 7 个有效样本后累计 12 局为 `2757 x9, 2717 x1, 2657 x1, 2557 x1`，min `2557`。Rose 问句重排仍不能过滤 stage3 同形低尾。
- `n528a`，code_id `fce3009bd9cb49d5b948917792527416`，从 `n527g` 派生，删除 Rose 段两个 like 型问句，5 局为 `2717 x4, 2627 x1`。即使 Rose stage6 由“态度怪？”触发，也会稳定少 40 分；若不触发 stage6，则进一步掉到 `2627`，丢弃。
- `n528b`，code_id `947a10e4f53143afac7fa668e8c58177`，从 `n527g` 派生，把“喜欢你？”替换成“是不是私下在一起？”，5 局为 `2757 x3, 2657 x1, 2557 x1`。非 like 文本可复现 `2757`，但同样会出现 `2557`，丢弃。

新增归因：

- `n528a` 证明不能简单删掉 like 型问句；这些问句即使带来 `2717` 风险，也参与 Rose 调查进度或效率组合，删除后高分上限会被压到 `2717`。
- `n528b` 证明把 like 触发文本改成关系问句不能解决核心低尾；`2557` 仍可在 Rose 三项全真、Z 双 `KeyError('8')`、Poker stage3 的可见同形条件下出现。
- 当前所有 stage3 高均值路线都已在扩样中出现 `2557/2517`；新增 Rose 变体没有提供稳定优势。

当前稳定结论不变：

- 稳定版仍是 `n518b`，code_id `ccab056263c540c4b61c1fec84828ac1`，25 有效局 `2707 x16, 2667 x7, 2657 x2`，min `2657`、avg `2691.8`。
- 高分研究方向仍是解释 Poker stage3 同形低尾，而不是继续盲改 Poker 答案文本、Yuan 探针或 Rose like 替换。

## 2026-05-08 09:00 UTC n529-n531 扩样复核

本轮继续只测 Game53 单人房间，不使用 batch，不上天梯；新增和扩样实体名均为中性编号，备注为 `r`。当前 token 仍由账号守卫核验为 `thebeginning / user_id=2646`。

已重新生成：

- `docs/generated/game2_room_eval_summary.md/json`
- `docs/generated/game2_room_score_factors.md/json`
- `docs/generated/game2_story_unlocks.md/json`

新增候选与扩样结果：

- `n529a`，code_id `b93415861eb8457d933150ac5396dddc`，5 局为 `2557 x3, 2617 x1, 2757 x1`。Rose 末段重排不能消除 stage3 隐藏低尾，丢弃。
- `n529b`，code_id `97dd8a7454534ab4aa62f0527e4d970a`，前 15 局一度为 `2757 x8, 2717 x6, 2657 x1`，但继续扩样到第 18 个有效样本时出现 `2457`；累计 18 局为 `2757 x10, 2717 x6, 2657 x1, 2457 x1`，丢弃。
- `n525c` 补扩后累计 12 局为 `2757 x6, 2717 x3, 2707 x2, 2617 x1`。Yuan 嫌疑人证据追问不能稳定抬分，丢弃。
- `n530a`，code_id `44452e96bdf14dff8ce1a40d996f307d`，从 `n525c` 派生，只在 Poker 接待者后仍 `stage<3` 时补问 Joker 聊天记录/到达表；累计 9 局为 `2757 x7, 2717 x1, 2517 x1`，丢弃。
- `n530b`，code_id `72ebafeb7fbd4ae59b7627d8c9cdaa29`，从 `n518b` 派生，仅叠加 Yuan 嫌疑人证据追问和鲁棒解析；3 局已出现 `2467, 2507, 2667`，提前停止。
- `n530c`，code_id `d2496ac3333a4d90bb8d72b341f5bdce`，是 `n530a` 的上下文优先信息源解析和 `range(12)` 鲁棒版；5 局为 `2757 x3, 2657 x1, 2617 x1`。低尾来自 Poker stage1 随机首答未进入接待者路线，丢弃。
- `n531a`，code_id `fd0e8e0b24f944d491ded1b1663e3915`，在 `n530c` 基础上对 Poker stage1 补问手机/随身物品/聊天记录/密码破绽；5 局为 `2757 x2, 2717 x2, 2557 x1`，丢弃。
- `n531b`，code_id `5eaa1d3cb95f4f939cee6c668a2d533f`，在 `n530c` 基础上对 Poker stage1 补问林渝植/梅花5/死者真实身份关系；初筛 5 局全 `2757`，但扩样后累计 11 局为 `2757 x9, 2717 x1, 2557 x1`，丢弃。

新增归因：

- `n531b` 和 `n529b` 都出现了长前缀高分后突然低尾，说明 `5-15` 局无低尾仍不足以证明 stage3 路线稳定。
- `n529b` 的 `2457` 是 Poker stage1 样本，说明 Rose 重排加 stage1 兜底仍可能完全失效；`n530c` 的 `2617/2657` 也来自同类 Poker stage1。
- `n530a/n531a/n531b` 的 `2517/2557` 都发生在 Poker stage3、Z 双 `KeyError('8')`、Rose 可见三项全对的同形条件下，继续证明核心低尾不是缺少 Poker `201-205` 证据或 Yuan stage1。
- Yuan 嫌疑人证据追问在 `n525c/n530b/n531*` 中没有形成稳定正收益；在保守 stage2 路线中还会引入 `2467/2507` 低尾。

当前稳定结论：

- 稳定版仍是 `n518b`，code_id `ccab056263c540c4b61c1fec84828ac1`，25 有效局 `2707 x16, 2667 x7, 2657 x2`，min `2657`、avg `2691.8`。
- 当前最高观测分仍是 `2757`；没有任何候选稳定超过 `n518b` 的最低分。`n529b/n531b` 均值高，但已经复现低尾，不能作为稳定提交。
- 除非找到新的可观测过滤特征，否则不建议继续在“Poker stage3 + 额外兜底问 + Yuan stage1”同类结构上扩样；下一步如果继续探索，应换到新的隐藏低尾解释假设。

## 2026-05-08 10:48 UTC n532-n537 评测回填

本轮继续只用 Game53 单人房间评测，不使用 batch，不上天梯；上传实体名 `n532a/b/c/n533a/n534a/n535a/n536a/n537a/b/c/d` 与备注 `r` 均保持中性，账号守卫为 `thebeginning / user_id=2646`。

新增候选与扩样结果：

- `n532a`，code_id `72bb08a99f2c4c5ab865eb8ed80ef72b`，5 局 `2707 x2, 2667 x2, 2657 x1`。只改 Rose 明确假冒兜底，基本等同 `n518b` 小样本，未见收益。
- `n532b`，code_id `e97f3c72a3d14b9e8e4f05b2f2fa629b`，5 局 `2757 x2, 2717 x1, 2557 x2`，丢弃。
- `n532c`，code_id `64681042e873470684a03a737ce682f4`，5 局 `2757 x3, 2717 x1, 2557 x1`，丢弃。
- `n533a`，code_id `65864a8c337046e99822b1540d1d96d2`，累计 9 局 `2757 x5, 2717 x3, 2617 x1`；低尾来自 Poker stage1 卡住，丢弃。
- `n534a`，code_id `b6e65d342e00413b8a725fc46aaedb31`，累计 20 局 `2757 x10, 2717 x6, 2657 x2, 2557 x2`；stage3 占优但仍有 `2557` 隐藏低尾，不能作为稳定版。
- `n535a`，code_id `7d5b7fa5bf404b0abecb7f2a1c040d6f`，5 局 `2757 x2, 2717 x1, 2557 x1, 2517 x1`；移除 stage3 后关系追问反而更差，丢弃。
- `n536a`，code_id `9f0955053f1344d7b656187c39b347e8`，5 局 `2707 x2, 2667 x2, 2507 x1`；Rose 动机与顺序改动不安全，丢弃。
- `n537a`，code_id `3f1894a51daa4337b494476a93d9f640`，累计 10 有效局 `2707 x6, 2667 x3, 2617 x1`。显式“不是本人/冒充”兜底不能稳定提升 Rose，出现 `2617` 后停止扩样。
- `n537b`，code_id `11ba3fd0bdf247238e1dadf3b4c3c399`，5 局 `2707 x2, 2667 x1, 2657 x1, 2257 x1`。只后置 like 问句会引入严重低尾，丢弃。
- `n537c`，code_id `36b8cfc5ecb3497cb314b8fb175e1062`，5 局 `2557 x2, 2517 x3`；`n537d`，code_id `e4e239b7813645788ae7599aabcde97e`，5 局 `2557 x2, 2517 x2, 2357 x1`。Poker `203/204/205` 虽指向自杀伪装剧情，但把最终手法或凶手字段改向自杀均为明显负收益。

新增归因：

- `n534a` 的 `2557` 可在 Rose 三项全真、Poker stage3 全证据、Z 双 `KeyError('8')` 的可见同形条件下发生。
- `n537c/d` 说明 Poker 最终答案字段很可能不按剧情自杀文本计分，当前高分仍更依赖调查进度而不是正确解释 `203/204/205`。

当前稳定结论不变：`n518b` 仍是唯一经过 25 局且最低分 `>=2657` 的版本。后续若继续探索，应优先寻找新的可观测低尾过滤特征；没有新特征时，不建议继续扩 stage3 答案文本或 Rose 尾段微调。

## 2026-05-08 12:48 UTC n538-n540 评测回填

本轮继续只用 Game53 单人房间评测，不使用 batch，不上天梯；上传实体名 `n538a/b/c/n539a/b/c/d/n540a` 与备注 `r` 均保持中性，账号守卫为 `thebeginning / user_id=2646`。

新增候选与扩样结果：

- `n538a`，code_id `3560b9409de24a108a032ed15b76cc97`，从 `n534a` 派生，只把 Poker 信息源解析改回上下文优先，5 局 `2757 x2, 2717 x1, 2657 x1, 2557 x1`，丢弃。
- `n538b`，code_id `aeb9674e0abf4052807157af57fbd99f`，从 `n534a` 派生，只加 Rose stage6 前的“假冒”兜底。首 5 局 `2757 x3, 2717 x2`，但扩样第 1 局出现 `2517`，丢弃。
- `n538c`，code_id `5f0ce8e49df64a20a3d9eee2ad815cb4`，合并 `n538a/b`，5 局 `2757 x2, 2717 x2, 2617 x1`，丢弃。
- `n539a`，code_id `2b890369c8eb4e168695bf966f4409de`，从 `n525b` 派生，把 Rose 尾段 `喜欢你？` 前移到 `你对 X 好？` 前。首 5 局 `2757 x3, 2717 x2`，扩样到 8 局出现 `2517`，累计 `2757 x5, 2717 x2, 2517 x1`，丢弃。
- `n539b`，code_id `e1d1f4eecbdf4167a5405902fec3878b`，从 `n518b` 派生，只在 Poker 两问后仍 stage1 且回复有拒绝语义时问一次接待者兜底，5 局 `2707 x4, 2657 x1`。基本等同基线小样本，无稳定收益。
- `n539c`，code_id `906c4545e04c4ab3aa6d2bb40972641d`，从 `n538b` 派生，把 Rose 兜底后置为“Rose下台后是谁上台表演？”，并只在 Poker 未达 stage3 时才问关系总括。5 局 `2757 x4, 2717 x1`，但与更小差分 `n539d` 分布相同，未扩样。
- `n539d`，code_id `fb11515cb46243a79491fbbb3dbf2170`，从 `n534a` 派生，仅改为 Poker 已达 stage3 时跳过关系总括问。它是本轮最佳高均值候选，累计 36 局 `2757 x23, 2717 x10, 2657 x2, 2557 x1`，min `2557`、avg `2734.778`。虽然低尾出现较晚，但仍不能作为稳定版。
- `n540a`，code_id `04dd107dc52e459a8ac27b4d7212e0ca`，从 `n525b` 派生，仅跳过已达 Poker stage3 后的关系总括问，10 局 `2757 x4, 2717 x3, 2617 x1, 2557 x1, 2517 x1`，丢弃。

新增归因：

- `n539d` 说明“已达 Poker stage3 后少问一步关系总括”可以显著改善均值并推迟低尾：前 27 个有效样本最低仅 `2657`，一度看起来可能以同最低分高均值替代 `n518b`。
- `n539d` 的 `2557` 样本 `8091812` 仍是可见同形低尾：Rose 三项全真且 stage6 由“态度怪？”触发，Poker stage3 且 `101/102/103/104/201/202/203/204/205` 全证据可见，Z 双 `KeyError('8')`，Yuan 仍 stage1 直接答。当前仍没有可在线过滤的特征。
- `n540a` 证明同样的“跳过总括问”不能简单迁回 `n525b`；它 10 局内同时出现 `2617/2557/2517`，说明 `n539d` 的长前缀高分仍是特定结构下的概率改善，不是稳定解。
- `n538b` 和 `n539c` 进一步证明 Rose “假冒/上台表演”兜底不能解决核心低尾；`n539a` 证明单纯调整 like 问句先后也不能解决 stage3 同形低尾。

当前稳定结论：

- 稳定版仍是 `n518b`，code_id `ccab056263c540c4b61c1fec84828ac1`，25 局 `2707 x16, 2667 x7, 2657 x2`，min `2657`、avg `2691.8`。
- `n539d` 是目前最强的高均值失败样本，证明理论上限仍有空间，但由于已复现 `2557`，不能按最低分优先口径替代稳定版。
- 后续若继续迭代，应优先寻找 stage3 同形低尾的隐藏评分来源；不建议继续在 Poker 最终答案文本、Rose 尾段兜底、Yuan 额外聊天或已达 stage3 后是否总括这些方向上扩同类样本。

## 2026-05-08 14:17 UTC n541-n543 评测回填

本轮继续只用 Game53 单人房间评测，不使用 batch，不上天梯；上传实体名 `n541a/b/c/d/n542a/b/n543a/b` 与备注 `r` 均保持中性，账号守卫为 `thebeginning / user_id=2646`。本轮目标分两类：继续压测 Poker stage3 高分路线的隐藏低尾，以及从稳定基线 `n518b` 出发只救 Poker stage1/stage2 低尾，尝试提高稳定最低分。

新增候选与扩样结果：

- `n541a`，code_id `ed84bfbf04e44b909b23dff74345cf5a`，从 `n539d` 派生，Rose 已达 stage6 后跳过“你对 X 好？”问句。首 5 局 `2757 x4, 2717 x1`，但扩样第 2 局出现 `2617`；累计 8 局 `2757 x4, 2717 x3, 2617 x1`，丢弃。
- `n541b`，code_id `322f1183188d4c85b51bf40ddc30926e`，把上述高风险问句改成“Rose 下台后是谁上台表演？”兜底，5 局 `2757 x3, 2657 x1, 2557 x1`，丢弃。
- `n541c`，code_id `38038949c06340539fd28a096075dc6d`，从 `n539d` 派生，加强 Poker stage1 拒证后的接待者兜底。首 5 局 `2757 x3, 2717 x1, 2657 x1`，扩样后累计 9 局 `2757 x5, 2717 x2, 2657 x1, 2617 x1`，丢弃。
- `n541d`，code_id `1c9be70becca4f0a881a08af69cde3d4`，合并 `n541a` 的 Rose 条件跳过和 `n541c` 的 Poker 拒证兜底，首 5 局 `2757 x4, 2717 x1`。由于它包含已在 `n541a` 扩样中证伪的 Rose 跳过策略，未继续作为稳定候选扩样。
- `n542a`，code_id `46240e41938645158571c29c33e6afbc`，在 Poker 拒证后转回信息源本人，明确索要手机、聊天记录、随身物品和现场证据，5 局 `2757 x2, 2717 x2, 2557 x1`，丢弃。
- `n542b`，code_id `bf3ccfa136f24417a29ca5b03848f721`，把信息源本人索证问提前为 stage1 通用兜底。首 5 局全 `2757`，但扩样第 4 局出现 `2557`；累计 9 局 `2757 x7, 2717 x1, 2557 x1`，丢弃。
- `n543a`，code_id `24e4feb1253d4c26a4ca8e546a3dbacf`，从稳定基线 `n518b` 派生，不追 Poker stage3，只在两问后仍 stage1 时加一条泛化“手机聊天记录/随身物品/到达表/异常发现”问；5 局 `2707 x3, 2667 x1, 2417 x1`，丢弃。
- `n543b`，code_id `d939ed925f784f62b13a1c6370ee0a46`，同样从 `n518b` 派生，但把兜底问收窄为“手中证据/死者手机/聊天记录/邀请函/随身物品/现场照片”。首 5 局 `2707 x3, 2667 x2`，一度看起来可能把最低分抬到 `2667`；扩样第 9 局出现 `2507`，累计 15 局 `2707 x9, 2667 x5, 2507 x1`，丢弃。

新增归因：

- `n541a` 的 `2617` 和 `n541c` 的 `2617` 都说明“少问 Rose 高风险问句”或“换 NPC 接待者兜底”不能稳定过滤 Poker stage1 低尾。
- `n542b` 的 5/5 `2757` 再次证明小样本非常危险；同一候选扩到第 9 个有效样本即出现 `2557`。
- `n543b` 证明保守地只追 Poker stage2 也不安全：它前 13 个样本没有低于 `2667`，但随后出现 `2507`，反而低于 `n518b` 的稳定最低分。
- 低尾的可见形态仍不一致：既有 Poker stage3 全证据同形 `2557`，也有 Poker stage1/stage2 救援失败后的 `2507/2617/2417`。当前没有新的在线判别特征能可靠过滤。

当前稳定结论：

- 稳定版仍是 `n518b`，code_id `ccab056263c540c4b61c1fec84828ac1`，25 局 `2707 x16, 2667 x7, 2657 x2`，min `2657`、avg `2691.8`。
- 本轮已同时证伪高分 stage3 微调、Poker 拒证兜底、信息源本人索证、以及保守 stage2 救援。除非出现新的可观测判别特征，不建议继续在这些同类问法上扩样。

## 2026-05-08 14:39 UTC n544 暂停与瓶颈判断

本轮仍只用 Game53 单人房间评测，不使用 batch，不上天梯；上传实体名 `n544a/b` 与备注 `r` 均保持中性，账号守卫为 `thebeginning / user_id=2646`。本轮原计划从稳定基线 `n518b` 派生五个隔离候选：`n544a/b/c` 验证 Yuan 的低成本刷新和零聊天答案文本，`n544d/e` 验证 Poker stage2 人口贩卖集团风险门控。运行中应用户要求暂停并做瓶颈分析，队列已停止，未继续上传 `n544c/d/e`。

已完成样本：

- `n544a`，code_id `951a5b9f8ac84056b599a3e0d645ae7f`，只在 Yuan 案中问 `marks=True` NPC “课程展示和投票是否异常”，命中投票异常关键词后刷新 `others()` 和 `marks()`。5 局 `2707 x3, 2667 x1, 2467 x1`，出现明显低尾，丢弃。
- `n544b`，code_id `9c1cd4175802461787edffa0c65fd95f`，只在 Yuan 案中问 `marks=True` NPC “你觉得谁在说谎？为什么？”，命中 `49人/48票/笔迹/24:23` 等关键词后刷新。暂停前 4 局 `2707 x2, 2667 x1, 2657 x1`，没有突破基线，也没有足够证据继续扩样。

瓶颈判断：

- 最高观测分 `2757` 已长时间没有突破，且所有 `2757` 路线都依赖 Poker stage3；这说明当前可见上限主要来自 Poker 调查进度，而不是 Rose 或 Z/F。
- 主要问题不是“无法到 `2757`”，而是 `2757` 路线存在不可在线判别的隐藏低尾。同样可见签名下，Rose 三项全真、Z/F 双 `KeyError('8')`、Poker stage3 和 `201-205` 全证据仍能掉到 `2557`。
- 保守路线 `n518b` 稳定但上限只有 `2707`；尝试把 stage1/stage2 低尾救起来会引入 `2417/2507/2467` 等更差尾部，说明额外聊天本身会扰动隐藏评分或错触发后案。
- Yuan 理论上可能还有 `703/704` 组件，但旧探针和最新 `n544a` 都显示低成本 Yuan 聊天没有稳定净收益；当前它更像高风险旁路，而不是主突破口。
- Rose 和 Z/F 目前接近吃满：Rose 高分样本三项答案全真，Z/F 已稳定触发已知 hidden stage8 错误；这些方向再改问句或答案文本，多数只是在移动低尾而非提高上限。

后续建议：

- 若继续追更高分，应先做“计分组件审计”或找到新的可观测低尾特征；没有新特征时，不应继续大规模扩同类 Poker stage3/接待者索证/Yuan 单问/Rose 尾段微调。
- 当前稳定版本仍是 `n518b`，code_id `ccab056263c540c4b61c1fec84828ac1`，25 局 `2707 x16, 2667 x7, 2657 x2`。

## 2026-05-08 16:20 UTC 分数分布与隔离/并发判断

本轮没有上传新版本，没有使用 batch，没有上天梯。曾在本地复制出 `n547a/n547b` 探针目录，但未上传、未评测；当前结论先基于已有单人房间样本和生成统计。

已重新生成：

- `docs/generated/game2_room_eval_summary.md/json`
- `docs/generated/game2_room_score_factors.md/json`

新增样本回填：

- `n545d` 扩样后累计 18 局 `2757 x13, 2717 x3, 2657 x1, 2557 x1`，仍是高均值但不稳定。
- `n546b` 4 局 `2707 x3, 2667 x1`，基本等同稳定基线，无突破。
- `n546c` 4 局 `2757 x3, 2717 x1`，小样本高分但未证明稳定。
- `n546d` 含被中断扩测的 5 个有效样本，累计 9 局 `2757 x7, 2717 x2`，尚未出现低于 `2717`，但样本不足，不能定版。
- `n546e` 4 局全 `2757`，样本太小，且包含 Yuan 额外聊天，风险高于最小差分。

当前 1114 个有效单人房间样本的主分布：

- 全局最高仍为 `2757`，未观察到 `>2757`。
- `Poker stage=None/1` 在 Rose 全真、Z 双 `KeyError('8')` 条件下最高 `2657`，分布为 `2417/2457/2487/2527/2617/2657`。
- `Poker stage2` 在同条件下最高 `2707`，分布为 `2467 x4, 2507 x16, 2667 x48, 2707 x127`。
- `Poker stage3` 在同条件下最高 `2757`，分布为 `2517 x22, 2557 x44, 2627 x1, 2717 x136, 2757 x326`。
- Rose motivation false 的高阶段样本通常比 Rose 全真少约 `200` 分；例如 Poker stage3 下 Rose 全真最高 `2757`，Rose false 最高 `2557`。
- Z/F 隐藏双问的可见收益约为 `+137`：无 Poker、Rose 同类触发时，`z_err8=0` 的高点 `2520/2480` 对应 `z_err8=2` 的高点 `2657/2617`。
- Rose stage6 由 `喜欢你？` 类问句触发时常比 `态度怪？` 类低约 `40` 分；这解释旧 `2480/2520` 与 `2617/2657` 档位的一部分。
- Yuan 当前只见 `stage1`，`703/704` 样本很少；在同 Poker/Rose/Z 条件下，Yuan `704` 没有证明稳定正收益。`('001','704')` 在 Poker stage2 下 4 样本全 `2707`，但无超过当前 stage2 上限；`('001','703','704')` 在 Poker stage3 下 4 样本全 `2757`，样本不足且无法排除与 Poker stage3 同时发生的幸存者偏差。

核心瓶颈：

- 目前最强的可见签名是：Rose 三项全真、Rose stage6=`other`、Z/F 双 `KeyError('8')`、Poker stage3、Poker evidence `001/002/101/102/103/104/201/202/203/204/205`、Yuan 只有 `001`。
- 这个完全相同的签名共有 464 个样本，分布为 `2517 x21, 2557 x41, 2627 x1, 2717 x122, 2757 x279`，跨度 `240`。
- `sample_count`、`decoded_stdin_records` 数、Poker 回复关键词如 `Joker/聊天记录/电脑/塑料盒/冰柜/刀具/医生/尸体` 都不能可靠区分 `2517/2557/2717/2757`。
- 因此当前低尾不是简单的“没拿到 201-205 证据”或“Yuan 没推进”，而是隐藏答案判定、隐藏进度、效率系数或平台内部状态里至少有一个不可见变量。

关于“快速放弃某些剧本再专注某一剧本”：

- 机制上可行。已有低分样本证明前案低分或错误后仍会继续进入后续案，例如 `2080/2217/1457/907/584` 样本都能看到 Rose、Z/F、Poker、Yuan 多案 hint。
- 但不建议把“故意得 0 分”作为生产评测。错答会引入大约 `200+` 分的固定损失，并放大总分噪声；同时总分仍受效率系数和隐藏变量影响。
- 更合理的隔离方式是：非目标剧本使用最短直接答案，尽量保持答案正确但不做调查；目标剧本才做变量实验。这样能降低非目标聊天噪声，但仍不能把隔离绝对分数直接换算回完整主线。
- 隔离实验只适合同一骨架内 A/B：例如 Poker-only A 与 Poker-only B 比较，Yuan-only A 与 Yuan-only B 比较。不能拿 Poker-only 的绝对分数直接判断完整版本会不会稳定。

关于并发探索：

- 只读分析可以并发，适合分别让子任务分析 Poker/Yuan 的证据和候选设计。
- Saiblo 房间评测不应在当前阶段盲目并发。现有 `run_room_eval.py` 和 `run_recovery_eval_queue.py` 都是串行；手工多进程并发虽然技术上可行，但会让服务器 pending、编译延迟、账号限流和样本归因混在一起。
- 若要并发，前提是先固定隔离骨架，并把每条线写入不同中性实体、不同输出目录、不同 code_id；每条线至少先跑 3-5 个 sanity 样本确认没有结构性失败，再扩到 16+。否则并发只会更快地产生不可解释样本。
- 当时不建议马上拆成 Poker/Yuan 两条并行评测线。后续 `n547/n548` 置零与组合补跑已完成 control 口径验证，因此最新结论改为“支持受控拆线，但必须固定骨架并只在同一骨架内做 A/B”。

后续决策门槛：

- Poker-only 只有在同一隔离骨架内 16+ 有效样本无 `2517/2557`，且 stage3 触发率高、均值明显优于对照时，才值得移植回完整主线。
- Yuan-only 只有在同一隔离骨架内稳定解锁 `703/704` 且相对对照有可重复净收益时，才值得移植；单局或 4 局全高分不足以说明 Yuan 有稳定收益。
- 完整主线仍必须回到 25+ 单人房间样本验证，最低分至少不低于 `n518b` 的 `2657`，否则不能替代稳定版。

## 2026-05-08 16:30 UTC `2757` 分数分解审计

本节只基于单人房间评测日志和 `docs/generated/game2_room_score_factors.json` 的 1114 个有效样本，没有新增上传、没有 batch、没有上天梯。重要限制：`match_download.json` 只暴露 Rose/case0 的 `answer_result`；Z/F、Poker、Yuan 没有逐案 `answer_result` 或逐案 score，所以“各剧本多少分”只能做差分归因，不能伪装成后台精确分项。

当前最可信的 `2757` 构造是：

| 组件 | 当前可见状态 | 可证增量 | 证据 |
| --- | --- | ---: | --- |
| 直接答案/基础包 | Rose 三项全真，后续案使用当前直接答案文本；无 Z/F err8 隐藏分、无 Poker stage2/3、Yuan 无推进 | `2520` 基底 | `mTmTmT + z_err8=0 + poker=None/1 + Rose stage6=attitude` 最高 `2520` |
| Rose 纠错/尾段 | `murderer/motivation/method=True`，stage6 非 `喜欢你？` 类低分触发 | 已含在 `2520`；错误 motivation 约 `-200`，`喜欢你？` 尾段约 `-40` | stage3 下 Rose 全真最高 `2757`，motivation false 最高 `2557`；like 档常比 attitude/other 低 `40` |
| Z/F | 双 `KeyError('8')` 隐藏触发，当前 `z_err8_count=2` | `+137` | 同 Rose/Poker 空档：`2520/2480 -> 2657/2617` |
| Poker | stage3，证据 `001/002/101/102/103/104/201/202/203/204/205` | `+100` | 在 Rose 全真、Z/F err8=2 下：无/1 阶最高 `2657`，stage2 最高 `2707`，stage3 最高 `2757` |
| Yuan | 多数高分样本只有 evidence `001`；`703/704` 只是小样本 | `+0` 已证明增量 | stage3 高分主签名中 Yuan `001` 已可到 `2757`；`001/703/704` 仅 4 样本全 `2757`，不能证明独立收益 |

因此现在应把 `2757` 写成：

```text
2520 基础包
+ 137 Z/F hidden err8
+  50 Poker stage2
+  50 Poker stage3
= 2757
```

各剧本当前能确认的分数状态：

- Rose：唯一有直接判题结果的剧本。`2757` 样本中 Rose 三项全真；从同档对照看，motivation 错误会损失约 `200`，`喜欢你？` 类 stage6 触发会损失约 `40`。Rose 的绝对分不能单独拆出，因为 `2520` 基础包同时包含后续案的直接答案分。
- Z/F：当前已证明的额外贡献是 `+137`。这是从 `z_err8=0` 到 `z_err8=2` 的差分，不是 Z/F 绝对分。
- Poker：当前已证明的额外贡献是 `+100`，其中 stage2 约 `+50`，stage3 再约 `+50`。stage1 与无 Poker 推进没有稳定分差。Poker stage3 是上限来源，但不是稳定来源。
- Yuan：当前没有证明稳定正增量。Yuan 直接答案可能已经在 `2520` 基础包里，但 `703/704` 的额外调查分尚未被单独证实。

低尾分布仍是核心不确定性。完全相同的核心可见签名：Rose 全真、Rose stage6=`other`、Z/F 双 err8、Poker stage3、Poker 全套 `001/002/101/102/103/104/201/202/203/204/205`、Yuan 只有 `001`，共有 464 样本，分布为 `2517 x21, 2557 x41, 2627 x1, 2717 x122, 2757 x279`。这说明 `2717/2557/2517` 不是简单的“少拿了 Poker 证据”或“Yuan 没推进”，而是还有不可见隐藏判分、效率或内部状态变量。

## 2026-05-08 18:34 UTC `n547/n548` 置零与组合隔离审计

本轮继续只用 Game53 单人房间评测，不使用 batch，不上天梯；上传实体名 `n547*`、`n548*` 与备注 `r` 均保持中性，账号守卫为 `thebeginning / user_id=2646`。补跑后 `docs/generated/game2_room_score_factors.json` 已刷新到 `1277` 个有效分数因子样本。

### 置零骨架是否稳健

`n547a/n547b` 验证了“快速放弃非目标剧本”的下限骨架：

| label | 模式 | 5 局分布 | 结论 |
| --- | --- | --- | --- |
| `n547a` | 所有案 hard-zero，固定提交 `无名氏/无/无` | `207 x5` | 可稳定进入后续四案，零基线是 `207` |
| `n547b` | 所有案 soft-zero，尽量选错嫌疑人 | `207 x5` | 与 hard-zero 同分，说明置零文本本身不引入额外噪声 |

因此可以确认：前案直接错答不会中断后案；“每次只测一个剧本，其他剧本置零”机制上可行。但它的绝对分数只适合同一骨架内做差分，不应直接外推到完整主线。

### 直接答案组件

相对 `207` 零基线，直接答案组件在 `n547i-o` 中表现为严格可加：

| label | 开启直接答案 | 5 局分布 | 相对零基线 |
| --- | --- | --- | ---: |
| `n547i` | Rose direct | `807 x5` | `+600` |
| `n547j` | Z/F direct | `807 x5` | `+600` |
| `n547k` | Poker direct | `407 x5` | `+200` |
| `n547g` | Yuan direct | `207 x5` | `+0` |
| `n547h` | Yuan probe | `207 x5` | `+0`，可见 `yuan_stage=1`，但无 raw score 增益 |
| `n547l` | Rose+Z/F direct | `1407 x5` | `+1200` |
| `n547m` | Rose+Poker direct | `1007 x5` | `+800` |
| `n547n` | Z/F+Poker direct | `1007 x5` | `+800` |
| `n547o` | Rose+Z/F+Poker direct，Yuan 置零 | `1607 x5` | `+1400` |

这给出当前最可靠的“案级直接答题分”：

```text
zero baseline = 207
Rose direct   = +600
Z/F direct    = +600
Poker direct  = +200
Yuan direct   =   +0
R+Z+P direct  = 1607
```

`n547c` all-direct 追加 `n547c_more` 后合计为 `1607 x9, 1407 x1`。它与 `n547o` 的 `1607 x5` 基本一致，说明 Yuan direct 不是稳定负组件；那一局 `1407` 更像已知隐藏 `-200` 低尾在 direct 骨架中的偶发样本。`n547g` 单独 Yuan direct 和 `n547h` Yuan probe 都是 `207 x5`，仍没有独立正分证据。

### 单剧本 full 调查增量

`n547d/e/f` 在“目标案 full，其余置零”骨架下给出第一层调查增量：

| label | 目标案 full | 5 局分布 | 相对对应 direct |
| --- | --- | --- | ---: |
| `n547d` | Rose full | `1257 x3, 1217 x2` | Rose direct `807` 之上 `+410/+450` |
| `n547e` | Z/F full | `1407 x5` | Z/F direct `807` 之上 `+600` |
| `n547f` | Poker full | `507 x5` | Poker direct `407` 之上 `+100` |

这说明 Rose 与 Z/F 的 full 调查分远大于 Poker 单独 full 的增量。Poker 更像最后的 `+50/+50` 上限层，而不是主要基础分来源。

### full/direct 组合隔离

为避免“单案 full 不能外推到完整主线”，本轮新增 `n548a-j` 组合分派器。Saiblo 恢复后已补齐主要缺口，并额外扩了三案 full + Yuan zero/direct/probe 的样本。

| label | 组合 | 有效分布 | 可见因素 | 解释 |
| --- | --- | --- | --- | --- |
| `n548a` | Rose full + Z/F direct + Poker direct + Yuan zero | `2017 x2, 2057 x3` | Rose 全真，Z err8=0，Poker None | 在 `1607` direct 基底上加 Rose full `+410/+450` |
| `n548b` | Z/F full + Rose direct + Poker direct + Yuan zero | `2207 x5` | Rose 全真，Z err8=2，Poker None | 在 `1607` direct 基底上稳定加 Z/F full `+600` |
| `n548c` | Poker full + Rose direct + Z/F direct + Yuan zero | `1707 x4, 1607 x1` | stage3 时 `+100`，stage1 时 `+0` | Poker full 单独只贡献 stage 层，不补基础分 |
| `n548d` | Rose full + Z/F full + Poker direct + Yuan zero | `2657 x4, 2417 x1` | Rose 全真，Z err8=2，Poker None | 高样本等于主线“无 Poker 推进”上限 `2657`；同形低样本 `2417` 说明低尾已可在 Rose+Z/F full 组合中出现，不是 Poker stage3 独有 |
| `n548e` | Rose full + Poker full + Z/F direct + Yuan zero | `2157 x3, 2117 x1, 1957 x1` | Z err8=0，Poker stage3 | Rose full + Poker full 可加，但仍可出现 Rose late `-40` 与隐藏 `-200` |
| `n548f` | Z/F full + Poker full + Rose direct + Yuan zero | `2307 x5` | Z err8=2，Poker stage3 | Z/F full + Poker stage3 在该骨架下稳定为 `+700` |
| `n548g` | Rose full + Z/F full + Poker full + Yuan zero | `2757 x12, 2717 x3` | 三案 full，Yuan zero | 当前完整高分骨架；`2717` 由 Rose stage6 late `-40` 解释 |
| `n548h` | Rose full + Z/F full + Poker full + Yuan direct | `2757 x11, 2717 x2, 2657 x2` | 三案 full，Yuan direct | Yuan direct 未提高上限，且样本中引入/伴随更多掉档 |
| `n548i` | Rose full + Z/F full + Poker full + Yuan probe | `2757 x10, 2717 x4, 2517 x1` | 三案 full，Yuan probe | Yuan probe 未提高上限；`2517` 反驳“小样本 Yuan 704 可过滤低尾”的假设 |
| `n548j` | Rose+Z/F+Poker direct + Yuan probe | `1607 x5` | direct 基底，Yuan stage1 | Yuan probe 在 direct 基底仍是 `+0` |

从 `n548a-j` 可更新 `2757` 的更细分构造：

```text
207  zero baseline
+600 Rose direct
+600 Z/F direct
+200 Poker direct
+410/+450 Rose full investigation layer
+600 Z/F full investigation / hidden layer
+0/+50 Poker stage2
+0/+50 Poker stage3
= 2417/2617/2657/2707/2757 等档位
```

这个拆法比旧的 `2520 + 137 + 50 + 50` 更适合指导隔离实验。旧写法仍可用于“完整主线样本的高层差分”，但它把 direct 答案、Rose full、Z/F full 混在了 `2520` 基础包里。

### 当前关键结论

- “其他剧本置零、只测一个剧本”是可执行且稳定的；hard-zero 与 soft-zero 都稳定 `207`，并且后续四案照常进入。
- 更高效的隔离骨架不是全错答，而是“非目标案用 direct 正确答案，目标案 full/probe”。这能把零基线从 `207` 提到 `1607`，显著降低无关噪声。
- Rose direct、Z/F direct、Poker direct 的可证贡献分别是 `+600/+600/+200`；Yuan direct/probe 当前没有独立正分。
- Rose full 的增量约 `+410/+450`，Z/F full 稳定 `+600`，Poker full 在当前已知问法下只带来 `+100` 上限层。
- `n548d` 的 `2417/2657` 同形差异证明，隐藏低尾不应再只归咎于 Poker stage3；Rose+Z/F full 组合已经足够产生 `240` 分跨度。
- `n548g/h/i` 证明 Yuan zero/direct/probe 在三案 full 骨架下都没有超过 `2757`；direct/probe 还分别出现 `2657` 或 `2517`，因此不能把 Yuan 当作已知正组件。
- 现在支持“受控骨架内”的独立 A/B 和并发准备：例如固定 Rose+Z/F full + Yuan zero，只比较 Poker 变体；或固定 Rose+Z/F+Poker full，只比较 Yuan 变体。但结论必须限定在该骨架内，不能把全零单案结果直接外推到完整主线。

### Rose stage6 触发序号新因子

补充挖掘后，`Game2/tools/analyze_room_score_factors.py` 已新增 `rose_stage6_index` 字段，表示 Rose 案第几条回复首次达到 stage6；`docs/generated/game2_room_score_factors.md/json` 已刷新出 `rose6_idx` 列。

这个因子解释了之前混在一起的一部分低尾：

- 在 `n548a` 和 `n547d` 中，`rose6_idx=28` 对应高档，`rose6_idx=32` 稳定低 `40` 分：`n548a` 为 `2057 -> 2017`，`n547d` 为 `1257 -> 1217`。
- 在 Rose 全真、Z/F 双 err8、Poker None/1 条件下，`rose6_idx=28` 主要是 `2657`，而 `rose6_idx=29..33` 主要是 `2617`。这就是稳定的 `-40` Rose 延迟触发层。
- 在 Poker stage3 条件下，`rose6_idx=28` 主要是 `2757`，`rose6_idx=29..33` 主要是 `2717`。这解释了大批 `2717`，它不是 Poker 证据少，而是 Rose stage6 触发晚。
- 剩余 `-200` 低尾仍未解释：同样 `rose6_idx=28` 下仍有 `2457/2557`，同样延迟触发下也有 `2417/2517`。Z/F stage7 首次触发序号、Z/F stage 计数、`KeyError('8')` 次数、常见文本关键词均无法区分。
- 进一步按 Rose 凶手/银行家/舞女、Z/F A/C/D/E 角色中文名分组，也没有出现可用硬过滤；坏样本分散在各个 NPC 名字上，不支持“特定名字混淆导致低尾”的简单假设。

因此当前更准确的高分模型是：

```text
基础 full/direct 组合高档
- 40  Rose stage6 延迟触发，通常 rose6_idx=29..33
-200  未识别隐藏低尾
+ 50  Poker stage2
+ 50  Poker stage3
```

这比“Poker stage3 不稳定”更精确：Poker stage3 只是把当前档位整体上移 `+100`，真正需要破解的是 Rose stage6 延迟和另一个不可见 `-200` 隐藏变量。

### 2026-05-08 19:20 UTC 本地格子复核

本小节是补跑前的本地格子复核；当时没有新增上传、没有新增房间评测、没有 batch、没有上天梯，只复核已有有效样本和 `n547/n548` 单人房间结果。Saiblo 房间 API 当时仍处于读超时状态；后续恢复和补跑结果见下方更新。

当前最可操作的分数格子如下：

```text
207  zero baseline
+600 Rose direct
+600 Z/F direct
+200 Poker direct
+410 Rose full late layer
+ 40 Rose full fast bonus, rose6_idx=28
+600 Z/F full layer
+ 50 Poker stage2 layer
+ 50 Poker stage3 layer
-200 unexplained hidden low-tail
```

由此可预测并解释当前主要档位：

```text
2657 = 207 + Rose direct/full fast + Z/F direct/full + Poker direct
2617 = 2657 - 40  Rose stage6 late
2457 = 2657 - 200 hidden low-tail
2417 = 2657 - 40 - 200
2707 = 2657 + 50  Poker stage2
2667 = 2707 - 40
2507 = 2707 - 200
2467 = 2707 - 40 - 200
2757 = 2657 + 50 + 50  Poker stage3
2717 = 2757 - 40
2557 = 2757 - 200
2517 = 2757 - 40 - 200
```

这套格子同时覆盖 `n548d` 的 `2657/2417` 和旧主线的 `2757/2717/2557/2517`。关键含义是：`2757` 并不是另一个独立大组件，只是在 Rose+Z/F full 高档上叠加 Poker stage2/stage3 的 `+100`；当前最大未知不是 Poker 证据是否可见，而是 `-200` 隐藏低尾何时发生。

`1277` 个有效样本中尚无任何 `>2757`。Yuan 相关样本也没有显示上穿：`yuan_stage=None` 和 `yuan_stage=1` 的最高分都只是 `2757`；三案 full 扩样中 Yuan zero/direct/probe 分别为 `2757 x12, 2717 x3`、`2757 x11, 2717 x2, 2657 x2`、`2757 x10, 2717 x4, 2517 x1`。因此“理论上限可能更高”目前仍是待证假设，证据上不能把 Yuan 视为已知正组件。

按 `mTmTmT + z_err8=2` 筛选已有样本后，可见分布也符合该格子：

- Poker None/1 且 Rose fast 主要在 `2657`，但有 `2457` 隐藏低尾；Rose late 主要在 `2617`，但有 `2417` 隐藏低尾。
- Poker stage2 且 Rose fast 主要在 `2707`，隐藏低尾是 `2507`；Rose late 主要在 `2667`，隐藏低尾是 `2467`。
- Poker stage3 且 Rose fast 主要在 `2757`，隐藏低尾是 `2557`；Rose late 主要在 `2717`，隐藏低尾是 `2517`。

因此“每次只独立测一个剧本，其他剧本按 0 分去答”已经被 `n547a/b` 证明机制可行，但更推荐的实验骨架是“非目标剧本 direct 正确答案，目标剧本 full/probe”。原因是全零骨架的绝对分太低，只能证明后案不中断；direct 骨架保留 `1607` 稳定基底，更适合同一骨架内做 A/B，同时不会把无关 full 调查的隐藏低尾混进目标变量。

并发拆线判断更新为“技术上支持，统计上必须受控”：

- 支持并发只读分析，或准备互不覆盖写集的候选目录。
- 支持并发跑不同中性实体和不同输出目录的房间评测，只要每条线固定同一背景骨架、只改目标剧本、样本结果按 label 分开汇总。
- 不支持把“其他案全零”的绝对分当作完整主线收益，因为全零骨架会移除 Rose/Z/F full 组合中的 `-40/-200` 低尾暴露面，也会丢掉 direct 基础分。
- Poker 线的合理骨架是 Rose+Z/F full + Yuan zero；Yuan 线的合理骨架是 Rose+Z/F+Poker full。Yuan 线优先级较低，因为 `n548h/i/j` 没有正收益证据。

### 当前完成度审计

目标是“尽可能彻底地通过大量实验分析每个剧本的实质分数贡献，在有清晰分析后再考虑高效迭代或并行迭代”。按当前真实证据，目标尚未完成：

| 要求 | 当前证据 | 状态 |
| --- | --- | --- |
| 验证置零是否会阻塞后案 | `n547a/n547b` hard-zero/soft-zero 均 `207 x5`，且后续案继续进入 | 已完成 |
| 拆出 direct 基础分 | `n547i-o` 证明 Rose `+600`、Z/F `+600`、Poker `+200`、Yuan `+0`，R+Z+P direct + Yuan zero 为 `1607 x5` | 基本完成 |
| 复核 all-direct + Yuan direct 异常 | `n547c+n547c_more` 为 `1607 x9, 1407 x1`，更像偶发隐藏低尾而非稳定 Yuan direct 负分 | 基本完成 |
| 拆出单剧本 full 增量 | `n547d/e/f` 证明 Rose `+410/+450`、Z/F `+600`、Poker `+100` | 已完成 |
| 拆出两两 full 交互 | `n548a-f` 已补齐，且符合 direct/full 格子；`n548d/e` 各暴露一次隐藏 `-200` | 已完成 |
| 拆出三案 full 与 Yuan 条件贡献 | `n548g/h/i` 合计各 15 样本，Yuan zero/direct/probe 均未超过 `2757`；`n548j` direct+Yuan probe 为 `1607 x5` | 基本完成 |
| 解释主要低尾 | Rose `rose6_idx` 已解释 `-40`；`-200` 隐藏低尾仍未解释 | 未完成 |
| 判断是否支持 Poker/Yuan 并发评测 | 机制上支持受控并发；统计结论必须固定骨架，且 Yuan 线暂不应占主优化资源 | 基本完成 |

因此当前可以把“置零/组合拆分”视为足够指导下一轮优化，但不能把“隐藏 `-200` 低尾解释”视为完成。下一轮若拆并发，应优先让 Poker 线在 Rose+Z/F full + Yuan zero 骨架下找更高上限或更低掉档率，Yuan 线只作为低优先级未知组件搜索。

### 2026-05-08 19:31 UTC 隐藏 `-200` 低尾再挖掘

本轮继续只读本地日志，没有新增上传或房间评测。筛选 `mTmTmT + z_err8=2` 且分数落在“预测高档”或“预测高档 -200”的 comparable 样本，共 `990` 条，其中隐藏低尾 `106` 条。再单独筛 Poker stage3 comparable 样本，共 `524` 条，其中低尾 `66` 条。

复核的可见特征包括：`sample_count`、`decoded_stdin_records` 长度、各案起始 index、各案 segment 长度、reply 数、Poker/Yuan evidence 数、Poker 常见关键词（`Joker/医生/人口贩卖/聊天记录/电脑/塑料盒/冰柜/刀具/寄送/自杀/伪装` 等）、Z/F 常见关键词、Yuan 投票/笔迹/手机/照片/假发关键词。结论仍是高度重叠，不能给出稳定硬过滤。

唯一低尾为 0 的简单条件曾只覆盖很小样本：Poker stage3 下 `yuan_ev_count>=2`、`Yuan 49/24/23/笔迹` 关键词、或 `poker_ev_count==10` 均只有 `8` 个样本，来自 `n546c/n546e` 的 Yuan `704` 或 `703/704` 小样本，分布为 `2757 x7, 2717 x1`。`n548i` 三案 full + Yuan probe 扩样后出现 `2517 x1`，已反驳“Yuan 704/703+704 可稳定过滤隐藏低尾”的假设。

### 服务器状态与补跑

`2026-05-08 18:26-18:34 UTC` 期间，Saiblo API 连续在 `POST /api/rooms/` 和 `GET /api/profile/` 上读超时。已落盘本线程专用补跑脚本：

```bash
Game2/runtime/recovery_eval_queue/20260508_175135/retry_n548_failed.sh
```

脚本已加固为按 `end_state=OK && score>0` 统计有效样本；服务器恢复后低频补跑缺口，不再把无效超时样本算作完成。`2026-05-08 21:33:51 UTC` watcher 检测到恢复并触发回调，已补齐 `n548d_more2`、`n547c_more`、`n548e-j`，随后又扩了 `n548g/h/i_more2`。当前脚本不再是阻塞项。

- `docs/generated/game2_room_eval_summary.md/json`
- `docs/generated/game2_room_score_factors.md/json`

下一步不是继续盲目改主线，而是在固定骨架中做更小粒度 A/B：Poker 线可独立推进，但要保留 Rose+Z/F full + Yuan zero 的背景；Yuan 线可并行低优先级探索未知组件，但当前数据不支持把它视为主突破口。

### 2026-05-09 04:31 UTC Yuan `+40` 聊天组件复核

本轮继续只用 Game53 单人房间评测，不使用 batch，不上天梯；上传实体名 `n549*` 和备注 `r` 均保持中性。`docs/generated/game2_room_score_factors.json` 已刷新到 `1539` 个有效因子样本。

#### direct 骨架下的 Yuan `+40`

`n549a-e` 都固定 Rose/Z/F/Poker direct 基底，目标是只观察 Yuan 探针是否能在 `1607` 上方产生稳定新分：

| label | 目的 | 有效分布 | 结论 |
| --- | --- | --- | --- |
| `n549a` | corrected Yuan 投票/动机长问，修正旧 `49人中2人不投却48票` 文本 | `1607 x4`，另 1 局平台 UE | 无增益，Yuan 仍 stage1，证据 `001/703/704` |
| `n549b` | 加入黑车、保安、生物馆、行李箱等固定追问 | `1607 x5` | 无增益 |
| `n549c` | 加入动态生物馆/保安 followup | `1607 x2, 1647 x1` | 首次观测 direct 基底上的 Yuan-like `+40` |
| `n549d` | 窄化到投票异常、凌晨照片/死亡时间、生物馆/李海天线 | `1607 x8, 1647 x1, 1407 x1` | `+40` 可复现；`1407` 这一局是 Rose direct motivation false |
| `n549e` | 与 `n549d` 同聊天，但 Yuan 最终答案强制置零 | `1607 x8, 1647 x1, 1407 x1` | `+40` 仍出现，证明它来自聊天/隐藏调查组件，不依赖 Yuan 最终答案 |
| `n549g` | 去掉 targeted 三问，只保留 broad + later followup | `1607 x8, 1407 x2` | 没有 `1647`，说明后段 followup 单独不足 |
| `n549h` | 保留 broad + targeted 三问，去掉 later followup | `1607 x9, 1647 x1` | `+40` 仍可复现，说明 targeted 阶段是必要候选，后段 followup 非必要 |

关键结论：

- Yuan `+40` 已不再只是单局误差：`n549c/d/e/h` 合计出现 `1647 x4`。
- `+40` 不是最终答案文本分。`n549e` 在 Yuan 最终提交 `无名氏/无/无` 后仍出现 `1647`。
- `+40` 目前仍没有可见 stage/evidence 标识。`1647` 样本仍是 `yuan_stage=1`，证据仍只到 `001/703/704`。
- targeted 三问比后段 followup 更接近触发源；候选包括投票异常 `46/47/24/23`、凌晨 1 点照片/伪造死亡时间/真实死者、以及生物馆实际目击者/尸块线。但它们仍不是充分条件，`n549h` 也只有 `1/10` 触发。
- direct 骨架的 `1407` 有两类：`n549d` 中一局可见 Rose `motivation=false`；`n549e` 中一局 Rose 全真，更像 direct 骨架自身的隐藏 `-200`。

这说明 Yuan 线确实存在一个此前未拆出的 `+40` 小组件，但触发率低，且当前可见日志不足以硬判触发条件。

#### 完整骨架叠加测试

为了验证该 `+40` 是否能把当前完整上限从 `2757` 推到 `2797`，`n549f` 从 `n548g` 复制 Rose+Z/F+Poker full 骨架，只把 Yuan zero 换成 `n549e` 的聊天置零探针。

`n549f` 15 局分布：

```text
2757 x11
2717 x2
2617 x1
2557 x1
```

因子复核：

- `2757/2717` 仍符合旧格子：Poker stage3、Z/F err8=2，`2717` 由 Rose `rose6_idx=31/32` 的 `-40` 解释。
- `2617` 是 Rose late 且 Poker 只到 stage1，相当于掉了 Poker stage2/3 的 `+100`。
- `2557` 是 Rose fast + Poker stage3 + Z/F err8=2 下的隐藏 `-200`。
- 没有任何 `>2757`，也没有 `2797`。

当前解释：direct 骨架中的 Yuan `+40` 很可能是一个小型聊天/隐藏调查分，但在 Rose+Z/F+Poker full 高档下没有证明可叠加；可能被完整骨架的计分上限、触发时序或不可见低尾变量吞掉。`n549f` 不能提升主线，也不应替代 `n548g` 或历史稳定版本。

#### 下一步口径

- Yuan 线继续有研究价值，但优先目标应从“直接上主线”降级为“在 direct 骨架中把 `1647` 触发率从约 10% 提高到可控”。
- 下一轮应围绕 `n549h` 的 targeted 阶段继续拆成单问 A/B：投票异常、凌晨照片/死亡时间、以及生物馆实际目击者三组分别验证。只有某组能稳定高于 direct 对照时，才值得回到完整骨架复测。
- 完整骨架只有在 direct 骨架能稳定触发 `+40` 后再复测；当前 `n549f` 已经证明低频触发不足以突破 `2757`。

### 2026-05-09 05:23 UTC Yuan targeted 单问拆分

本轮继续只用单人房间评测，不使用 batch，不上天梯；上传实体名 `n549i/j/k` 和备注 `r` 保持中性。`docs/generated/game2_room_score_factors.json` 已刷新到 `1665` 个有效因子样本。

`n549i/j/k` 都沿用 Rose/Z/F/Poker direct 基底和 Yuan 最终置零，只把 `n549h` 的三条 targeted 问句拆成单问：

| label | targeted 单问 | 有效分布 | 结论 |
| --- | --- | --- | --- |
| `n549i` | 投票异常：`46/47/24/23/异笔迹` | `1607 x10` | 不能独立触发 `+40` |
| `n549j` | 凌晨 1 点照片、伪造死亡时间/死者身份、lo 裙/栗色假发/尸块 | `1607 x9, 1407 x1` | 不能独立触发 `+40`，且仍有 direct 低尾 |
| `n549k` | 生物馆实际目击者：张壹传闻、夜跑实际看到谁、生物馆/世纪林/尸块关系 | `1607 x8, 1647 x1, 1407 x1` | 单问足以复现 `+40`，但触发率仍只有约 10% |

轨迹复核：

- `n549h` 的唯一 `1647` 与“实际看到的是张朔”完全重合，但这个特征只有 1 个样本，不能直接当硬条件。
- `n549k` 的 `1647` match `8102632` 没有张朔，而是 NPC 回答“可不是张壹，实际看到赵一橙从生物馆出来”，说明 `+40` 更可能来自“纠正张壹传闻并给出实际目击者”的调查动作，而不是特定人名张朔。
- 投票精确解释、`24 比 23`、异笔迹、lo 裙/栗色假发/嫁祸等关键词在多个 `1607` 样本中也出现，不能解释 `+40`。
- 当前还不能证明 `+40` 与最终 Yuan 答案有关；所有这些版本继续 Yuan 置零。

进一步围绕 `n549k` 拆出三个假设：

| label | 变体 | 有效分布 | 结论 |
| --- | --- | --- | --- |
| `n549l` | 去掉 broad 两问，直接问生物馆实际目击者 | `1607 x10` | broad/703/手机等上下文很可能是必要前置；裸问失效 |
| `n549m` | 保留 broad，但把生物馆问题收窄为“只说真实看到的人、时间、地点” | `1607 x10` | 收窄问句失效，说明宽问句里的世纪林/尸块/袁樱瞳死亡关系不能直接删除 |
| `n549n` | 保留 broad，尝试只向 broad 回复中最像“路线/目击者”的 NPC 追问原宽问句 | `1607 x8, 1647 x2` | 触发率高于 `n549k`，但轨迹复核发现过滤器经常回退成全员追问，不能证明目标选择有效 |

`n549n` 的两个 `1647` 样本说明 route-like 方向没有破坏触发，可能把触发率从 `n549k` 的 `1/10` 提到 `2/10`。但事后复核发现 `n549n` 的过滤条件把“普通同学”回复中的泛化手机字样误判为 winner-like，导致 witness_targets 为空时回退到全员追问。因此 `n549n` 不能作为“目标 NPC 选择有效”的强证据。

阶段性判断：Yuan 的低分突破口已经从泛化 targeted 三问收窄到“broad 后用宽生物馆/世纪林/尸块关系问句纠正张壹传闻”。下一步应继续改进目标 NPC 识别和问句顺序，先把 direct 骨架 `1647` 触发率提高到可控，再做完整骨架复测。

下一轮候选结果：

| label | 变体 | 有效分布 | 结论 |
| --- | --- | --- | --- |
| `n549o` | 修正 `n549n` 的 witness 过滤，避免普通同学因泛提手机被排除；没有目标则不回退全员 | `1607 x9, 1407 x1` | 单独追问过滤出的 witness 目标失效 |
| `n549p` | 保留 broad，固定只追问 Yuan 可见 NPC 列表最后一人 | `1607 x9, 1647 x1` | 可触发 `+40`，但不优于 `n549k` 的全员宽问 |

这一轮反证了“只要找到正确目击者就能稳定触发”的简单假设。`n549o` 没有 `1647`，说明过滤后的单 NPC 追问过窄；`n549p` 只复现 `1/10`，说明最后一人位置假设最多能保留低频触发，不能提升触发率。当前更合理的解释是：`+40` 需要 broad 后的宽生物馆问题，但可能还依赖三类角色的组合回答、全员追问顺序、或随机身份实例中的隐藏条件。

下一步不应继续单点删减。更有价值的方向是：

- 对 `n549k/n549p/n549n` 的 `1647` 与 `1607` 做身份/角色组合审计，找出是否依赖“手机持有人、教师、普通同学/夜跑者”的具体排列。
- 如果继续上传，优先测试“全员宽问但调整顺序”，例如先问最后一人再问教师/手机持有人，验证是否能保留全员上下文并提高触发率。
- 在 direct 骨架触发率未超过当前约 `10%-20%` 前，不应移植到完整 `2757` 骨架。

### 2026-05-09 06:39 UTC Yuan `+40` 全零骨架验证

本轮继续只用单人房间评测，不使用 batch，不上天梯；上传实体名 `n550a` 和备注 `r` 保持中性。`docs/generated/game2_room_score_factors.json` 已刷新到 `1925` 个有效因子样本。

`n550a` 是一个受控隔离实验：其他剧本全部硬置零，只在 Yuan 案执行 broad 两问 + 宽生物馆问，然后尝试把“实际从生物馆跑出的人”作为凶手提交。

结果：

```text
n550a: 207 x8, 247 x2
```

含义：

- `247 = 207 + 40`，说明 Yuan 的 `+40` 在其他三案全零时也能独立出现；它不是 Rose/Z/F/Poker direct 基底中的副作用。
- `n550a` 的最终答案没有产生额外分数。如果“生物馆实际目击者”作为 murderer 能拿 Yuan 答案分，分数应明显高于 `247`；实际只有 `207/247` 两档。
- 两个 `247` 样本都没有可见 stage/evidence 变化，仍停在 `yuan_stage=1`。一个样本回复“张朔从生物馆跑出”，另一个回复“许清和从生物馆出来”但本地提取器没有抓到，仍然拿到 `+40`。因此特定人名和提交 murderer 都不是必要解释。
- `n550a` 同时反证了一个看似合理但错误的方向：把生物馆线索指向的人写入 Yuan 最终答案不能打开答案分。

结合 `n549e`、`n549f` 和 `n550a`，当前对 Yuan 的更严格结论是：

- `+40` 是宽生物馆问句相关的隐藏聊天/调查分，可在 direct 骨架和全零骨架中独立出现。
- `+40` 当前没有证明能叠加到完整 `2757` 骨架；`n549f` 没有出现任何 `>2757`。
- Yuan 最终答案仍没有正分证据：`marks=False`、手机持有人/展示胜者、生物馆实际目击者这些 murderer 方向都不能稳定拿分。
- 后续 Yuan 若继续探索，优先级应是提高 `+40` 触发率，而不是继续换最终答案文本；若要测答案，也必须使用全零或 direct 固定骨架，避免把低频聊天分误判为答案分。
